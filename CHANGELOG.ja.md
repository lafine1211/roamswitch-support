# 変更履歴

[English](CHANGELOG.md) | **日本語**

RoamSwitch のユーザー向け変更点。Mac 版（1.x）と
Linux 版（別系列・1.0.x）でバージョン番号は独立しています。

---

## RoamSwitch for Linux

Linux 版（systemd + nftables）。apt / dnf / zypper で配布（GPG 署名）。
詳しくは <https://lafine.net/linux>。

### 1.9.40

- **修正: ログ監査の頻度急増通知が、直後に確認しても検知内容を確認
  できない不具合を修正**: ログテンプレート異常検知は、頻度急増を検知
  した直後にその原因となったログ行を処理済みカーソルより前として
  消費する設計のため、デーモンの自動スキャンが通知を出した直後に
  GTKアプリのLogsタブや`roamswitch audit-logs`／MCPで確認しても
  「異常なし」と表示され、後から検知内容を振り返る手段がありません
  でした。新規パターン検知と同様に、頻度急増の検知内容も通知履歴
  （`roamswitch notifications`）に記録するようにしました。Mac版
  1.9.31の同機能修正と同じ設計変更です。

### 1.9.39

- **追加: タイポスクワッティング検知（npm/pnpm package.json、静的検知、
  Pro）**: package.jsonのdependencies/devDependencies/optionalDependencies
  を、有名なnpmパッケージ名リストと編集距離（レーベンシュタイン距離1〜2）
  で突き合わせ、`expres`→`express`・`loadash`→`lodash`のようなタイポ
  スクワッティング（似た名前で偽装した悪意あるパッケージ）の可能性を
  検出します。アプリ自体は判定にネットワーク接続を一切行いません。
  判定に使う人気パッケージ名リストは、CVEマップ群と同じ「1日1回・
  受信専用・署名検証済み」のupdaterパイプライン経由で配信され、
  アプリのリリースを待たずにリストを更新できます。参考情報であり
  断定ではなく、正規の類似パッケージ（`preact`等）は許可リストで
  一部除外しています。`roamswitch scan-typosquat <フォルダ...>`。
  Client Edition限定。

### 1.9.38

- **追加: npm/pnpm installのライフサイクルスクリプトをサンドボックス内に
  封じ込める機能を追加しました（Client Edition限定、Pro）**: これまでの
  3機能が「検知して警告する」機能だったのに対し、本機能は実際に
  preinstall/install/postinstall/prepareスクリプトの実行をbubblewrap
  (bwrap)によるファイルシステム制限下に封じ込めます。ダウンロード
  フェーズは`--ignore-scripts`でネットワーク許可のまま通常通り実行し、
  スクリプト実行フェーズのみ`~/.ssh`・`~/.aws`・`~/.gnupg`・`~/.netrc`
  ・`~/.npmrc`等の資格情報パスを空のtmpfs（ファイルは`--ro-bind
  /dev/null`）で覆い隠します。ネットワークは遮断しません（sharp・
  esbuild等の正規パッケージがpostinstallでプラットフォーム別バイナリを
  取得する動作を壊さないため）。`roamswitch install [--pm npm|pnpm]`
  サブコマンドを新設。npm/pnpm対応、yarnは非対応。bwrapが無い環境では
  スクリプト実行を拒否し、サンドボックス無しへの暗黙フォールバックは
  行いません。

### 1.9.37

- **追加: npm installの供給網リスクに対応する3機能を追加しました（Client
  Edition、一部Server Editionにも対応）**: 依存関係ロックファイル
  （package-lock.json/yarn.lock/pnpm-lock.yaml/npm-shrinkwrap.json）の
  外部改ざんをSHA-256ベースラインで常時監視する「Lockfile FIM」
  （Client/Server両対応、`roamswitch lockfile-fim`）、node_modules配下の
  package.jsonが宣言するpreinstall/install/postinstall/prepareスクリプトを
  静的に列挙する「インストールスクリプトの一覧」（Client限定、脅威判定
  ではなく一覧表示、`roamswitch scan-scripts`）、npmレジストリと通信して
  インストール済みパッケージの署名/provenanceを検証する「npm署名検証」
  （Client/Server両対応・オプトイン、`roamswitch audit-npm-signatures`）
  の3つです。npm署名検証はRoamSwitch全体で唯一npmjs.comと通信する機能で、
  既定で無効かつ実行毎の確認が必須です。3機能ともPro限定。

### 1.9.36

- **追加: 機密情報・APIキー漏洩監査が、暗号資産ウォレットのシードフレーズ
  ・秘密鍵も検知できるようになりました**: Microsoftが2026年6月に公表した
  「Crypto Clipper」マルウェア（クリップボード監視でウォレットのシード
  フレーズや秘密鍵を窃取し、送金先アドレスもすり替える）を受けて追加しました。
  BIP39ニーモニックのシードフレーズ（12〜24語）は、単語の並びが実際の
  チェックサム（SHA-256ベース）を満たすかまで検証するため、たまたま
  英文にそれらしい単語が混じっただけでは検知しません。Bitcoin秘密鍵
  （WIF形式）と拡張秘密鍵（xprv/yprv/zprv/tprv）もチェックサム検証付き
  で検知します。検知した値はAPIキーと異なり一切表示せず完全に非表示に
  し、対応方法も「失効」ではなく「新しいウォレットへの資産移動」を
  案内します。

### 1.9.35

- **修正: EULAの通信範囲の記載を実態に合わせました**: EULA §5「アップデート
  確認はapt経由のみ」という記載が、実際に発生している脅威フィード取得・
  ClamAV定義更新の受信専用通信と矛盾していたため修正しました。
- **アプリ内ヘルプ画面にサードパーティ・ライセンス表記を新設**しました
  (ClamAV(GPLv2)・Tailscale(BSD-3-Clause)・GTK3(LGPL-2.1)、10言語対応)。

### 1.9.34

- **MCP・SDK・アプリ内ヘルプ&ガイドを現行機能に完全対応させました**:
  読み取り専用MCPサーバーのツールを19種から25種に拡張し、VPN(WireGuard
  /Tailscale)・受動リンク保護・緊急ネットワーク遮断(Air-Gap、凍結中
  プロセス含む)・共有サービス自動停止・Bluetoothガード・USBガードの
  状態を新たに取得できるようにしました。ガード状態の一覧に含まれる
  設定項目も、実際の設定ファイルに合わせて拡充しています。
- URL安全性診断・機密情報漏洩監査・セキュリティログ監査・ランサムウェア
  ・カナリアの結果が、これまで日本語固定だったのを10言語(日本語・
  英語・簡体字・繁体字・韓国語・ドイツ語・フランス語・スペイン語・
  イタリア語・ポルトガル語)対応にしました。アプリ画面の該当タブでも
  同様に反映されます。
- 総合セキュリティ診断(Server Edition)の項目数表記の誤り(28→実際は
  30項目)を修正し、全文言を10言語対応にしました(従来は日英2言語の
  みでした)。Client Edition側でも一部項目(Docker検知・特権コンテナ・
  ランタイム隔離・カーネルCVE)が日本語以外で日本語のまま表示される
  不具合を修正しています。
- MCPの内蔵ナレッジベース(`get_app_help`が参照)を全面刷新し、10言語
  完全対応にしました。VPN・機密情報漏洩監査・ログ監査・通知履歴・
  ARP監視/固定・Air-Gap・共有サービス・Bluetoothガード・実証型脆弱性
  診断など、これまで説明が欠けていた機能を追加しています。
- アプリ内蔵の「ヘルプ&ガイド」タブを全面的に書き直し、これまで
  記載のなかったVPN・ARP監視/固定・受動リンク保護のフェイルクローズ
  動作・機密情報監査・クリップボード監視・緊急ネットワーク遮断・
  通知履歴・統合インシデントタイムライン等を追加しました。総合診断の
  項目数表記(24項目)も全10言語で統一しています。
- `server.conf`の`language`設定を日英2言語限定から10言語+システム
  ロケール追従に対応させました。

### 1.9.33

- **改善: EICARテスト署名の検出で通知を出さないようにしました**:
  EICAR はウイルス対策の動作確認に使う業界標準のテスト文字列で、実際の
  脅威ではありません。従来から隔離・遮断はしていませんでしたが、検出の
  たびに「🧪 EICAR テスト署名を検出（無害）」のデスクトップ通知を出して
  いました。対処の必要がない通知が繰り返し表示されると、本当に対応が
  必要な通知が埋もれてしまうため、通知は出さず通知履歴
  (`roamswitch notifications`・アプリの通知履歴・MCP)への記録のみと
  しました。fanotify ガードとダウンロードガードの両方が対象です。

### 1.9.32

- **修正: 誤検知で凍結されたプロセスを解除する手段がありませんでした**:
  ランサムウェアの高速暗号化を検知するとプロセスをSIGSTOPで凍結しますが、
  これは強制終了ではなく「あとで解除できる」ことが設計上の前提でした。
  ところが実際にはどのプロセスを凍結したかを記録する場所がなく、
  解除する操作もアプリ・コマンドのどちらにも用意されていませんでした。
  そのため正規のプログラム(バックアップ作成やパッケージのインストール等、
  短時間に大量の高エントロピーなファイルを書くもの)が誤って凍結されると、
  通知に表示されたPIDを控えて端末から手動で `kill -CONT` を実行するまで、
  そのプログラムは停止したままになっていました。凍結したプロセスを記録
  するようにし、アプリの「カナリア」タブに凍結中プロセスの一覧と解除
  ボタンを追加、コマンドにも `roamswitch frozen [list|resume <PID>|resume all]`
  を新設しました。`roamswitch emergency-restore` もこれまで一部の経路で
  凍結されたプロセスしか解除していなかったのを、全経路を解除するよう修正
  しています。**凍結は強制終了ではないため、解除するとプログラムは停止した
  位置からそのまま処理を再開します。**
- **修正: 同じ通知が履歴に2件ずつ表示される**: デーモンが検知内容を通知履歴に
  記録したうえでデスクトップ通知の表示をアプリに依頼し、依頼を受けたアプリが
  表示の際に同じ内容をもう一度記録していたため、通知履歴・`roamswitch
  notifications`・MCP のいずれでも同じ通知が約0.5秒差で2件並んでいました。
  記録は検知した側の1回だけになります。すでに記録済みの重複(最大1週間分)も
  表示されなくなります。
- **改善: ランサムウェア検知の通知が実際の動作と食い違っていました**:
  検知した時点でプロセスが既に終了していた場合や、コンテナランタイム等の
  保護対象プロセスだった場合は凍結が行われませんが、通知は一律に
  「SIGSTOPで即時凍結しました」と表示していました。実際に凍結できたかどうかで
  文面を分け、凍結できた場合は解除コマンドも併記するようにしました。
  あわせて、プロセスを特定できなかった検知(検知時点で既に終了しており、
  凍結も行われていない)はポップアップ通知を出さず通知履歴への記録のみと
  しました。調査も解除もできないPIDの警告が繰り返し表示され、対処が必要な
  通知が埋もれていたためです。

### 1.9.31（Server Editionをお使いの方は緊急対応を推奨）

- **修正: Default-Denyファイアウォールポリシーが、DockerコンテナからのすべてのアウトバウンドAPI通信を遮断していた重大な不具合**:
  `roamswitch-server`が起動・再起動のたびに適用するnftablesの`forward`チェーンが
  `policy drop`のままルールが一切無い状態で作成されており、コンテナが外部
  (取引所API・外部Webhook等)へ発信した通信はもちろん、その戻りパケットまで
  カーネルのnetfilterレイヤーで無条件に遮断されていた。外部からの実運用環境
  2件で「Dockerコンテナのアウトバウンド通信がすべてタイムアウト・502
  Bad Gatewayになる」という報告を受け特定。`forward`チェーンに
  `ct state established,related accept`と、外部インターフェース以外から
  到達したフォワード通信を許可するルールを追加し、外部インターフェースから
  直接ホストを経由しようとする通信のみ引き続き遮断する設計に修正。
  外部インターフェースの自動判定に失敗した場合は、安全側に倒して
  (フォワード通信を許可せず)警告ログを出す。**Server Editionでnftablesの
  Default-Denyポリシーを有効にしている環境で、稼働中のDockerコンテナ等が
  外部への通信を必要とする場合は本バージョンへの更新を推奨。**

### 1.9.30

- **追加: 新規テンプレート異常を通知履歴に永続化**: 「テンプレート異常」は
  一度検出すると即座に既知テンプレートとしてベースラインに吸収されるため、
  従来は再スキャンしても同じ内容が二度と「新規」として検出されず、後から
  見返す手段がなかった。新規パターン検出時に通知履歴へ記録するよう修正
  (頻度急増側は繰り返し検出され得る意図的な仕様のため対象外)。Mac版の
  同種修正と対になる。
- **保守: apt/rpm公開パイプラインの信頼性を強化**: リリース処理に排他制御
  (同時実行防止)を追加し、公開直後にビルド済みパッケージと配信URLの
  ハッシュを突合する検証ステップを新設。配信キャッシュの設定も見直した。
  今回のリリース作業中に、CDNのミラー同期タイミングによって一部の
  パッケージが一時的にダウンロード失敗する事象を確認したことへの対応。
- 実験的機能: eBPF Guardのptrace系アラートにMITRE ATT&CKマッピングを追加
  (継続、未検証のプロトタイプ)。

### 1.9.29

- **改善: ログ監査の異常通知が専門知識のないユーザーには意味不明で、
  不安を煽るだけだった不具合**: Mac版と同じ修正をクライアント版daemon・
  Server Edition・CLI・GTKアプリに適用。誤解を招く「頻度学習中」の
  注記(新規パターンは一回限りの検知)を修正し、平易な一言ガイダンスを
  通知本文に追加。これまでKPIカードの件数表示のみだったテンプレート
  異常も、CLI出力とGTKのログ監査タブの一覧に実際に表示・検索できる
  ようにした。
- **追加: ポート異常ガードの自動遮断通知に「許可」ボタンを追加**:
  信頼ネットワーク上でも新規リスニングプロセスを検知次第まず遮断する
  安全側の挙動は維持しつつ、正規のdevサーバーであればその場で解除
  できるようにした。GUIのポート監査画面の「許可」ボタンと同じ経路を
  再利用。
- 実験的機能を複数追加（統合インシデントタイムライン、XDP起動時
  default-denyゲート、悪性IP egress遮断、eBPF Guardのピンポイント
  隔離優先化、MITRE ATT&CKマッピング拡充など）。一部は未検証の
  プロトタイプとして含まれている。

### 1.9.27

- **修正: ログ監査のマスキングが`0x`接頭辞付きポインタアドレスを捕捉
  できない不具合**: 既存のhex用正規表現(`\b[0-9a-fA-F]{8,}\b`)は"0x"の
  直後に単語境界が発生しないため、XPC接続ID等の"0x..."形式アドレスを
  一切マスクできていなかった。ホスト名に融合した数字と同じ根本原因の
  別の現れ。実機(Mac)で"[0xbb38aae40] invalidated because ..."が接続
  ごとにアドレスが変わるため永久に「新規パターン」として鳴り続けて
  いたことから発覚。0x接頭辞付きの16進を先にまとめてマスクする専用の
  正規表現を追加。Mac版にも同一の修正を移植済み。

### 1.9.16 - 1.9.26

- **追加: クライアント版にもCritical Path FIM(改ざん検知)の自動バック
  グラウンド実行を追加**(1.9.17): これまでServer Edition限定だった
  event-driven fanotify監視+周期バックストップスキャンをクライアント
  版daemonにも追加。
- **重要: ログ監査のテンプレート化にあった根本的な数値マスキングの
  バグを修正**(1.9.18): 数字と英字が隣接する箇所でマスキングが効かず、
  タイムスタンプ差だけで同一ログが毎回「新規パターン」扱いされていた
  問題を修正。本番環境の既知テンプレートが58.8%減に収束することを確認。
- **重要: Server Editionでクライアント専用コマンドが誤情報を返す不具合、
  および複数のセキュリティ操作が実際の成否を確認せず成功を偽装していた
  不具合を修正**(1.9.19): `emergency-restore`・カーネル/マウント強化・
  `roamswitch airgap`等、複数箇所で「実行した」ではなく「成功を確認
  した」場合のみ成功表示するよう修正。
- **追加: リソース枯渇・プロセス異常検知ガード(Server Edition限定)**
  (1.9.20〜1.9.22): メモリ枯渇型DoS・UAFクラッシュループ・ゾンビ
  プロセス数・システム負荷の持続的増加を検知し、eBPF Runtime Guard/
  Critical Path FIMのイベントと相関させて確信度を付与。同時期に
  クライアント版daemonの通知履歴記録漏れ(28箇所中26箇所)、DOCKER-USER
  チェーンの起動順序競合、28項目セキュリティ診断の誤検知バグ7件も修正。
- **ログ監査・ランサムウェア検知の誤検知を多数解消**(1.9.20〜1.9.25):
  `cp`/`mv`/`pip`のランサムウェア誤検知、anacron・tailscaled・cron
  セッション等の定型ノイズ、AppArmorプロファイル判定の一般化、ログ監査
  が自身の通知を無限に再検知するループ、許可リスト判定が短命プロセスで
  機能しない不具合などを個別に修正。
- **修正: ARPスプーフィング検知がDockerブリッジ内部IPを誤検知**
  (1.9.24)。GUI通知履歴タブが更新されない不具合も修正。
- **修正: `audit-secrets`のディレクトリ再帰スキャンがシンボリックリンクの
  循環で無限再帰し、CPU約96%に張り付いたまま終了しない不具合**
  (1.9.26、重要): リンクを解決しない`DirEntry::file_type()`へ変更し、
  シンボリックリンクは常にスキップするよう修正。

### 1.9.6 - 1.9.15

- **ログ監査の自動バックグラウンド実行をクライアント版daemonにも追加**:
  Server Editionのみ自動実行していた非対称を解消。
- **通知履歴(過去1週間保持)を追加**: `roamswitch notifications`・GUI・
  MCP・SDKから参照可能。
- **クリップボード上のClickFix型不正コマンド検知を追加**。
- **ログ監査の誤検知を多数解消**: daemon自身の起動バナー・自己申告ログ、
  snapd/gnome-shellのOSノイズ、Ollama/Ubuntu Proの定常ログを除外。
  systemdユニットのライフサイクル報告は、個別サービス名の列挙ではなく
  「送信元がsystemd自身(journaldのカーネル認証情報により偽装不可能)、
  かつ固定文言に一致」という構造的な判定に変更し、未知サービスにも
  自動対応。
- **daemon再起動時の新旧プロセス競合、および監査ウィンドウの重複に
  よる二重通知・学習消失を解消**。
- **通知履歴が一般ユーザーのCLIから常に空に見えていた不具合を修正**
  (root権限daemonとの保存先不一致)。
- **通知本文を改善**: 「新規パターンN件・頻度急増K件」の内訳と、
  読みやすい実ログ行の例示を表示するように。

### 1.9.0 - 1.9.5

- **Wi-Fiなりすまし(Evil Twin SSID)警告**: 過去に接続したWi-Fiの
  {SSID, ゲートウェイMAC}を学習し、酷似した未知のSSIDを警告。
- **USB HIDキーストロークの異常検知(BadUSB補助シグナル)**、
  **ランサムウェア・カナリアの被害範囲(blast radius)表示**、
  **Link GuardのJA3 TLSフィンガープリント照合基盤**を追加。
- **ログ監査の頻度急増検知を、テンプレート自身の過去の頻度と比較する
  方式に再設計**: 正規の定期ジョブや`apt upgrade`起因のバーストを
  毎回誤検知していた問題を解消。
- **upower.serviceのクラッシュループ**や**パッケージ更新に伴うバースト**
  が誤って異常検知される問題を修正。
- **Link GuardのNFQUEUEが正常なパケットバーストで不要にクラッシュ・
  再構築していた不具合を修正**。

### 1.4.1 - 1.8.0

- **パッケージCVE照合機能を追加**: OSパッケージ(dpkg/pacman/dnf/zypper)と
  開発言語7エコシステム(npm/PyPI/crates.io等)の依存関係を、ローカルに
  保持した既知CVEマップと突き合わせ。ネットワーク接続は一切行わない
  (1.5.0)。
- **ログ監査にテンプレート異常検知 + シークレット自動マスキングを追加**:
  未知パターン・頻度異常(Z-score)の検知と、ログに含まれるAPIキー等を
  クリップボード・MCP経由の生JSON双方でマスキング(1.6.0)。
- **Air-Gap発動3ガード(eBPFランタイムガード/Port Anomaly/カナリア)の
  インシデント履歴をMCP・CLIに公開**: 発動理由をdaemonの外から確認可能に
  (1.7.0)。
- **File Scan Guard新設・Dockerリスク検知・コンテナ隔離ポスチャ監査を追加**、
  機密情報・APIキー漏洩スキャナがフォルダ全体を再帰的にスキャン可能に
  (1.4.1、1.4.3)。
- **マルウェア検知通知にClamAVの裏付け検査を追加**し、誤検知時は緊急度を
  下げてアラート疲れを軽減(1.8.0)。
- **重要な不具合修正**: Link GuardのNFQUEUEキュー詰まりによるインストール
  直後のネット接続断(1.5.1)、USB Zero-Trust有効時にキーボード以外の新規
  USBデバイスが恒久的に使用不能(1.5.3)、CVEマップが公開当日は常に
  「未取得」扱い(1.5.2)、RHEL/openSUSE向けCVEマップがファイルサイズ
  超過で一度も配信できていなかった(1.5.2、1.5.3)、`clamd`が未使用でも
  常時1GB超のメモリを消費(1.5.4)。

### 1.1.1 - 1.3.2

- **Server Edition の設定・ドキュメント全面整合**: 運用マニュアルに記載された
  設定項目をすべて実際に機能するように修正 — Docker バイパス防止のON/OFF、
  SSH保守経路温存のON/OFF、`isolate`/`freeze`/`alert_only` の封じ込めモード、
  eBPFソケットパスの変更に加え、重大度ごとに動作を細かく指定できる新しい
  任意設定ファイル `guard.yaml`（同一インシデントの再発でホスト隔離へ自動
  エスカレーション、確認応答が無ければ自動復帰するセーフティタイマー付き）
  を追加。
- **イベント駆動のファイル改ざん監視 (FIM)**: 監視対象ファイルへの書き込み
  完了の瞬間に改ざんを検知するように（従来は周期スキャン待ちで最大数分の
  遅延）。Fedora/RHEL/openSUSE でも Debian/Ubuntu と同様、パッケージ更新後の
  FIM ベースライン自動更新に対応。
- **サーバー版の Egress / C2 遮断**: ローカルの悪性IPブロックリストと任意の
  DNSシンクホール（社内DNS環境を壊さないよう既定OFF）を追加。ランサムウェア・
  カナリアと恒常的カーネル堅牢化もサーバー版で稼働するように。
- **Tetragon センサー対応**: eBPF ランタイムガードが Falco に加えて Cilium の
  Tetragon（ネイティブ gRPC、ローカル UNIX ソケット限定・新規リッスンポート
  無し）にも対応（クライアント・サーバー両版）。
- **自律封じ込めの安全性強化**: プロセスを凍結・強制終了しうる全経路に、
  基盤デーモン（init・コンテナランタイム・パッケージマネージャー・
  ブラウザ等）を除外する共通の保護リストを適用。プロセス単位のネットワーク
  隔離も、何にも一致しない不完全なルールを使っていた不具合を修正し、実際の
  UID またはコンテナを正しく指定する方式に改めた。
- **クライアント版: Air-Gap 復旧の安全性向上と適応型検知感度**:
  「解除」操作は、該当プロセスが判明している場合はネットワーク遮断を解く前に
  そのプロセスを終了するように変更（従来は遮断解除のみ）。eBPF の検知感度も、
  未信頼ネットワークでは自動的に強化し、信頼ネットワークに戻ると緩和するように。
- **診断・通知の整理**: セキュリティ診断表示の「CVE-2026-53362」表記を
  「Frag Gap」のみに簡潔化。起動直後、既知のネットワークに接続しただけでは
  「保護プロファイル切替」通知が出ないように調整（実際のローミング時は
  従来どおり通知）。

### 1.1.0

- **RoamSwitch Server Edition（サーバー・クラウド版）の正式導入**:
  - Falco eBPF ランタイム脅威検知と自動隔離（UNIXドメインソケット `/var/run/roamswitch/events.sock` 連携）。
  - 重要パス改ざん検知（FIM: Critical Path File Integrity Monitoring, SHA-256）と自動リストア。
  - サーバー専用25項目セキュリティ・ヘルスチェック（カーネルパラメータ、Docker特権コンテナ/ソケット露出、eBPF LSM、SYN cookies、空パスワードアカウント等）。
  - Webhook（Slack / Discord / Teams / Generic）および PagerDuty によるリアルタイム障害・脅威通知。
- **MCP サーバー（`roamswitch-mcp`）の Server Edition 対応**:
  - `get_security_report` ツールに `isServer: true` 引数を追加。Claude や AI エージェントから 25 項目のサーバーセキュリティ診断結果を直接取得可能に。
- **SDK（`roamswitchkit` / Python `roamswitch`）のサーバー診断メソッド追加**:
  - Rust SDK: `client.server_security_report().await?` を追加。
  - Python SDK: `client.server_security_report()` および `client.security_report(is_server=True)` を追加。
- **CLI サーバー管理機能の拡充**:
  - `roamswitch status --server`、`roamswitch server [config|setup|test-notify|restart]`、`roamswitch fim [verify|update]`、`roamswitch emergency-restore` コマンドを追加。

### 1.0.31 - 1.0.66

- **アップデート体験の刷新**: 進行状況スピナー付きの手動「アップデートを確認」
  ボタン、更新ステータスの1カード統合、新バージョンがある時だけ表示する
  アップグレードボタン、パッケージ更新後のアプリ自動再起動。
- **マルチディストリビューション対応**: Fedora/RHEL（`dnf`）、openSUSE
  （`zypper`）、Arch Linux（`pacman`/AUR）のアップグレード手順を
  Debian/Ubuntu（`apt`）と同様にネイティブ表示。全10言語に対応。
- **デーモンIPCソケット認証保護とVault堅牢化**: `SO_PEERCRED` による実ユーザー/root限定アクセス制御、シンボリックリンク解決・プレフィックス検証による任意ファイル削除・特権昇格防止、WireGuard引数サニタイズ。
- **セキュリティ診断の判定基準適正化とSBC最適化**: 無人自動更新の実効性検証、Raspberry Pi等のSBCにおける誤判定解消、SSH分割設定監査。
- **UIスレッド完全非同期化**: GNOME / KDE / XFCE / Wayland各環境への適合、
  無線復旧とAir-Gap耐性の強化。
- **ARPスプーフィング検知・復旧の抜本的改修**: 空間的・時間的差分チェックの
  組み合わせ、未確認ゲートウェイMACの誤固定防止、個別強化ボタンの追加。

### 1.0.1 - 1.0.30

- **主要防衛機能の追加**: VPNトンネル(WireGuard/Tailscale)とキルスイッチ、
  未知ポート自動遮断、受動リンク保護、日次脅威フィードアップデータ、
  BadUSBキーボード・USBストレージ承認ダイアログ、共有サービス(SSH/Samba等)
  の自動停止・復元。
- **検知しきい値の適正化**: ランサムウェア暗号化検知しきい値(5秒20ファイル/
  エントロピー7.92以上)、予防的ゲートウェイARP固定。
- **UIとデスクトップ環境の整備**: UIスレッド完全非同期化、タスクバー・
  システムトレイアイコンの動的切り替え、Tailscale/WireGuardの安定性強化。
- **初期セットアップと公式リポジトリ**: 初回セットアップウィザード、
  apt / dnf / zypper公式リポジトリ（GPG署名）の開設。

### 1.0.0

- Linux版初回リリース。macOS版のゼロトラスト思想をLinux（systemd + nftables）へ移植。接続先Wi-Fi/有線の自律識別、ランサムウェア挙動検知・緊急Air-Gap隔離、20項目セキュリティ診断、MCPサーバー搭載。


---

## RoamSwitch for Mac

## 1.9.31

- **修正: ログ監査の頻度急増通知が、直後に詳細画面を開いても確認できない
  不具合を修正**: 「Macセキュリティログ監査」の頻度急増検知（急増した
  ログの内容を通知)は、検知直後にその原因となったログ行を処理済みとして
  消費する設計のため、通知が出た直後に詳細画面や監査履歴を開いても
  「異常なし」と表示され、何が検知されたのか後から確認する手段が
  ありませんでした。新規パターン検知と同様に、頻度急増の検知内容も
  通知履歴（`NotificationHistory`）に記録するようにし、通知後でも
  内容を振り返れるようにしました。Linux版1.9.40の同機能修正と
  同じ設計変更です。

## 1.9.30

- **追加: タイポスクワッティング検知（npm/pnpm package.json、Pro）**:
  package.jsonのdependencies/devDependencies/optionalDependenciesを、
  有名なnpmパッケージ名リストと編集距離（レーベンシュタイン距離1〜2）
  で突き合わせ、`expres`→`express`・`loadash`→`lodash`のようなタイポ
  スクワッティング（似た名前で偽装した悪意あるパッケージ）の可能性を
  検出します。アプリ自体は判定にネットワーク接続を一切行いません。
  判定に使う人気パッケージ名リストは、既存のCVEマップ群と同じ「1日1回・
  受信専用・Ed25519署名検証済み」のPackageCveMapUpdaterパイプライン
  経由で配信され、アプリのリリースを待たずにリストを更新できます。
  参考情報であり断定ではなく、正規の類似パッケージ（`preact`等）は
  許可リストで一部除外しています。「📦 パッケージCVE照合」→
  「タイポスクワッティング検知 (Pro)」タブから利用でき、MCPツール
  `run_typosquat_scan`も追加しました（Pro限定）。Linux版1.9.39の
  同機能と同じ更新パイプライン・判定ロジックです。

## 1.9.29

- **追加: npm/pnpm installのサンドボックス実行 (roamswitch-npm, Pro)**:
  preinstall/install/postinstall/prepareスクリプトの実行を、ネットワーク
  接続不可のサンドボックス(`sandbox-exec`, Seatbelt)内に封じ込める
  コマンドラインラッパー`roamswitch-npm`を追加。これまでの3機能が
  「検知して警告する」機能だったのに対し、本機能は実際にインストールを
  代行します。Linux版はbwrapによるファイルシステム制限を採用しますが、
  macOSには同等技術がないため、実機で動作検証済みのネットワーク遮断
  (`(allow default)` + `(deny network-outbound)`)を採用しています。
  「📦 パッケージCVE照合」→「サンドボックス実行 (npm/pnpm) (Pro)」から
  ラッパーをインストールし、任意でシェルエイリアス(`npm`/`pnpm`)を
  追加できます。npm/pnpm対応、yarnは非対応。Mac版にはCLIバイナリが
  無いため、`~/Library/Application Support/RoamSwitch/bin/`に配置する
  軽量なコマンドラインツールとして実装しました。

## 1.9.28

- **追加: npm installの供給網リスクに対応する3機能 (Pro)**: 依存関係
  ロックファイル(package-lock.json/yarn.lock/pnpm-lock.yaml/
  npm-shrinkwrap.json)の外部改ざんをSHA-256ベースラインで常時監視する
  「依存関係ロックファイルの改ざん監視」、node_modules配下の
  package.jsonが宣言するpreinstall/install/postinstall/prepareスクリプト
  を静的に列挙する「インストールスクリプトの一覧」(脅威判定ではなく
  一覧表示、何も実行しない)、npmレジストリと通信してインストール済み
  パッケージの署名/provenanceを検証する「npm署名/provenance検証」
  (オプトイン、実行毎の確認あり)の3つ。npm署名検証はRoamSwitch全体で
  唯一npmjs.comと通信する機能。いずれも「📦 パッケージCVE照合」ウィンドウ
  から利用できる。

## 1.9.27

- **追加: 暗号資産ウォレットのシードフレーズ・秘密鍵の漏洩検知
  (BIP39/WIF/BIP32)**: 機密情報・APIキー漏洩監査(Secret Leak
  Auditor)がAPIキー・SSH秘密鍵のみに対応しており、クリップボード
  監視でシードフレーズ・秘密鍵を窃取するマルウェア(Microsoftが
  2026年6月に公表した「Crypto Clipper」等)に対する検知が一切
  なかった不備を修正した。BIP39ニーモニック(12/15/18/21/24語)は
  単語列の一致ではなく実際のチェックサム(SHA-256ベース)で検証する
  ため、通常の英文にBIP39収録語が混じっても誤検知しない。Bitcoin
  WIF形式の秘密鍵・BIP32拡張秘密鍵(xprv/yprv/zprv/tprv)も
  Base58Checkチェックサム検証付きで検知する。検知した値はAPIキー
  と異なり一部マスクではなく完全に非表示にする(総当たり探索の
  手がかりを与えないため)。Linux版と同じアルゴリズム・検証済み
  テストベクトルを使用。

## 1.9.16 - 1.9.26

- **追加: 重要システムファイルの改ざんを監視するCritical Path FIM**
  (Pro、1.9.16): sudoers・SSH設定・hosts等、OSアップデートやHomebrewでは
  通常触られない低頻度変更パスに絞ったSHA-256ベースライン監視。
- **重要: セキュリティ関連の成否偽装・未検証項目を4件修正**(1.9.19):
  未知ポート遮断・マルウェア隔離の成功通知がXPC呼び出しの完了を待たずに
  出ていた問題、「macOSアクセサリ接続保護」項目が実際の設定を一切確認
  していなかった問題等を修正。
- **ログ監査の誤検知を多数解消**(1.9.16〜1.9.22): loginwindow起因ログの
  個別列挙ではなくクラス単位の構造的判定への変更、数字と英字が隣接する
  箇所でマスキングが効かない根本バグの修正(Linux版と対)、ロック画面
  レンダリング・スリープ/ウェイク・`0x`接頭辞ポインタアドレス等の
  誤検知を個別に解消。
- **修正: Air-Gap自動発動時のWi-Fi無線遮断が、アプリのクラッシュやMac
  再起動後に自動復帰しない不具合**(1.9.23): 独立したAirGapFailsafe監視
  にWi-Fi復帰も統合し、最大10分での自動復帰を実機確認。
- **改善: ログ監査の異常通知を平易化し、テンプレート異常を一覧・検索
  可能に**(1.9.23〜1.9.24): KPIカードのクリック不具合、新規テンプレート
  異常が通知履歴に残らない不具合もあわせて修正。
- **改善: EICARテスト署名検出時の不要な通知を抑制**(1.9.25): 通知は
  出さず通知履歴への記録のみに変更。
- **修正: リンク保護のホモグラフ判定・ランサムウェア/カナリアのMITRE
  ATT&CK ID判定が、翻訳後の文字列比較のため日英仏以外で正しく動作
  していなかった不具合**(1.9.26): 判定を言語非依存化。あわせて
  MCP・SDK・ヘルプ&ガイド・内蔵ナレッジベースを現行機能に合わせて
  全面更新(全10言語対応)し、一部地域ロケールが日本語にフォールバック
  していた不具合も修正。

## 1.9.13 - 1.9.15

- **自動ログ監査(1.9.10で追加)まわりの誤検知・二重通知を解消**: 有効化
  直後のキックオフスキャンは学習のみ行い通知しないよう修正、
  CFPasteboardRefの一時的な接続断ノイズとAppKit内部のフォーカス移動
  KVO通知(`firstResponder changed`)を除外リストに追加、重複する
  スキャンウィンドウによる同一事象の再検知・再通知を解消。
- **ログイン/ログアウト時のPersistentAppsSupport/BTMManagerイベントの
  誤検知を解消**。
- **通知本文を改善**: 内訳(新規パターンN件・頻度急増K件)+読みやすい
  実ログ行の例示を表示するように。

## 1.9.6 - 1.9.12

- **クリップボード上のClickFix型不正コマンド検知を追加**: Script Editor
  経由でTerminalの履歴監視を回避する手口にも対応。
- **通知履歴(過去1週間保持)を追加**。
- **自動ログ監査(Pro)を追加**: 1時間ごとにバックグラウンドで頻度異常の
  ベースラインを学習・通知(既定オン)。
- **ログ監査の頻度急増検知を、テンプレート自身の過去の頻度と比較する
  方式に再設計**: 正規の定期ジョブの誤検知を削減。
- **自動ログ監査の通知文言2件の翻訳漏れを修正**。

## 1.9.0 - 1.9.5

- **実証型脆弱性診断とパッケージCVE照合を追加**(Linux版から移植、既定オフ)。
- **MCPサーバーの機能欠落を解消**: 機密情報漏洩スキャン・ログ監査・隔離
  ファイル一覧・カナリア監視状況等をMCP経由で取得可能に。
- **Wi-Fiなりすまし(Evil Twin)警告**、**BadUSBキーストローク異常検知**、
  **Link GuardのJA3フィンガープリント照合基盤**を追加。
- **ダウンロードガードの静的シグネチャ検知にClamAVの裏付け検査を追加**。
- **ランサムウェア防護のAir-Gap隔離解除直後に誤検知が繰り返し発生する
  不具合を修正**。
- **重要: 特定条件下でMCPサーバーがクラッシュする不具合を修正**:
  ローカライズ文字列キャッシュが並行アクセスに未対応だった問題を解消。

## 1.8.4 - 1.8.9

- **静的シグネチャによるダウンロードファイル検知・ClickFix対策(既定OFF)・
  Dockerリスク検知ガード(既定OFF)を新設**: EICARテスト署名や定番リバース
  シェル一行コマンドの検知、不審なTerminalコマンド実行時の緊急遮断、
  `--privileged`起動等のコンテナ脱出リスク検知。
- **Apple XProtect検知に連動する自動Air-Gap隔離を追加**(Pro、既定ON)。
  エアギャップ・フェイルセーフを完全に独立した監視デーモンとして再設計し、
  ヘルパープロセスがクラッシュしてもタイムアウトが確実に解除されるように。
  *(Dev.to の技術ディスカッションにおける [Super Funicular 氏](https://dev.to/superfunicular)（[該当記事](https://dev.to/superfunicular/turn-an-old-android-phone-into-a-screen-off-security-camera-no-cloud-lan-only-5cll)）の洞察と提起に深く感謝いたします。)*
- **機密情報・APIキー漏洩監査がフォルダ全体をスキャン可能に**。
- **リンク保護「警告」モードをfail-closedに変更**し保留時間短縮(25秒→
  8秒)、特権ヘルパーの署名検証(動的audit_token検証)・外部バイナリ実行前
  の検証を強化。
- **ローカルポート監査時にエフェメラルなCookie分離を追加**し、認証Cookie
  の漏洩・セッション汚染を防止。
- 上記期間中、ダイアログ表示の不具合・多言語翻訳漏れ92件・Helperバージョン
  同期不具合など複数の軽微な修正あり。

## 1.8.0 - 1.8.3

- **緊急確認ダイアログの最前面オーバーレイ化**: 脅威検知時の4種の確認ダイアログ（BadUSB、ランサムウェア、ARPスプーフィング、リンク保護保留）が他アプリや全画面の背後に隠れず最前面に表示されるよう改善。
- **セキュリティ診断の項目別修復**: 要対策項目へのワンクリック「有効化」「設定を開く」ボタンの追加と即時再診断対応。
- **マルウェア検疫の適正化**: 無害なEICARテスト文字列の誤隔離防止（通知のみ表示、Linux版と挙動統一）。
- **リンク保護のコンテンツフィルタ強化**: ネットワーク拡張機能によるDoH/DoT・SNI検査対応、前面パネルによる警告・安全側ブロック判定。
- **VPNデュアルバックエンド対応**: WireGuardに加えてTailscale Exit Node連携に対応。

## 1.6.0 - 1.7.6

- **VPNトンネル（キルスイッチ付き）の追加**: 未信頼ネットワーク接続時の自動暗号化トンネル（WireGuard）と通信遮断キルスイッチ機能の導入。
- **予防的ゲートウェイARP/NDP固定**: カフェ等の未登録ネットワーク接続時にルーターMACを固定し、MITM攻撃を未然に防止。
- **不正USB / BadUSB物理キーボード承認ガード**、**USBストレージ承認プロンプト**を追加。
- **受動リンク保護**: 詐欺サイト一覧とブランド偽装判定に基づくフィッシング接続の自動遮断。
- **カナリアおとりファイルの永続化**とベースラインハッシュのディスク保存、**隔離Vaultの安全保護強化**(chmod 000)。

## 1.4.0 - 1.5.9

- **ローカルAI / LLM推論サーバー保護**: Ollama、LM Studio、Gradio、vLLM等の0.0.0.0露出検知とプロアクティブ遮断。
- **クリップボード機密保護**: APIキーや秘密鍵のクリップボード保持をローカル正規表現スキャンで検知・警告。
- **オープンソースMCPサーバー公開**: 読み取り専用MCPサーバーおよび検知ロジックをOSSとして公開。
- **Web・メール3重保護ガード**、**特権ヘルパーのセキュリティ堅牢化**、
  **クラッシュ自動復旧（Watchdog）**を追加。

## 1.0.0 - 1.3.0

- **初期公開および基本機能リリース**: ゲートウェイMACアドレスによる信頼ネットワーク自律識別、ファイアウォール/共有サービス自動切替、未知ポート自動遮断、ARPスプーフィング対応、読み取り専用MCPサーバーの初搭載。
