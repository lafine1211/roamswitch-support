# 変更履歴

[English](CHANGELOG.md) | **日本語**

RoamSwitch のユーザー向け変更点。Mac 版（1.x）と
Linux 版（別系列・1.x）でバージョン番号は独立しています。
古い版は、範囲でまとめています。

---

## RoamSwitch Sensor

ユーザー自身の汎用機器(PC・Raspberry Pi等、x86_64/arm64)にインストール
するソフトウェア(別プロダクト)。RoamSwitch 導入済み端末（Mac / Linux
Client / Server Edition）とのペアリングコード方式(固定IP+短命コード)に
よる能動的な脆弱性監査、LAN 上の新規機器出現・なりすまし検知を行います。
現在も開発中です。現状の実装状況・動作確認方法は
<https://lafine.net/roamswitch-sensor-manual.html> を参照してください。

### 0.3.11

- **改善: 手動ペアリング画面のフィンガープリント欄の説明を分かりやすくしました**。表示コマンドに
  既に`--fingerprint`が含まれるようになったため（0.3.10）、この欄はGUI/Mac版の入力欄用であることを
  明記しました。

### 0.3.10

- **修正: 案内されるペアリングのコマンドが、そのままでは失敗しました**。TUIの `sudo roamswitch sensor pair
  --addr … --code …` には指紋が含まれておらず、Linux 1.9.90以降のクライアントは指紋なしのペアリングを
  拒否するため、案内どおりに実行すると必ず失敗していました。TLS有効時は `--fingerprint` を含めます。
  `sensor-cli issue-code` も、端末側で実行するコマンド（Sensor自身のIPと指紋つき）を表示します。

### 0.3.9

- **追加: 制御APIがTLS 1.3と証明書の指紋固定に対応しました**。これまでは、ペアリングコードや診断結果が
  LAN上で平文でした。Sensorが自己署名の証明書を持ち、RoamSwitch側がその指紋を固定します。
  ペアリング要求には指紋が署名で束ねられ、中間者は成立しません。`control.tls` は
  `off` / `optional` / `required`（既定は `optional`）です。`optional` では、対応前の
  クライアントは従来どおり平文で接続でき、その間はTUIとCLIに警告が出ます。すべてのクライアントが
  対応したら `required` に切り替えてください。`sensor-cli tls show|rotate`、`pairing show`。
- **変更: リプレイ対策**。署名つきの要求にタイムスタンプ（±300秒）と使い捨ての値を加えました。
  従来の形式は `control.allow_legacy_signatures` が有効な間だけ受け付け、使うたびに警告します。
  ペアリングの総当たりは、送信元ごと・全体でロックアウトします。要求の行の長さも64KiBに制限しました。
- **追加: TUIが、CLIとデーモンの全機能を扱えるようになりました**。設定の表示と編集（全25キー）、
  TLSの状態と証明書の更新、手動ペアリング、テスト通知、受動キャプチャ・IoT挙動・ネットワーク脅威・
  IoT勧告のイベント、collectorの管理、絞り込みつきの書き出しなど。10言語に対応し、レポートと
  書き出しも10言語で出力します。
- **追加**: 待ち受けアドレス（`control.bind_addr`）、CVEマップとNSEデータベースの取得を止める設定
  （`updates.fetch_cve_map`）、RoamSwitchが入っているのにペアリングしていない端末と、不明な端末の区別
  （collectorへは任意で、端末ごとの概要を送れます）。
- **互換性**: Mac版のRoamSwitchは、このSensorのTLS・新しい署名にまだ対応していません。Mac版とは、
  `control.tls=optional` と `allow_legacy_signatures=true`（どちらも既定）のままで、従来どおり
  ペアリングできます。Linux版は1.9.90以降でTLSに対応します。

### 0.2.1 - 0.3.8

- **ネットワーク構成の把握を強化**（0.2.1〜0.2.7）: ネットワーク構成の常時把握と、
  拡張検知（ボットネット・DDoSへの参加、DNSトンネリング）を追加。`nmap -sn`
  相当の能動ARPスイープで検出漏れを解消し（自機が一覧から漏れる不具合も修正）、
  ネットワーク構成とARPイベントを、現場で判断できる情報量に作り直しました
  （更新は1時間ごと）。機器メモ機能の追加と、TUIの表示の不具合も修正しています。
- **大規模組織のセキュリティ担当者向けの監査運用機能を追加**（0.3.0）: 定期監査、
  前回との差分、通知（Webhook・syslog）、改ざん検知つきの操作ログ、エクスポート、
  保持期間、複数のSensorを集約する中央コレクター。すべてオプトインで、初期状態では
  何も外部へ送信しません。
- **CVEマップとNSEスクリプトDBの自動更新が、systemdの書き込み制限で静かに失敗して
  いた不具合を修正**（0.3.1〜0.3.2）。
- **TUIに、差分タブ・操作ログタブ・エクスポートを追加**（0.3.4）。
- **能動監査の対象を拡充**（0.3.5）: Elasticsearch、CouchDB、VNC、RDP、SMBを追加。
- **ゆっくりしたARPスイープの検知**(0.3.8): 24時間のうちに、異なる宛先が64件に達すると通知する、長い窓を加えました(1アドレスあたり2.5秒のスイープは、64件目で通知されました)。生きていると分かっている機器への問い合わせは、この窓に数えず、追跡するMACアドレス、MACごとの宛先、生きている機器の表には、上限があるので、偽の送信元の洪水で、メモリが際限なく増えることはありません。
- **リポジトリの大きさ**(0.3.7): aptとrpmのリポジトリは、過去のすべての版を持っていたため、最新の1版にしました(1.10.5以降は、最新の3版を残します)。

### 0.2.0

- **追加: IoT機器のセキュリティリスクに特化したARPイベント機能**。従来は
  MAC/IPアドレスの生の差分しか分からず現場で判断できなかったため、
  4段階で拡充しました。
  - ① IEEE OUIレジストリから検証済みのベンダー一覧を照合し、
    「カメラ」「スマートプラグ」等のカテゴリに分類。機器ごとの
    永続インベントリを追加（`sensor-cli device-inventory`）
  - ① 単一MACが短時間に多数の異なるIPへARP要求を送る挙動（LAN内偵察の
    疑い）を検知
  - ② mDNS/SSDP/DHCPの自己申告情報を読み取り、OUI単体より高精度な
    機器特定（既存の受動パケットキャプチャ基盤を拡張）
  - ③ 分類済みIoT機器からの不正通信先アクセス・管理用ポートへの接続を
    文脈付きで提示（`sensor-cli iot-events`、要パケットキャプチャ設定）
  - ④ 実在する公開CVE（Hikvision製カメラの認証バイパス・
    リモートコード実行等）に限定した検証済みの参考情報
    （`sensor-cli iot-advisories`。ネットワーク経由の定期更新は
    今回のスコープ外です）
  - いずれも検知・参考情報の提示のみで、Sensorが遮断を行うことは
    ありません。`sensor-tui`のARPイベントタブでも分類情報を表示します。

### 0.1.0 - 0.1.7

- **deb/rpmパッケージで配布**（0.1.0）。systemdサービスとして常駐します。受動的な
  LAN可視化の拡張（オプトイン）も追加しました。
- **ペアリングを、mDNS自動探索からペアリングコード方式に刷新**（0.1.3）。mDNSは
  同一LANセグメントの中でしか機能せず、常時ブロードキャストし、標準では厳格な
  認証もないためです。固定IPのSensorが発行する使い捨てコード（8文字・10分で失効）
  と、Ed25519署名で認証します。クライアントからの監査リクエストの受付と、nmap
  NSE補完診断の常時有効化も同じ版で入りました。
- **ペアリング済み端末の監査状態バッジと、最新レポートの閲覧を追加**（0.1.4）。
  手動ペアリングを廃止して、コード方式に一本化し、IPアドレスを常に表示するように
  しました。
- **不具合の修正**（0.1.5〜0.1.7）: 同じ端末に対して監査が並列に走る問題、
  `Too many open files` でデーモンがクラッシュし、進行中の監査が失われる問題
  （ファイルディスクリプタの上限を65536に）、監査結果の指摘事項から詳細説明が
  欠落していた問題。

## RoamSwitch for Linux

Linux 版（systemd + nftables）。apt / dnf / zypper で配布（GPG 署名）。
詳しくは <https://lafine.net/linux>。

### 1.10.8

- **修正: Serverの調査エージェントの報告と、連携失敗の通知に、ホスト名とIPアドレスがありませんでした**。ほかの警告には入っているため、複数台のServerを運用していると、どの機械の報告か分かりませんでした。通知(10言語)と、報告ファイルに追記される「エージェント調査結果」の節に、ホスト名とIPアドレスを加えました。

### 1.10.7

- **変更: ライセンスの署名鍵を作り直し、アプリは、古い公開鍵と新しい公開鍵の両方を受け付けます**。すでに認証したライセンスは、そのまま検証を通ります。新しい認証は、新しい鍵で署名されるので、古い版では、新しいライセンスを認証できません。先にアップグレードしてください。
- **修正: GUIからのアップグレードが、止まる、変わっていないのに成功と表示する、アプリを再起動しない、という問題を直しました**。(1) `apt-get update` が、すべてのリポジトリを待つため、不通のミラーが1つあるだけで、画面が「アップグレード中…」のまま止まっていました。RoamSwitchのリポジトリ(`roamswitch.list`)だけを取得し、60秒で打ち切ります。(2) リポジトリが登録されていないと、`apt-get install --only-upgrade` は何もせずに終了コード0を返し、何も入っていないのに「完了」と出ていました。前後で、インストール済みの版を比べます。(3) 再起動が、パッケージがファイルを置き換えたあとに `/proc/self/exe` が示す `…/roamswitch-app (deleted)` を指し、何も起動していませんでした。実際のパスで起動します。GUIの外(端末、unattended-upgrades)でのアップグレードも見つけ、自動で再起動します(画面が非表示なら直ちに、表示中なら通知の40秒後)。アップグレードのタブは、aptだけでなく、dnf、zypper、pacmanでも、アップグレードの有無を確認できます。Ubuntu 24.04のVMで、実際のpolkitの認証画面を通して、再現し、確認しました。
- **追加: メインウィンドウに「復旧とアンインストール」タブ**(10言語)。2つの操作は、ヘルプのページの中ほどにありました。トレイからのアンインストールは、パッケージマネージャのロックを、最長300秒待ちます(起動の直後は、unattended-upgradesが握っているため、最後の手順が失敗していました)。実際のpolkitの認証画面を通して確認しました。
- **追加: 部分的に暗号化されたファイルの検知**。これまでは、全体が暗号化されたファイルだけを数えていました。32バイトのうち16バイトを暗号化するファイル(LockBit系が使う書き方)を、検知します(模擬のファイルの94%。通常のファイル19,997件のうち、誤検知は2件、0.01%)。ほかの3つの形(4KiBの区間が交互、先頭だけ暗号化、一様なBase64)は、数えてログに出すだけの「shadowモード」です(`ransomware shadow (nothing frozen)`)。実際のデータで安全が確かめられるまで、プロセスを止めません(通常の媒体ファイルの1%ほどを誤検知するか、圧縮データをBase64にしたものと区別できないため)。測定: `docs/RANSOMWARE_SHAPES_2026-09-26.ja.md`。
- **修正: 解像度の低い画面で、トレイのメニューの下のサブメニュー(復旧、言語)が、空に見えました**。GNOMEは、サブメニューをメニューの中に展開するため、メニューが画面より高いと、見える範囲の外に出ます(実機で確認)。画面の高さが820ピクセル以下のときは、メニューを短くします(4つの保護レベルを、1つのサブメニューにまとめます)。`ROAMSWITCH_TRAY_COMPACT=1` または `0` で切り替えられます。**追加: ヘルプ&ガイドのタブに、「復旧とアンインストール」のボタン**(小さな画面では、トレイのメニューが使えないことがあるため)。ヘルプ&ガイドに、隔離の状態と解除、復旧とアンインストール、短いトレイのメニュー、ガードの稼働の監視、DNSトンネリングの除外ドメインを加えました(10言語)。
- **追加: 復旧とアンインストール**。`roamswitch emergency-restore` と、トレイの「復旧とアンインストール」メニューで、ネットワークを元に戻します(隔離、ファイアウォールのテーブル、ゲートウェイの固定、DNS、接続追跡)。`roamswitch uninstall`(deb、rpm、Archの削除フックからも呼ばれます)は、先にそれを行い、そのあとおとりファイルと、セットアップが加えたファイルを取り除きます。ゲートウェイが信頼済みのものと違うとき、または隔離の原因が解消したと確認できないときは、承認がないと実行しません(`--force`、アプリのチェックボックス、端末がなければ終了コード2)。実際のdpkg、rpm、pacmanの削除で確認しました。
- **修正(セキュリティ): 隔離の解除が、そのときいたゲートウェイを、信頼済みとして採用していました。ゲートウェイの固定が、偽のMACを恒久的に固定していました**。確認できた解除だけが、いまのゲートウェイを採用します。偽装が解消していないとき、および信頼済みのMACと違うときは、固定しません。
- **修正(セキュリティ): 有線リンクで、ゲートウェイのMACが変わっただけで、別のネットワークへ移ったと見なしていました**。そのため、偽のMACが入っても、基準が作り直され、検知されないまま固定されていました。有線では、ゲートウェイのアドレス、インターフェース、リンクの状態のいずれかが変わったときだけ、移動と見なします。
- **修正: 1台の機器が、1つのMACで2つのIPに答えるだけ(仮想マシンを動かすPC、別名アドレスを持つNASなど)で、lockdownのマシンが完全に遮断され、解除もされませんでした**。実際のLANで見つけました。自動の隔離は、ゲートウェイが関わる検知だけに絞りました。それ以外は、通知(10言語)だけで、通信は遮断しません。解除の判定も、ゲートウェイが関わる重複だけを数えます。LAN上の別のマシンで確かめました。偽のゲートウェイARPで、約1秒でAir-Gapが発動し、攻撃をやめて約11分後に、無害な重複が残ったまま、自動で解除されました。
- **修正(セキュリティ): 隔離の記録(原因、確度、開始時刻)が、サービスの停止のたびに消えていました**。systemdが `/run/roamswitch` を削除していたためです。アップグレード、再起動、クラッシュのあとで、高確度の隔離が、低確度の隔離に変わり、10分で自動解除されました。ユニットに `RuntimeDirectoryPreserve=yes` を加えました。再起動をまたいで確認しました。
- **修正: ランサムウェアのバースト検知が、許可リストの名前のプロセスをすべて除外していました。短時間で終わるツールは、誤って通知されていました**。プロセスが、名前を `python3` や `git` に変えるだけで、40ファイルを、検知されずに暗号化できました(実機で測定)。許可リストは、実行ファイル、またはインタープリタが動かすスクリプトが、名前と一致することも確かめます。また、`cp -r`、`rsync -a`、`tar -x`、Pythonのスクリプトが、20個以上の圧縮に見えるファイルを書くと、判定の時点でたいてい終了していて、『不明なプロセス』として通知されていました。書き込みを見た時点で識別します(署名の検査は続けます)。部分暗号化とBase64での書き出しは、まだ検知できません。測定結果は、Linux版ホワイトペーパーにあります。
- **修正: ゲートウェイが関わらない異常に、『ゲートウェイのMACが変わった』という誤った確認画面を出していました**。
- **修正: ネットワークタブの『Air-Gapを解除して復旧する』ボタンが、緊急ウィンドウと違い、確認を出さずに解除していました**。同じ確認を出し、原因が解消したと確認できないときは、承認のチェックが要ります。
- **修正: VPNタブでExit Nodeを入力すると、『(なし)』のラベルに続けて入力され、ラベルごと保存されていました**。実際のGTKアプリを操作して見つけました。
- **修正: ゲートウェイに届かない間、デーモンが起動しませんでした**(起動時に、ClamAVの署名の更新を、待ち時間の上限なしで同期実行していたため)。**修正: snap版のTailscaleが、サンドボックスの中のデーモンから見えませんでした**。**追加: Tailscaleの出口ノードを設定したあと、カーネルの経路が実際にトンネルを通ることを確認し**、通らなければ自分で解除して通知します。**修正: 共有サービスの停止と復元で、通知が重複していました**。
- **追加: 隔離の間、トレイと解除の画面に、状態(全面か縮退か)、原因、開始からの時間、解除の前に確認することを表示します(10言語)**。
- **追加: リンク保護タブに、DNSトンネリング検知の誤検知を避けたいドメインの欄を追加しました**(10言語。これまでは `config.json` を手で編集する必要がありました)。
- **Server: セットアップウィザードで、保守元を必須にしました**(空欄は受け付けません。`ANYWHERE` で、隔離中も含めて全アドレスに開きます)。**調査エージェントは、rootでは動かしません**(ウィザードは保存せず、デーモンは起動しません)。**検知内容は、長さを切り詰めたJSONの文字列として渡します**。区切り線や偽の指示欄を作れません。**追加: `honeytokens_home`**(おとりを置く場所)。**追加: 証拠バンドルの `manifest.json` を、バンドルの外にも記録します**(`evidence_anchor.log` のハッシュの鎖と、システムログ)。`roamswitch-incident-capture verify` は、manifestが書き換えられたとき、鎖が改ざんされたときに失敗します。
- **テスト: Falcoのイベント行、`guard.yaml`、`server.conf`、隔離の記録、IPCの拒否の文言、DNSの許可リストの欄、保守元の答えに、決定的なファジングを行い**、パニックしないことを確認しました。編集できる設定キーの数(`server.conf` 88個、`guard.yaml` 37個)を、テストで固定しました。
- **セキュリティ: リンクガードのパケット解析を、rootでは行いません**。新しい接続の最初のパケット(IP、TCP、DNS、TLSのClientHello、HTTP)は、通信相手が決めたバイト列です。子プロセス(同じバイナリを `--link-guard-parser` で起動)が、`nobody` に権限を落とし、capabilityを空にし、no-new-privilegesを設定し、2本のパイプの読み書きとメモリの管理だけを許すseccompを入れたうえで解析し、デーモンは、その答えだけを読みます。子が使えない、または遅いときは、パケットを解析せずに通し(接続は止めません)、デーモンが自分で解析することもしません。

### 1.10.6

- **セキュリティ(サーバー版・クライアント版): デーモンを、systemdのサンドボックスの中で動かします**。カーネルモジュール、カーネルログ、cgroup、時計、ホスト名、名前空間、リアルタイムスケジューリング、SUID/SGIDファイルを保護し、デーモンが使わないcapabilityとシステムコールを除き、使うソケットの種類(Unix、IP、netlink、packet)だけを許可し、書き込み可能な実行メモリを禁止します。`systemd-analyze security` は、9.3(UNSAFE)から5.4(MEDIUM)になりました。各設定は、実機のsystemd 255で適用し、診断、ガード、ホストの隔離と解除、ダウンロード保護、FIMが、設定なしと同じ結果になることを確認しました。`NoNewPrivileges`、`PrivateTmp`、`PrivateMounts` は、ユーザーへの通知(`sudo -u`)と、`/tmp` のnoexecのガードを壊すため、入れていません。
- **修正: 毎日の更新が、ClamAVの署名を更新していませんでした**。更新のユニットが `NoNewPrivileges=yes` だったため、`freshclam` が `clamav` ユーザーに切り替えられず(「Failed to switch to clamav user」)に失敗していました。ユニットを、これを許し、必要なcapabilityだけ(`NET_ADMIN`、`SYS_ADMIN`、`NET_RAW`、`KILL` は持たない)を持ち、netlinkとpacketのソケットを開けず、`/var/log/clamav` に書けるようにしました。
- **修正(セキュリティ): `rustls` を0.23.45に更新しました(RUSTSEC-2026-0285、TLS 1.3のハンドシェイクメッセージが、暗号化レベルをまたいで受け付けられる)**。CIは、プッシュのたびに、依存をRustSecの助言データベースと照合します。
- **修正: 診断「補助プログラムの導入」の状態が、日本語以外の言語でも日本語で出ていました**。翻訳済みのレポートに日本語が残ると、テストが失敗します。
- **テスト: リンクガードのパケット解析(TLSのClientHello、SNI、DNS、HTTPのHost、TCPの再構成)が、悪意ある入力(ランダムなバイト列、すべての切り詰め、極端な長さの値)でパニックしないことを確かめます**。

### 1.10.5

- **修正: 1.10.0〜1.10.4 のパッケージに `roamswitch-honeytokens` と `roamswitch-incident-capture` が入っておらず、apt・dnf/zypper・tarball から入れた環境では、認証情報のおとり(ハニートークン)と証拠バンドルが一度も動いていませんでした**(デーモンは名前で起動し、実行ファイルがなければ黙って何もしない作りでした)。クライアントとサーバーの両方のパッケージに同梱し、どちらかが無ければデーモンが起動時にエラーを記録します。CI も、パッケージに含まれていないビルドを失敗させます(`scripts/check_package_contents.sh`)。
- **修正(セキュリティ): rootのデーモンが、`/home/*` の `~/.config/roamswitch/config.json` を、所有者を確かめずに読み、書き込みではシンボリックリンクを辿っていました**。ローカルユーザーが、rootに別のJSONファイルを上書きさせられ、ユーザーの設定で全員のガードを止められました。設定は、通常のファイル(リンクは不可)で、そのホームの所有者(rootかuid 1000以上)が所有し、他者に書き込まれないもの(グループ書き込みは所有者の主グループのときだけ)に限ります。ガードを止める設定やスキャン除外は、rootか、ローカルにログインセッションを持つUIDのものだけを受け付けます(IPCと同じ規則)。
- **追加(サーバー版): `honeytokens_enabled`(既定true)と `harden_userns_enabled`(既定true)**。ハニートークンを無効にすると、配置と監視を止めます(配置済みのおとりは残ります)。`harden_userns_enabled=false` は、非特権ユーザー名前空間の恒久的な禁止を行いません。禁止すると rootless コンテナが動かなくなり、CVE-2026-53362 の根本対策は修正済みカーネルへの更新です。
- **変更(サーバー版): 権限確認を省略する設定(`--dangerously-skip-permissions` など)のまま、rootで動く調査エージェントを拒否します**。セットアップのフォームでは保存できず、デーモンも起動しません。
- **改善(サーバー版): ホスト全体のAir-Gap、プロセス単位の隔離に失敗した場合の昇格、ランサムウェアのカナリア改ざんの各経路で、遮断の前に証拠を採取します**。従来はプロセス単位の隔離のときだけでした。
- **改善(サーバー版): 隔離中も保守用のSSHポートが全アドレスに開く構成(`whitelist_ips` が空)と、Egress遮断が有効なのにフィードが空の場合に、起動時に警告します**。
- **修正(サーバー版): ホスト隔離が、残すはずの管理用SSHを遮断していました**。隔離用の出力チェインの優先度が -300 で、コネクショントラッキングより前だったため、ホスト自身が送る応答では `ct state established,related` が一致せず、SSHのSYN-ACK(と既存セッションのすべてのパケット)が既定のdropに落ちていました。`preserve_ssh_on_isolation=true` でも、隔離中は管理者が締め出されました。チェインの優先度を -100 にし、回帰テストを追加しました。修正したペンテストスイートで、公開済みの1.10.4に対して確認しています。
- **修正(サーバー版): `preserve_ssh_on_isolation=false` でも、ホスト隔離がポート22を開けていました**(ポートの一覧が空だと、従来の管理用ポートに退避する作りでした)。文書どおり、ループバック以外をすべて遮断します。
- **修正(サーバー版): 高確度の隔離で、原因が消えたように見えると、運用者の確認応答(`roamswitch server ack`)が無視されていました**。自動では復旧しないと表示していながら、解除されていました。確認応答した FULL の隔離は、運用者が解除するまで維持します。
- **修正: FIM が、`/root/.ssh` のハニートークンのおとりを新規ファイルとして報告していました**。補助実行ファイルをパッケージに入れると、デーモンがベースラインの後におとりを置くため、FIM が自分の罠を踏んでいました。ハニートークンのマーカーを持つファイルは一覧から外し、マーカーの無い本物の鍵は、これまでどおり報告します。
- **ペンテストスイートを2026-09-25に再実施**(Docker Desktop、arm64、Ubuntu 24.04のコンテナ): サーバー版は、1.10系のソースにこれらの修正を加えたビルドで19項目中19項目が成功し、公開済みの1.10.4では、修正したスイートで2項目(隔離中のSSH、確認応答)が失敗します。クライアント版は11項目中11項目が成功します。実機のVMが要る検証(ユーザー名前空間とYamaのsysctl)は対象外です。スイートは `PENT_DEB` が必須になり、固定のv1.3.2パッケージを誤って試すことはなくなりました。
- **修正(サーバー版): systemd ユニットのサンドボックス(`ProtectSystem=strict`、`ProtectHome=read-only`)が、デーモンとハニートークンの実行ファイルによる、`~/.aws`、`~/.docker`、`/root` と `/opt` のランサムウェアのカナリアの作成を止めていました**。実行ファイルは終了コード0で終わるため、デーモンは「ハニートークンを配置しました」と記録するだけで、おとりは1つもありませんでした。ユニットは、`ProtectHome=no` と `ReadWritePaths=-/root -/opt` にしました(`/home` は `ProtectSystem=strict` で、読み取り専用のままです)。おとりを置けないときは、実行ファイルが終了コード3で終わります。公開済みの1.10.4 を、Ubuntu 24.04 のVM(カーネル 7.0、systemd 255)で動かして見つけ、確認しました。Dockerのスイートには、このサンドボックスがありません。
- **追加: 診断「補助プログラムの導入」**(クライアント27項目、サーバー33項目、10言語)。デーモンが名前で起動する2つの実行ファイルがあるかを確かめます。
- **変更: aptとrpmは、各パッケージの最新の3版を残します**(従来は1版)。欠陥のある版が出たときに、`apt install roamswitch=<版>` や `dnf downgrade` で戻せます。
- **実機のVMでの確認(2026-09-25)**: Frag Gap の緩和が実際に反映され(`user.max_user_namespaces=0`、`kernel.unprivileged_userns_clone=0`、Yama 2、非特権BPF無効)、`unshare -U -r` が拒否されること。ホスト隔離中に、別のマシンからのSSHが、修正版では通り、公開済みの1.10.4 では遮断されること。

### 1.10.4

- **修正: ClamAV の検知が、`clamdscan --fdpass` の失敗をフォールバックが隠していたため、常時は片方の経路でしか動いていなかった不具合**。デーモンの systemd サンドボックス内では `--fdpass` が常に「Not a regular file」で失敗していました。`--stream` を先に使い、失敗したときだけ `--fdpass` を再試行します。両方失敗した場合は「エラー」として扱い、「問題なし」にはしません。
- **修正: USB ディスクのスキャンで、`clamdscan` の終了コード2(実行失敗)まで「マルウェア」と判定して `umount -f` していた誤り**。終了コード1(検知)のときだけ取り外します。
- **追加: ClamAV の検知の自己診断と、fanotify のイベント溢れの診断(診断項目 Client 24→26、Server 30→32、10言語)**。EICAR テストファイルで、起動120秒後と6時間ごとに、検知経路が実際に動くことを確認します。
- **修正: DNS の強制が、3秒ごとに `resolvectl`(`dns`・`default-route`・`flush-caches`)を実行していた不具合**。定常状態でも毎回 DNS キャッシュが破棄され、名前解決が遅くなっていました。状態が変わったときだけ実行します。
- **改善: Docker の保護(`DOCKER-USER`)を、60秒ごとに自己修復します(IPv6 と native nftables を含む)**。デスクトップ版でも、起動時の1回だけではなくなりました。
- **改善: Link Guard**。パケット待ちのスリープを `poll(2)` に置き換え、キュー長を4096にし、TCP のシーケンス番号で ClientHello を再構成します(最大8192バイト)。ブロックモードでは、UDP/443(QUIC)を遮断して TCP にフォールバックさせるオプション(`linkGuard.blockQuic`、既定オフ)を追加しました。
- **改善: fanotify のオンアクセススキャン**。許可イベントは実行(`FAN_OPEN_EXEC_PERM`)のみで、`open` は通知にしました(`fanotify_open_perm` で従来の動作)。スキャン済みの実行ファイルは60秒キャッシュし、デーモン自身の子プロセスは除外します。`/home` が `/` と同じファイルシステムのときは警告します。
- **変更: BadUSB ガード**。未承認のキーボードの掴みを、承認待ちの3分では解除しません(`usb_keyboard_timeout_release` で従来動作)。`/dev/input` の変化で、ホットプラグを即時に検知します。USB ディスクリプタの指紋(初回信頼)で、許可済みの VID:PID を名乗る別デバイスを検出します。
- **変更(セキュリティ): 状態を変える IPC は、root か、ローカルのログインセッションを持つユーザーだけが呼べます**。読み取り系は従来どおりです。拒否は固定のコードで返り、GUI(10言語の通知)と CLI(日英)で表示します。CLI の `airgap enable/disable` が、応答を読まずに成功と表示していた誤りも直しました。
- **改善: ハニートークン(囮ファイル)が、実際に使うツールを壊さないようにしました**。AWS の囮は `[default]` をやめて別のプロファイル名に、Docker の囮の宛先は Docker Hub をやめて実在しないレジストリ名にしました。権限は 0600 です。`ssh`・`scp`・`sftp` など OpenSSH のクライアントと、RoamSwitch 自身のアクセスは、実行ファイルのパスで判定して誤検知しません。アクセス元のプロセスは、通知クラスの fanotify でイベント時点に特定します。旧版が設置した囮は、起動時に新しい内容へ移行します(本物のファイルには触れません)。
- **改善: 外部コマンドを絶対パスで起動します**。`PATH` 探索の失敗による監査ログのノイズを解消しました。`/proc/net/route` は直接解析し、不要なコマンドの起動を減らしました。
- **修正: 詳細設定が、未知のキーを保存時に捨てていた不具合**。GUI に、QUIC 遮断・open の同期スキャン・キーボード自動解放のチェックボックスを追加しました(10言語)。

### 1.10.3

- **改善: 同じ通知が繰り返しポップアップされる問題を抑えました**。同一（タイトル・本文）の通知は
  10分間、ポップアップと IPC キューへ出さず、通知履歴にだけ記録します（履歴は全件残ります）。
  緊急度が低い通知は履歴のみで、ポップアップしません。Docker の危険な構成の通知も、同じイメージ・
  同じ理由の連続通知を抑えます（理由が変われば、すぐに通知します）。
- **修正: DNS トンネリング検知が `clamav.net` の TXT 応答（freshclam の定義確認）を誤検知していた
  不具合**。この TXT は検知の対象から外しました（NULL レコードや高エントロピーの応答は、従来どおり
  検知します）。再通知の間隔は 300→600→1200→1800 秒と、段階的に延びます。通知本文と
  タイムラインに、対象のドメイン（apex）を追加しました。
- **改善: 開発用ビルドの生成物（`target/debug`・`target/release` 配下）への YARA 誤検知で、
  通知と承認要求を出さないようにしました**。ClamAV が同意せず、実行の拒否も伴わない場合は、
  履歴にのみ記録します。ClamAV が同意しない同じディレクトリ・同じファミリの検知は、30分に1回に
  集約します。
- **改善: Arch 系ディストリビューションでは、「自動セキュリティアップデート」の診断を「非該当」として
  扱います（減点なし）**。ローリングリリースの無人の `pacman -Syu` は、部分アップグレードによる
  起動不能を招きうるため、公式に推奨されていません。利用者が自動更新を有効にしている場合は、
  通常どおり評価します。Arch 系以外の挙動は変わりません。
- **修正: 特定の環境で、開発者のホームディレクトリの固定パスを参照する箇所を取り除きました**。
  `HOME` が未設定のとき、MCP サーバーの設定ファイルの探索と、アプリのスキャン対象が、固定のパスに
  フォールバックしていました。ユーザー名を引けない UID は、固定のユーザー名でコマンドを実行せずに
  スキップします。

### 1.10.2

- **修正: Server Edition の未知ポート露出ガードが、同居する RoamSwitch Sensor 自身のペアリング用
  制御ポート（50543）を、起動時のタイミング次第で誤って永続ブロックしてしまう不具合**。このガードが
  「まだ知らない公開ポート」のベースラインを取得するタイミングが、Sensor が制御ポートで待ち受けを
  開始するより一瞬早いと、Sensor自身の制御ポートを新規の不審なリスナーとして遮断し、以後デーモンを
  再起動するまで解除する手段がありませんでした。ファイアウォールが既に常時許可しているポート
  （`ssh_ports`・`allowed_ports`）は、検知タイミングに関わらず自動遮断の対象から除外するようにしました。

### 1.10.1

- **修正: `wsdd`のようにインタプリタ経由でランダムなエフェメラルUDPポートへ再バインドするプログラムが、
  再起動のたびに別ポート扱いされ、通知が無限に繰り返されていた不具合**。ポートを含まないフィンガー
  プリント（実行ファイルパス＋スクリプト引数）を新設し、初回のみデスクトップ通知、以降は同一
  フィンガープリントである限り通知履歴への記録のみに抑制するようにしました。個別インシデントの記録
  （MCPの履歴用）は従来通り毎回行います。

### 1.10.0

- **追加: DNSトンネリング/持ち出し検知**（Client / Server Edition 共通）。DNSトンネリングツール
  （iodine・dnscat2等）が使う「ラベルが長く高エントロピー」または「TXT/NULLタイプ」の不審な形の
  クエリを、このホスト自身が送信するDNSクエリの中から検知します。短時間に一定件数を超えたら通知
  （インシデントタイムラインにも記録）。DNS以外のトラフィックは一切検査対象にせず、検知プロセスが
  停止・遅延していても名前解決を妨げない設計です。Server Edition では新規設定キー
  `dns_tunnel_detect_enabled`（既定オン）で切り替えられます。
- **追加: フォレンジック証跡バンドル**（Client / Server Edition 共通）。ランサムウェア・カナリア発火や
  eBPF重大検知などの封じ込め発動時に、プロセス一覧・直近のファイル変更・SHA-256付きマニフェストを
  自動保存します。
- **追加: デセプション/ハニートークン**（Client / Server Edition 共通）。`.aws/credentials`・
  `.ssh/id_rsa`・`.docker/config.json`等に「本物らしい」おとりファイルを配置し、実際にアクセスされたら
  即座に検知・通知します（関連する複数のおとりを連鎖配置）。
- **追加: 脆弱性スキャンの実施状況（経過日数）を確認できるようになりました**。実証型脆弱性診断
  （`scan-vulns --confirm`）の各項目について、最後に確認できた結果と、それからの経過日数を
  `roamswitch vuln-status`・GUIアプリの「ポート」タブ・MCPツールから確認できます。
- **追加: 健全性診断の各項目に、NIST CSF 2.0 / CIS Controls v8 のマッピングを追加しました**（Client /
  Server Edition 共通）。監査対応や法人利用での説明にお使いいただけます。確信を持って対応付けできる
  項目のみに付与し、不確かな項目は空欄のままにしています。
- **修正: まれにCPU使用率が張り付き続けることがありました**。マルウェア検知（YARA）がClamAVへ
  セカンドオピニオンを依頼する際に起動する`clamdscan`自身のファイルオープンを、オンアクセス監視が
  再度検知してしまい、通知の生成が自己増殖的に繰り返される場合がありました。`clamdscan`自身のプロセス
  は監視対象から除外するよう修正しました。
- **改善: `credential_watch`（既定オフ）のAWS向け許可リストに`ansible`/`python3`/`python`を追加**し、
  IaCツール経由の誤検知を減らしました。
- **追加: roamswitch-mcp（MCPサーバー）に、AIエージェント向けの検知結果トリアージ手順（skill）を
  4本追加しました**。ポート異常・実証型脆弱性診断・パッケージCVE・ランサムウェア/インシデント
  タイムラインの優先度判断を、AIエージェントが一貫した手順で行えるようになります。

### 1.9.39 - 1.9.94(まとめ)

これらの版の内容を、テーマ別にまとめました(特に断りがなければ、クライアント版とサーバー版の両方)。各項目の全文は、このファイルのgitの履歴にあります。

- **隔離(Air-Gap)の作り直し**(1.9.90、1.9.91): 確度の高い検知(おとりの改ざん、ランサムウェアの疑い、重大なeBPFの検知)による隔離は、タイマーでは、もう解除されません。上限(既定1時間)までに、原因の解消を再確認できなければ、時間では開かない「縮退」モードに移り、`roamswitch emergency-restore`、GUI、サーバーの `roamswitch server ack`、または、解消を確認できた再確認だけで、解除されます。誤検知が起きやすい検知は、これまでどおり、短いタイマーで解除されます。すでに開いていた接続は、隔離のあとに残らなくなりました(サーバーは、保守用のSSHと許可したIPだけを残し、クライアントの縮退は、ループバックだけを許可します)。ARPの再確認は、ゲートウェイの近隣エントリがないとき、ARPの問い合わせを直接送るので(`arp_recheck_active_probe`)、ARP起因の隔離が、解除されないまま残ることが、なくなりました。
- **プロセスの実行の記録、転送、サーバー版のTUI**(1.9.90): どのプログラムが、どの親と、どのハッシュで、どのコンテナで動いたかを、その機器の中だけに、連続して記録します(既定は200MB、14日、ハッシュの鎖)。通知だけの相関ルール7つ。イベントの転送(既定はオフ。syslog、CEF、JSON Lines、HMAC署名つきのwebhook)。サーバー版の、全機能を操作できるTUI(`roamswitch server tui`)。あわせて、`--json`/`--export`、ほかのプログラムが認証情報のファイルを開いたときの通知(既定はオフ)。
- **コンテナとネットワーク**(1.9.43、1.9.44、1.9.90): Dockerのファイアウォール迂回の対策は、DockerのネイティブnftablesのバックエンドとIPv6に対応し、1分以内に自己修復します。コンテナの実行の防御(サーバー版)は、コンテナのrootfsに、通知専用の fanotify `FAN_OPEN_EXEC` を付け、自分を削除する攻撃者のバイナリも捕まえます。Egressの防御は、コンテナの通信も対象にし、プロセスごとのCPU飽和の検知、FIMの範囲の拡大(`authorized_keys`、`ld.so.preload`、監視するディレクトリの新しいファイル)も加えました。
- **調査エージェント**(サーバー版、1.9.45〜1.9.56): eBPF、FIM、ロックファイルの検知に、自動の一次トリアージ(誤検知の可能性、理由、次のコマンド、Markdownの報告)を行い、任意で(既定はオフ)、エージェント型のCLI(Claude Code、agy、Codex CLI、OpenCode、独自)へ引き継ぎます(ホスト隔離の直前は、行いません)。rootで動かなかった問題、sudoの包み方、エージェント自身の調査が新しい事案に見える再帰を、直しました。
- **セキュリティの修正**(1.9.84〜1.9.90): ローカルのユーザーが、Falcoのソケットへ、偽の重大なイベントを送り、全面のAir-Gapを起こせました(ソケットは、モード0600と、相手のUIDの確認にしました)。`sshd-x` のようなよく似た名前が、保護するプロセスの一覧をすり抜けました。プロセスの名前を変えるだけで、許可リストを回避できました(`--exe` で、実際の実行ファイルに結びつけられます)。TelegramとLINEのトークンやwebhookのURLが、プロセスの一覧から見えました。ポートスキャンの自動ブロックは、偽の送信元アドレスで、悪用できました(新規の受信接続だけを拒否し、ゲートウェイ、DNSリゾルバー、このホスト自身のアドレスは、ブロックせず、SYNの送信元MACも確認します)。偽のSYNの洪水で、スキャンの記録が、ログから押し出されました(毎秒200件の上限を先に置き、検知が飽和したときは通知します)。Sensorのアドレスを名乗るスキャンは、ブロックを免れました(ペアリングのときに、SensorのMACを記録します)。未知のリスニングポートの検知は、システムのデーモンの名前(`/tmp/sshd`)や、一時ディレクトリのインタープリタのスクリプトで回避できました。開発サーバーの遮断ルールは、再適用のあいだ、一瞬、外れていました。ログの頻度の検知は、大きな急増のあと、働かなくなりました。rootのデーモンが、`~/.local` の下に、root所有のディレクトリを作りました。
- **ポートスキャン、リスニングポート、脆弱性の確認**(1.9.39〜1.9.94): ポートスキャンの検知の強化(洪水でもメモリが増えない、24時間の窓、IPv6は/64ごと)。ワイルドカード以外へのバインド、一時、メモリ上、削除されたバイナリの未知のリスニングポートの検知、インタープリタのUDPのリスナー、本当のプログラム名を示す通知(`wsdd (python3)`)。Elasticsearch、CouchDB、Jenkins、VNC、RDP(NLA)、SMB(SMBv1、署名)の能動的な確認。安全と判断不能の結果を説明するCSVの書き出し、スキャンID、ロックつきのログ。npmパッケージのタイポスクワットの検知(Pro)。カーネルのCVEの確認が、ABI番号だけを上げるディストリビューションのバックポートを、誤って指摘しなくなりました。nmapのNSEのスキャンは常に有効で、CVEマップは毎月更新されます。
- **RoamSwitch Sensor**(1.9.55〜1.9.91): ペアリングを、mDNSから、ペアリングコードにしました。Sensorに能動的な監査を依頼し、結果をあとで取得できます(MCPツール `get_sensor_audit_results`)。証明書を固定したTLS 1.3(`roamswitch sensor pair --fingerprint`、`sensor repin`。新しいペアリングにはSensor 0.3.9以降が必要で、すでにある平文のペアリングは、警告つきで、動き続けます)。同じホストにSensorがあるとき、セットアップのウィザードが、プリセットを提案します。ペアリングに失敗したときは、理由を示します(30秒の待ち、考えられる原因、Sensorのホストで実行するコマンド)。
- **ネットワークの制御の見直し**(1.9.71〜1.9.72、1.9.85〜1.9.87): デーモンの再起動の直後に、ファイアウォールの設定が再適用されない問題、共有サービスの制御で止めたサービスが復元されない問題、手動のセキュリティレベルの変更と、デーモンの巡回が競合する問題。VirtualBoxのNATやNAT環境で、ARPの誤検知が、Air-Gapを起こす問題。Frag Gapの緩和(ユーザー名前空間の禁止)は、クライアントで、ブラウザ、Flatpak、Electron、rootlessコンテナを壊すため、任意にしました(`auto_enable_userns_restriction`)。
- **通知、画面、言語**(1.9.40〜1.9.94): すべての通知が10言語になりました(FIM、ロックファイル、eBPF、カーネルのエクスプロイト、サーバー版のすべての警告、2つ目のインスタンスのメッセージ、ヘルスチェックの行)。サーバー版の言語は、`language` で設定します。通知履歴は、固まらなくなりました(本文は2,000文字まで、新しい200件を描画)。保存を繰り返すファイルについて、「マルウェアの疑い」の通知が繰り返されなくなりました。画面をタブに分けました(パッケージCVEのスキャン、ポートとDevIsolator)。オンボーディングの既定は、「開放」ではなく、標準の保護になりました。GTKのレイアウト、CSVの見出し、翻訳の修正がいくつかあります。
- **パッケージと更新**(1.9.75〜1.9.88): nmapのNSEのデータベースの定期更新と、CVEマップの更新が、systemdのサンドボックスで黙って失敗するか、共有のマーカーで止められていました。環境によっては、起動時にRoamSwitchが始まりませんでした。Arch系で、ヘルスチェックがaptを勧めました。Air-Gapを解除するたびに、管理者のパスワードを求められました。aptとrpmのリポジトリは、公開ジョブが失敗するまで、過去のすべての版を持っていたため、最新の1版にしました(1.10.5で、最新の3版になりました)。

### 1.0.0 - 1.9.38（初期リリース〜安定期、まとめ）

- **初回リリース(1.0.0)**: macOS版のゼロトラスト思想をLinux(systemd +
  nftables)へ移植。以降、VPNトンネル(WireGuard/Tailscale)+キルスイッチ、
  未知ポート自動遮断、受動リンク保護、日次脅威フィード、BadUSB/USB
  ストレージ承認、ARPスプーフィング自律検知等の主要防衛機能を順次追加
  (1.0.1〜1.0.66)。
- **RoamSwitch Server Editionを正式導入(1.1.0)**: Falco eBPFランタイム
  脅威検知・自動隔離、Critical Path FIM、サーバー専用25項目セキュリティ
  診断、Webhook/PagerDuty通知。以降、イベント駆動FIM・Egress/C2遮断・
  Tetragonセンサー対応・guard.yamlによる細粒度設定を追加(1.1.1〜1.3.2)。
- **パッケージCVE照合(1.5.0)**、**ログ監査のテンプレート異常検知+
  シークレット自動マスキング(1.6.0)**、**Air-Gap発動ガードのインシデント
  履歴公開(1.7.0)**、**File Scan Guard・Dockerリスク検知(1.4.1〜1.8.0)**
  を追加。
- **Wi-Fiなりすまし(Evil Twin)警告・BadUSB HIDキーストローク異常検知・
  ランサムウェア被害範囲表示(1.9.0〜1.9.5)**、**通知履歴・ClickFix検知・
  ログ監査バックグラウンド実行のクライアント版追加(1.9.6〜1.9.15)**。
- **クライアント版へのCritical Path FIM拡張、リソース枯渇・プロセス
  異常検知ガード(Server Edition)、ログ監査の数値マスキング根本バグ修正、
  Server Editionでの成否偽装バグ修正(1.9.16〜1.9.26)**。
- **サプライチェーン対策4機能(Lockfile FIM・インストールスクリプト
  一覧・npm署名検証・bubblewrapサンドボックス封じ込め)、タイポ
  スクワッティング検知、暗号資産シードフレーズ漏洩検知、Default-Deny
  ファイアウォールのDockerアウトバウンド遮断という重大な不具合の修正
  (1.9.27〜1.9.38)**。


---

## RoamSwitch for Mac

## 1.10.5

- **変更: ライセンスの署名鍵を作り直し、アプリは、古い公開鍵と新しい公開鍵の両方を受け付けます**。以前の鍵の秘密鍵は、ライセンスのバックエンドの環境とともに失われました。すでに認証したライセンスは、そのまま検証を通ります(以前の鍵も受け付けます)。新しい認証は、新しい鍵で署名されるので、**古い版のアプリでは、新しいライセンスを認証できません。先に、この版へアップグレードしてください**。
- **修正: 細工したMach-Oファイルで、rootのヘルパーを落とせました**。Homebrewのツール(WireGuard、Tailscale)を固定するとき、ヘルパーは、ツールのMach-Oのヘッダーを読みます。64ビットのfatヘッダーで、スライスの位置が `Int` の範囲を超えると、数値変換がトラップし、ヘルパーが終了しました。rootの側の解析(Mach-O、WireGuardの設定、ARPの表、アドレス、解析ワーカーのフレーム)に、壊れた入力とランダムな入力を与える新しい試験(乱数の種を固定して、約8万件)で見つけました。ヘルパーを止められるだけで、コードは実行できません。
- **追加: 特権ヘルパーが、起動のたびに、自分のサンドボックスを自己診断し、入っていない、または拒否すべきことを拒否しない場合に、知らせます**。サンドボックスは、禁止を並べる形なので、macOSの変更で、拒否しなくなっても、ヘルパーは、誰にも知らせずに動き続けていました。ヘルパーは、拒否すべき4つ(`/Library/LaunchDaemons`、`/etc/pam.d`、`/etc/sudoers.d` への書き込みと、書き込める場所からのプログラムの実行)と、許可すべき3つ(`networksetup`、`arp`、自分の状態フォルダーへの書き込み)を試し、結果を `/Library/Application Support/RoamSwitch/sandbox_selftest.json` に書きます。アプリは、問題ごとに1回、通知します(10言語)。実機のMac(macOS 27.0)で確認しました: サンドボックスに入り、拒否すべき4つは、すべて拒否され、許可すべき3つは、すべて通りました。
- **修正: ARPなりすましによる隔離を、原因の解消を確認できたら、自動で解除します**。実機のMac(Wi-Fi)で確認しました。隔離は、上限(既定3600秒、試験では300秒)で縮退し、隔離でオフになっていたWi-Fiの無線が戻ります。ゲートウェイの項目が本物のアドレスで戻り、連続3回・60秒以上の確認のあと、ヘルパーが解除します(上限300秒で、隔離から解除まで8分31秒)。**変更(Linux版にそろえました。実機のMac(Wi-Fi、無線を入れたまま、上限300秒)で確認: 3分で「解消」、5分の再確認で解除、隔離から解除まで5分13秒、縮退なし): 高信頼の隔離は、全遮断から10分が過ぎたころから再確認し、原因の解消を確認できれば、上限を待たずに解除します**(上限に達したときだけ縮退します)。これまでは、確認できても、上限(既定3600秒)まで解除されませんでした。縮退の通知に「原因の解消を確認できたときは、自動で解除します」を加えました(10言語)。アプリ本体の日本語表記は、アップデートではなく、アップグレードにそろえました。ゲートウェイの項目がないあいだは、ヘルパーが、ARPの問い合わせを1回だけ(IPではなくリンクの上なので、隔離に遮断されません)、10秒に1回まで出します。フレームはBPFで送り、隔離の外と、隔離の最中の、どちらでも、実機のMacで確認しました(無線を入れたままだと、隔離から約2分で、ゲートウェイの項目が自然に消え、問い合わせを送り、約90秒後に本物のアドレスが戻りました。返事によるのか、別の理由かは、ログでは区別できません)。無線がオフのあいだは、送る先がありません。確認の結果は、公開のログ(`category: arp-clearance`)に出ます。
- **変更: HomebrewでWireGuardやTailscaleをアップグレードしたら、版ごとに1回、通知でお知らせします**(「確認して固定する」のボタンから、固定してある版と、いま入っている版を出す確認画面へ進みます)。固定は、これまでどおり、あなたの承認なしには行いません。確認は、起動時と30分ごとです(10言語)。
- **修正: アンインストールが、自分で元に戻り、結果を出す前にクラッシュすることがありました**。ヘルパーの登録を解除するとXPCの接続が切れ、アプリは、ヘルパーを再登録していました。アプリ本体がゴミ箱にあると、`SMAppService.register()` がクラッシュし(ServiceManagementでSIGSEGV)、結果の画面が出ませんでした。アンインストールの間は、どの経路も、サービスを登録しません。**修正: 「設定・履歴・ライセンス情報も削除する」にチェックを入れても、終了時の処理が、ウォッチドッグの状態ファイルとそのフォルダを書き戻していました**。書き込みを止め、結果の画面のあとに、ユーザーのデータをもう一度消します。**追加: アンインストールで、アプリ内のMCPサーバーの実行中のものを終了します**。エディタがアプリのパスから起動したものは、アプリをゴミ箱へ移したあとも動き続け、システム設定に、RoamSwitchがバックグラウンドで実行中と出ていました。実行ファイルがこのアプリのサーバーと一致するプロセスだけを終了します。**変更: 結果の画面、ヘルプ&ガイド、FAQに、システム拡張は、承認を求められなくても、次の再起動で削除が完了することを加えました**(実機で、アプリの削除のあとに、そうなりました)。**追加: ヘルプ&ガイド**に、隔離の状態と解除の案内、復旧とアンインストール、ポートスキャンの送信元MACの照合、VPNツールの固定、確認画面のFAQを加えました(10言語)。実機のMacで、アンインストールを3回実行して、見つけ、確かめました。
- **追加: 復旧とアンインストール**。別メニュー「復旧とアンインストール」で、ネットワークを元に戻します(隔離、VPNとTailscaleのキルスイッチ、ポートスキャンの遮断、ゲートウェイのARP固定、DNS、リンクガードのhostsの項目、停止したサービス)。いまのゲートウェイのMACと信頼済みのMAC、隔離の原因が解消したかを示し、解消を確認できないときは、承認が要ります。アンインストーラは、先に同じことを行い、そのあと、ヘルパーのサービス、システム拡張、ログイン項目の登録を解除し、おとりファイルとシェルのエイリアスを取り除き、アプリをゴミ箱へ移します。
- **修正(セキュリティ): 隔離の解除が、そのときいたゲートウェイを、信頼済みとして採用していました。ゲートウェイのARP固定が、偽のMACを恒久的に固定していました**。別のマシンから偽のARPを送って見つけました。解除しても、通信が戻らず、再起動するまで接続が不安定でした。
- **追加: ポートスキャンの自動遮断が、Linuxと同じく、送信元のMACを確かめます**。SYNのフレームを `tcpdump -e` で取り込み、送信元のMACを短時間だけ覚えて、名乗っているアドレスの近隣エントリと合わないときは、遮断しません。実機のmacOSで取った `tcpdump -e` の出力で、テストしました。
- **変更(セキュリティ): Sensorとの通信を、rootのヘルパーの中では行いません**。LAN上の他の機器から届く、TLSのハンドシェイクと応答の解析は、`nobody` に権限を落とし、サンドボックスに入れた短命の子プロセスで行います。許されるのは、Sensorの制御ポートへの外向きTCP接続1本だけで、ファイル、他のポート、fork、execは使えません。ヘルパーは、署名鍵と信頼の記録を持ち、応答は、整形し直した1つのJSONオブジェクトとして受け取ります。
- **変更(セキュリティ): ヘルパーは、正規のアプリだけを相手にします**。Team IDに加えて、ハードンドランタイムが有効で、コード注入を許すentitlementがなく、build 122以上であることを要求します。古いbuildや、再署名したコピーは、使えません。
- **修正(セキュリティ): スナップショットの復元は、あなたの権限で、あなたのフォルダーに書き込みます**。確認の後にコピー先をリンクに差し替えられると、rootがその外に書き込む余地がありましたが、今はカーネルが拒否します。LinkFilterのフィードは、リンクを辿らず、ユーザーが所有するものだけを読みます。
- **修正: 1〜65535の範囲外の開発サーバーのポートや、終わらないブロック時間が保存され、ファイアウォールの規則の再構築が失敗し続ける可能性がありました**。ポートは1〜65535、ブロック時間は1秒〜24時間に制限します。
- **変更: 固定したVPNツールのフォルダーに、マニフェストにないファイルがあれば、拒否します**。
- **リリースの手順に追加: 作ったアプリを、Appleのツール(`codesign`、`spctl`、ハードンドランタイム、entitlement、クライアントのbuild)で検証し、通らなければリリースを止めます**。
- **変更(セキュリティ): `eslogger` の出力(起動した全プロセスのコマンドライン)と、`tcpdump` の出力(LAN上の機器のパケット)の解析を、rootのヘルパーの外で行います**。同じ種類の、`nobody` で動くサンドボックス内のワーカーが行います。ワーカーは、やはり `nobody` で動く小さな起動役が始めます。ヘルパー自身のサンドボックスより厳しいサンドボックスに、ワーカーが入れるようにするためです。
- **変更(セキュリティ): ヘルパーは、起動時からサンドボックスの中で動きます。ヘルパーが起動するプログラムにも及びます**。ユーザーやダウンロードが書ける場所にあるプログラムの実行と、LaunchDaemon、システムのプログラム、sudoers、PAM、sshd、ユーザーデータベースへの書き込みを、カーネルが拒否します。ヘルパーの操作が使うものは、拒否しません。
- **修正: IPv6のポートスキャンが、検知されていませんでした**。行の解析が、IPv6アドレスを、自身のコロンで切っていました。
- **追加: VPNのツールがまだ固定されておらず、接続できないときに、通知を出します(10言語)**。これまでは、ログだけでした。

## 1.10.4

- **変更(Mac版): Sensorとの制御通信は、平文にフォールバックしません**。TLS対応前にペアリングしたSensorは、証明書フィンガープリントを登録するまで接続できません。
- **追加(Mac版): 隔離中は、メニューバーに、全面か縮退かと、原因を表示します**。
- **変更(Mac版・セキュリティ): rootのヘルパーは、HomebrewのVPNツールを直接は実行しません**。`/opt/homebrew` は、あなたのユーザー権限で動くものなら何でも書き換えられるため、そこから `wg-quick`・`wg`・`wireguard-go`・bash・TailscaleのCLIをrootで実行すると、そのユーザーにrootへの道を渡すことになります。今後は、確認のうえで一度「固定」します。ツールと、読み込むライブラリを、root専用のフォルダーにコピーしてSHA-256を記録し、実行のたびに検証して、そのコピーだけを実行します。固定するまでは接続を拒否し、Homebrewで更新したときは、メニューバーが固定し直しを案内します(10言語)。
- **追加(Mac版): 隔離の状態と解除の手順を案内する画面**(メニュー「隔離の状態と解除の案内…」)。全面か縮退か、原因、その原因ごとに解除の前に確認すること、Proでなくても使える解除ボタンを表示します(10言語)。
- **修正(セキュリティ): root で動くヘルパーが、信号を送ってよいプロセスに、自前の下限を設けました**。`terminateProcess` は、ヘルパー自身、`/System/`・`/usr/libexec/`・`/usr/sbin/`・`/sbin/` の OS のデーモン、RoamSwitch 自身のバイナリを拒否します。`/bin/bash` や `/usr/bin/curl` などの一般的なツールは、これまでどおり停止できます。
- **修正(セキュリティ): `setSecureDNSServers` が、任意の文字列を DNS サーバーとして受け付けていました**。IPv4/IPv6 のアドレスだけを受け付けます。
- **修正(セキュリティ): WireGuard の取り込みが、部分一致の禁止語(`postup`、`table` など)で検査していました**。素直なトンネル用のキーだけを許可する方式にしました。コメントに「table」と書いてあるだけの正当な設定は拒否されなくなり、未知のディレクティブは常に拒否されます。
- **追加: 未信頼のネットワークで、VPN の自動接続も ARP/NDP 固定も無効なとき、メニューバーに案内を出します**。

## 1.10.3

- **修正: 認証情報ハニートークン（1.10.0で追加）が、実利用のツールを壊したり、自己誤報を起こしたりする
  内容だった問題**。sshのおとりを既定名の`~/.ssh/id_rsa`から、sshが既定では探さない
  `id_rsa_backup`に変更しました（0600だと通常のsshが誤報になり、0644だと権限警告が出るため）。AWSのおとりは
  `[default]`ではなく`[backup-admin]`、Dockerのおとりの宛先はDocker Hubではなく実在しない社内レジストリ名に
  変更し、設置は0600にしました。旧版が置いたおとりは新しい内容へ移行します（旧`id_rsa`のおとりは削除。
  本物のファイルには触れません）。
- **修正: ハニートークンの自己誤報**。監視の再有効化時に、古い基準時刻のまま自分の読み取りを不審な
  アクセスと判定する場合があったため、停止時に基準を消し、開始時に取り直すようにしました。また、
  アクセス元の実行パスが RoamSwitch 自身、または`/usr/bin/`のssh系（sshのおとりのみ）の場合は不審扱いしません。
  実行パスが特定できない場合は従来どおり警告します。
- **修正: 機密情報の漏えい監査（SecretLeakScanning）が、ハニートークンのおとりファイルを読んで、検知を
  誘発していた不具合**。パスとサイズが一致するおとりだけを読まないようにしました（同名の本物のファイルは
  従来どおり監査対象です）。
- **改善: 同一の脅威通知を10分間は連続させないようにしました**。同じ内容の脅威通知は、サウンド・バナーを
  10分間だけ抑止します（本文が変われば通常どおり通知。通知履歴には重複も含めて全件記録します。隔離状態の
  通知は対象外）。Linux版の通知洪水対策と同じ設計です。

## 1.10.2

- **修正: Tailscale Exit Nodeへの到達性確認（`tailscale ping`）が、DERPリレー経由で正常に
  疎通しているExit Nodeを「到達不能」と誤判定し、正当な手動接続まで切断していた重大な不具合**。
  `tailscale ping`は直接（P2P）接続が確立していない場合、実際にはリレー経由でpongが返っていても
  終了コードが非0になる。終了コードのみで判定していたため、自宅と異なるネットワークにいる際など、
  DERPリレー経由でしか繋がらない環境で正常なExit Node接続まで誤って切断していた。実機でpongが
  返っているのにfalse判定になることを確認し、標準出力の"pong from"も見るよう修正した。
- **修正: Tailscale Exit Nodeが、直近のいずれかのネットワークで一度でも接続失敗（到達不能・経路未
  確認）によるバックアウトが発生すると、それ以降どのネットワークに移動しても自動接続が働かなくなって
  いた不具合**。バックアウト時に立てる抑制フラグがアプリ再起動までリセットされない設計になっており、
  無関係な別ネットワークへの移動時にも抑制が持ち越されていた。ネットワーク変更を検知した時点で
  抑制フラグをリセットするようにした（「今すぐ接続」による明示的な手動接続の意思は維持したまま）。
- **修正: ブラウザ認証情報監視が、アクセス元プロセスを特定できない場合（ブラウザ終了時はほぼ毎回
  発生し、実際の脅威とは無関係）でも、通知回避後もなお履歴への記録を残していた**。特定できない場合は
  記録も含めて一切何もしないよう単純化した（実際の攻撃プロセスは実行中に読み取るため、通常は特定
  可能）。
- **変更: VPN自動接続の発動レベルをLinux版に統一**。「標準保護 (balanced)」ではなく「最大ロック
  ダウン (lockdown)」でのみ自動接続するようにした（WireGuard・Tailscale共通）。「今すぐ接続」に
  よる手動接続はレベルに関わらず維持される。
- **多言語対応: 前回追加したTailscale関連の新規通知文言（経路未保護・ネットワーク復旧失敗）に、
  未翻訳のまま残っていた9言語分の翻訳を追加**。

## 1.10.1

- **修正: `wsdd`のようにインタプリタ経由でランダムなエフェメラルUDPポートへ再バインドするプログラムが、
  再起動のたびに別ポート扱いされ、通知が無限に繰り返されていた不具合（Linux版と同じ設計で移植）**。
  ポートを含まないフィンガープリント（実行ファイルパス＋スクリプト引数）を新設し、初回のみアラート、
  以降は同一フィンガープリントである限り通知履歴への記録のみに抑制するようにしました。
- **修正: 汎用エントロピー型ランサムウェア検知（1.10.0で追加）が、無害な常駐プロセス（Spotlightの
  mdworker等）の存在によって、実際の攻撃プロセスを見逃す場合があった不具合**。安全なプロセスかどうかの
  判定を、候補プロセスを集める段階で行うよう修正し、安全でないプロセスが1件でも見つかれば、無害な
  プロセスと同時に存在していても必ずそちらを優先して検知するようにしました。
- **修正: 認証情報ハニートークン（1.10.0で追加）が、ハニートークンファイル作成後にアプリを再起動すると
  監視が無音で停止していた不具合**。再起動時にも、自分が過去に設置したハニートークンファイルだと
  判定できた場合は監視を再開するようにしました。
- **修正: プロセス実行の記録のDYLD_INSERT_LIBRARIES検知（1.10.0で追加）が、Xcodeでのデバッグ実行の
  たびに誤検知していた不具合**。判定基準を、実行したバイナリ自身の署名ではなく、注入されたライブラリ
  自身が信頼できる場所（システム領域・Xcode.app内）にあるかどうかに変更しました。
- **修正: リンク保護のブランドなりすまし（ホモグラフ）検知が、Apple・PayPalの2ブランドについて、
  一度も機能していなかった不具合**。內部の文字正規化処理に、文字"l"を常に別の文字へ変換してしまう
  誤りがあり、この2ブランド名の判定が構造的に成立しない状態でした。あわせて、`app1e.com`のような
  1文字だけを似た記号に置き換える手口の検知漏れも修正し、`microsoft-security-alert.com`のような、
  ブランド名と「security」「support」等の語を組み合わせた新規ドメインによるなりすまし（フィード側の
  把握が追いつく前の新規登録ドメインで特に有効）を新たに検知するようにしました。
- **修正: Web・メールダウンロード保護（ブラウザ/メールでダウンロードされたファイルの即時スキャン）が、
  起動直後の内部的な不具合により、一度も動作していなかった重大な不具合**。初回起動時にだけ表示される
  フォルダアクセスの案内ダイアログの表示タイミングが原因で、アプリの起動シーケンス中にクラッシュする
  ことがありました。この機能に依存するEICARテスト検知等が効かなかったのは、検知精度の問題ではなく
  この起動時クラッシュが原因でした。
- **修正: 起動直後にゲートウェイのMACアドレス解決が一時的に失敗すると、以後ずっと失敗したまま
  固定され、「現在のネットワークを信頼済みネットワークに登録」が反応しなくなる不具合**。再試行の
  対象を起動直後の初回失敗にも広げ、さらに解決できるまでバックグラウンドで自己回復リトライを
  続けるようにしました。
- **修正: Tailscale Exit Nodeが、tailscaled側の自己申告（接続中の報告）のみを信頼しており、実際の
  通信がExit Node経由になっているかを検証していなかった重大な不具合**。tailscaled自身にmacOS固有の
  既知バグ（Exit Node設定後もOSのデフォルトルートが物理ネットワーク側のままになることがある）が
  あり、RoamSwitchが「保護中」と表示していても実際には保護されていないサイレントな失敗状態が
  発生し得ました。実際のOSデフォルトルートを独立検証し、経路が確立していなければExit Nodeの選択を
  解除した上で通知するようにしました。あわせて、バックアウト時に自動リトライを止め忘れて通知が
  繰り返される不具合、Exit Node切断後のネットワーク復旧処理（DHCP再取得）がネットワークサービスの
  再有効化コマンドの終了コードのみを見て、実際に有効なアドレスを取得できたかを確認せずに成功を
  報告していた不具合（放置するとMacがネットに全く繋がらなくなる場合がありました）も修正しました。
- **修正: ブラウザ認証情報監視が、Chrome/Brave等のHelperプロセス（レンダラー・GPU等）による正当な
  アクセスを不審なアクセスとして誤検知していた不具合**。プロセス名の取得に`lsof`の人間可読形式の
  出力を使っており、長いプロセス名（例:「Google Chrome Helper (Renderer)」）が切り詰められて
  許可リストと一致しなくなっていました。切り詰めのない機械可読形式に変更して修正しました。

## 1.10.0

- **追加: 汎用エントロピー型ランサムウェア検知**。おとりファイルの改ざんに加え、Documents・Desktop・
  Downloads・Pictures フォルダへの書き込みのエントロピー（データのランダムさ）を解析し、短時間に
  大量の暗号化されたようなファイルが書き込まれた場合を検知します。おとりファイルの無い場所での
  暗号化も捉えられます。検知すると、既存のランサムウェア検知と同じくネットワークを緊急遮断します
  （Pro、既定オン）。
- **追加: フォレンジック証跡バンドル**。緊急ネットワーク遮断が発動すると、その時点のプロセス一覧・
  ネットワーク接続・直近の変更ファイルを、SHA-256付きで自動保存します。
- **追加: 認証情報ハニートークン**。`~/.aws/credentials`・`~/.ssh/id_rsa`・`~/.docker/config.json`に
  偽の認証情報ファイルを設置し、アクセスされたら通知します（Pro、既定オン。既存の本物のファイルは
  上書きしません）。
- **追加: ブラウザ認証情報アクセス監視**。Chrome・Firefox等の保存済みパスワード・Cookieに、ブラウザ
  以外のプロセスがアクセスした場合に通知します（Pro内でも既定オフ、任意で有効化）。
- **追加: プロセス実行の記録の検知ルールを2件追加しました**。python3・perl・ruby・php・pwshの
  インライン実行でbase64/evalデコードとcurl/wget等が同じ行に現れるパターン、
  `DYLD_INSERT_LIBRARIES`環境変数によるdylibインジェクションを新たに検知します（Pro）。
- **追加: 脆弱性スキャンの実施状況（経過日数）を確認できるようになりました**。実証型脆弱性診断の
  各項目について、最後に確認できた結果と、それからの経過日数をMCPツールから確認できます。
- **追加: 健全性診断（18項目）に、NIST CSF 2.0 / CIS Controls v8 のマッピングを追加しました**。
  監査対応や法人利用での説明にお使いいただけます。
- **追加: roamswitch-mcp（MCPサーバー）に、AIエージェント向けの検知結果トリアージ手順（skill）を
  4本追加しました**。

## 1.9.6 - 1.9.54(まとめ)

これらの版の内容を、テーマ別にまとめました。各項目の全文は、このファイルのgitの履歴にあります。

- **隔離(Air-Gap)の作り直し**(1.9.52、1.9.23): 何が隔離のきっかけだったかで、終わり方が決まります。ランサムウェアのおとりの改ざん、XProtectの確定した検知、ARPの警告から選んだ手動の隔離は「確度が高い」、それ以外(ClickFix、ポートの異常など)は「確度が低い」です。確度が低いものは、10分で自動で解除されます。確度が高いものは、10分では解除されず、上限(既定1時間)まで全遮断を続け、原因の解消を確認できれば解除し、できなければ「縮退モード」(新規の通信は遮断したまま、時間では開かない)に移ります。すでに開いている接続は、隔離の開始時に切り、状態は、ヘルパーの再起動をまたいで残ります。隔離でオフになったWi-Fiの無線は、アプリのクラッシュや再起動のあとも、独立した監視で戻ります(1.9.23)。(1.9.52では、ARPなりすましによる隔離は、自動では解除されませんでした。1.10.5で変わりました。)
- **ランサムウェア**(1.9.50、1.9.51): 「ランサムウェアからの復旧」(Pro)は、APFSのローカルスナップショットを、既定で6時間ごと(オフ、1、3、6、12、24時間)に取り、検知の時にも1つ取り、暗号化の直前の世代が押し出されないよう、最長7日間、整理を止めます。復旧は手動で、ファイル単位で、`~/RoamSwitch-Recovered/` へコピーし、いまのファイルは上書きしません(ヘルパーに、フルディスクアクセスが要ります)。読み取り専用のMCPツール `get_ransomware_recovery_snapshots` で、スナップショットの一覧を見られます。おとりの改ざんから緊急遮断までは、数分から約2秒になりました(開いているファイルの一覧の全行で、コード署名の確認をしていたため)。
- **プロセスの実行の記録**(1.9.52、Pro): このMacの中だけで、macOSの `eslogger` で、プロセスの起動を記録します(既定はオフ、フルディスクアクセスが要ります)。改ざんを検出できる形で保存し、何も遮断しません。7つの振る舞いを通知します(ブラウザ、Office、メールアプリがシェルを直接起動、一時フォルダーや隔離フラグつきの未署名のバイナリの実行、ダウンロードをシェルへパイプ、base64やevalつきの `osascript`、隔離フラグを外した直後のファイルの実行、新しく書かれたLaunchAgentからの未署名のバイナリの起動、Apple以外の親プロセスによるキーチェーンの読み取り)。バナーは重大度が高いときだけ、種類ごとに10分に1回まで。「このプログラムを許可」は、キーチェーンの1項目だけを許可します。MCPツール `search_exec_events` と `get_process_tree`。
- **ポートスキャンと未知のポート**(1.9.32、1.9.44〜1.9.49、1.9.54): nmapやmasscanのようなスキャンを、自動で検知し、10分間ブロックします(Pro)。ゲートウェイのアドレスを名乗る偽のSYNで、ブロックを悪用することはできません(新規の受信TCPだけを拒否し、ゲートウェイ、DNSリゾルバー、このMacのアドレスは対象外)。起動の直後に検知が止まる問題(`pflog0`)と、偽のSYNの洪水でpfの状態表が埋まる問題も直しました。rootのプログラムが開いたポートは、特権ヘルパーが見つけ、libprocで読みます(2秒ごと。`lsof` の約16msに対し、約2.4ms)。署名済みの実行ファイルを、更新日時を戻した未署名のものに差し替えても、気づきます。インタープリタや未署名のプログラムが開いたUDPのポートも報告し、`python3` や `node` の向こうの、本当のプログラム名を、通知に出します。
- **能動的な脆弱性の確認とログ**(1.9.44、1.9.53): 認証なしのElasticsearch、CouchDB、Jenkins、VNC、SMBv1、SMB署名を加えました。CSVの書き出しは、安全と判断不能の結果を、正しく説明し、スキャンごとにIDと、ロックつきのログを持ちます。
- **RoamSwitch Sensor**(1.9.32〜1.9.37、1.9.52): 相互のペアリングを、mDNSから、ペアリングコードに変えました。Sensorに能動的な監査を依頼し、結果をあとで取得できます(MCPツール `get_sensor_audit_results`)。証明書のフィンガープリントを固定すると、Sensor 0.3.9以降と、すべてTLSで通信します。ペアリングが成功するたびに、特権ヘルパーがクラッシュする問題を直しました。
- **サプライチェーンと秘密情報**(1.9.27〜1.9.30): 依存関係のロックファイルの改ざんの監視、インストールスクリプトの一覧、npmの署名と来歴の確認、ネットワークを遮断したサンドボックスで、インストールを実行する `roamswitch-npm`、編集距離によるタイポスクワットの検知、暗号資産のシードフレーズと秘密鍵の漏洩の検知(チェックサムで検証)。Proです。
- **ログの監査と通知**(1.9.6〜1.9.26): 過去7日間の通知履歴。自動ログ監査(Pro)は、1時間ごとに、新しいパターンと頻度の急増を確認し、テンプレートごとに、自分の履歴と比べます。クリップボード上のClickFix型のコマンド(スクリプトエディタの経由を含む)も検知します。多くの誤検知(CoreAudio、loginwindow、`PersistentAppsSupport`/`BTMManager`、`CFPasteboardRef`、ポインタアドレス)を取り除き、通知は、対応が要るかを、平易に説明し、EICARの試験の検知は通知を出さず、ログ監査の画面のカードは、クリックできるようになりました。
- **重要パスのFIM**(1.9.16、Pro): `/etc/sudoers`、SSHの設定、`/etc/hosts`、rootの `authorized_keys` などの、SHA-256の基準の監視。
- **成功したことにしていた報告**(1.9.19): 確認せずに成功と報告していた4項目(開発サーバーのポートの自動遮断、マルウェアの隔離の移動、合格に固定されていた「macOSのアクセサリ接続の保護」の項目、重要パスのFIMのヘルパー接続の失敗)は、実際の結果を示すようになりました。
- **更新と言語**(1.9.26、1.9.34〜1.9.43): 更新のとき、特権ヘルパーを確実に再起動します。更新の直後の警告の連発と、偽の「/etc/hostsが改ざんされた」警告を直しました。更新の案内は、アプリを前面に出し、共有サービスの停止と復元は、`launchctl` の終了コードを確認します。リンクガードのホモグラフの検知と、ランサムウェアの防御のMITRE ATT&CKのIDは、言語に依存しない値ではなく、翻訳された文字列を比べていて、日本語、英語、フランス語以外の多くの言語で動きませんでした。CSVの見出し、パッケージCVEのタブ、いくつかの文言は、10言語で直しました。

## 1.0.0 - 1.9.5（初期リリース〜安定期、まとめ）

- **初期公開(1.0.0〜1.3.0)**: ゲートウェイMACアドレスによる信頼ネット
  ワーク自律識別、ファイアウォール/共有サービス自動切替、未知ポート
  自動遮断、ARPスプーフィング対応、読み取り専用MCPサーバー初搭載。
- **ローカルAI/LLM推論サーバー保護・クリップボード機密保護・MCPサーバー
  のOSS公開(1.4.0〜1.5.9)**。
- **VPNトンネル+キルスイッチ(WireGuard)・予防的ゲートウェイARP/NDP固定・
  BadUSB/USBストレージ承認ガード・受動リンク保護・カナリアおとり
  ファイル永続化(1.6.0〜1.7.6)**。
- **緊急確認ダイアログの最前面化・セキュリティ診断のワンクリック修復・
  VPNデュアルバックエンド(WireGuard/Tailscale)対応(1.8.0〜1.8.3)**。
- **静的シグネチャダウンロード検知・ClickFix対策・Dockerリスク検知
  ガードを新設、Apple XProtect連動の自動Air-Gap隔離、機密情報漏洩監査の
  フォルダ全体スキャン対応(1.8.4〜1.8.9)**。
- **実証型脆弱性診断・パッケージCVE照合をLinux版から移植、Wi-Fiなりすまし
  (Evil Twin)警告、BadUSBキーストローク異常検知、MCPサーバーの機能欠落
  解消(1.9.0〜1.9.5)**。
