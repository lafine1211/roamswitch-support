# 変更履歴

[English](CHANGELOG.md) | **日本語**

RoamSwitch のユーザー向け変更点。Mac 版（1.x）と
Linux 版（別系列・1.0.x）でバージョン番号は独立しています。

---

## RoamSwitch for Linux

Linux 版（systemd + nftables）。apt / dnf / zypper で配布（GPG 署名）。
詳しくは <https://lafine.net/linux>。

### 1.9.18

- **重要: ログ監査のテンプレート化(新規パターン検知の基盤)にあった
  根本的な数値マスキングのバグを修正**: 数字と英字が直接隣接する箇所
  (ISO8601タイムスタンプの`...-10T04:...`、`wait=3h17m58s`のような
  時間表記)では、内部のマスキング正規表現が単語境界を要求していた
  ため数字が一切マスキングされずに残っていた。roamswitch-server-daemon
  自身の自己ログには、まさにこの形のマイクロ秒精度タイムスタンプが
  自動的に埋め込まれるため、「FIM routine integrity verification
  passed」等の本来同一のはずのログが、タイムスタンプの違いだけで
  毎回別々の「新規パターン」として扱われ続け、学習によって既知化する
  ことが原理的に不可能だった。本番環境の既知テンプレート691件に修正後
  のマスキングを適用して検証したところ285件(58.8%減)に収束することを
  確認。これまで個別に追加してきた自己ログの除外の多くは、実質的に
  このバグの応急処置だった。

### 1.9.17

- **追加: クライアント版にもCritical Path FIM(改ざん検知)の自動バック
  グラウンド実行を追加**: これまでFIMはServer Editionのみの機能で、
  日常使いのクライアント機はroot権限を奪われても改ざんを自動検知する
  仕組みが無かった(手動の`roamswitch fim verify`のみ)。event-driven
  fanotify監視+周期バックストップスキャンをクライアント版daemonにも
  追加し、監視対象にsystemdバイナリ本体(`/usr/lib/systemd/systemd`)も
  加えた。apt/dnf/zypperのパッケージ更新に伴う正規のファイル差し替え
  を、進行中のパッケージマネージャートランザクションをロックファイル
  で確認する構造的な判定で見分け、一時的な「改ざん検知」を誤報しない
  ようにした(周期スキャンでの再検証は引き続き行われるため検知漏れ
  にはならない)。改ざん検知の警告ログには、書き込みを行った実際の
  プロセス名も記録されるようになった(判定はプロセス名の列挙リストに
  頼らない構造的な方式のまま)。パッケージング(apt Post-Invokeフック・
  RPM系のpathユニット)もクライアント版deb/rpmに展開。

### 1.9.16

- **追加: 新規パターンのみ(頻度急増なし)の異常は、ホストの学習開始から
  7日間は自動通知しない保護機構**: 個別ノイズ除外とsystemdライフサイクル
  の構造的除外だけでは、未知の構成要素が多いホストで初回学習直後に
  「新規パターン」通知が集中し、実際に過剰通知による離脱が確認された
  ための追加防御。頻度急増検知・USB/ポート/FIMガード・手動および
  MCP経由でのログ監査結果の閲覧には影響しない。

### 1.9.6 - 1.9.15

- **ログ監査の自動バックグラウンド実行をクライアント版daemonにも追加**:
  Server Editionのみ自動実行していた非対称を解消。
- **通知履歴(過去1週間保持)を追加**: `roamswitch notifications`・GUI・
  MCP・SDKから参照可能。
- **クリップボード上のClickFix型不正コマンド検知を追加**。
- **ログ監査の誤検知を多数解消**: daemon自身の起動バナー・自己申告ログ、
  snapd/gnome-shellのOSノイズ、Ollama/Ubuntu Proの定常ログを除外。
  systemdユニットのライフサイクル報告は、個別サービス名の列挙ではなく
  「送信元がsystemd自身(journaldのカーネル認証情報により偽装不可能)、
  かつ固定文言に一致」という構造的な判定に変更し、未知サービスにも
  自動対応。
- **daemon再起動時の新旧プロセス競合、および監査ウィンドウの重複に
  よる二重通知・学習消失を解消**。
- **通知履歴が一般ユーザーのCLIから常に空に見えていた不具合を修正**
  (root権限daemonとの保存先不一致)。
- **通知本文を改善**: 「新規パターンN件・頻度急増K件」の内訳と、
  読みやすい実ログ行の例示を表示するように。

### 1.9.0 - 1.9.5

- **Wi-Fiなりすまし(Evil Twin SSID)警告**: 過去に接続したWi-Fiの
  {SSID, ゲートウェイMAC}を学習し、酷似した未知のSSIDを警告。
- **USB HIDキーストロークの異常検知(BadUSB補助シグナル)**、
  **ランサムウェア・カナリアの被害範囲(blast radius)表示**、
  **Link GuardのJA3 TLSフィンガープリント照合基盤**を追加。
- **ログ監査の頻度急増検知を、テンプレート自身の過去の頻度と比較する
  方式に再設計**: 正規の定期ジョブや`apt upgrade`起因のバーストを
  毎回誤検知していた問題を解消。
- **upower.serviceのクラッシュループ**や**パッケージ更新に伴うバースト**
  が誤って異常検知される問題を修正。
- **Link GuardのNFQUEUEが正常なパケットバーストで不要にクラッシュ・
  再構築していた不具合を修正**。

### 1.4.1 - 1.8.0

- **パッケージCVE照合機能を追加**: OSパッケージ(dpkg/pacman/dnf/zypper)と
  開発言語7エコシステム(npm/PyPI/crates.io等)の依存関係を、ローカルに
  保持した既知CVEマップと突き合わせ。ネットワーク接続は一切行わない
  (1.5.0)。
- **ログ監査にテンプレート異常検知 + シークレット自動マスキングを追加**:
  未知パターン・頻度異常(Z-score)の検知と、ログに含まれるAPIキー等を
  クリップボード・MCP経由の生JSON双方でマスキング(1.6.0)。
- **Air-Gap発動3ガード(eBPFランタイムガード/Port Anomaly/カナリア)の
  インシデント履歴をMCP・CLIに公開**: 発動理由をdaemonの外から確認可能に
  (1.7.0)。
- **File Scan Guard新設・Dockerリスク検知・コンテナ隔離ポスチャ監査を追加**、
  機密情報・APIキー漏洩スキャナがフォルダ全体を再帰的にスキャン可能に
  (1.4.1、1.4.3)。
- **マルウェア検知通知にClamAVの裏付け検査を追加**し、誤検知時は緊急度を
  下げてアラート疲れを軽減(1.8.0)。
- **重要な不具合修正**: Link GuardのNFQUEUEキュー詰まりによるインストール
  直後のネット接続断(1.5.1)、USB Zero-Trust有効時にキーボード以外の新規
  USBデバイスが恒久的に使用不能(1.5.3)、CVEマップが公開当日は常に
  「未取得」扱い(1.5.2)、RHEL/openSUSE向けCVEマップがファイルサイズ
  超過で一度も配信できていなかった(1.5.2、1.5.3)、`clamd`が未使用でも
  常時1GB超のメモリを消費(1.5.4)。

### 1.1.1 - 1.3.2

- **Server Edition の設定・ドキュメント全面整合**: 運用マニュアルに記載された
  設定項目をすべて実際に機能するように修正 — Docker バイパス防止のON/OFF、
  SSH保守経路温存のON/OFF、`isolate`/`freeze`/`alert_only` の封じ込めモード、
  eBPFソケットパスの変更に加え、重大度ごとに動作を細かく指定できる新しい
  任意設定ファイル `guard.yaml`（同一インシデントの再発でホスト隔離へ自動
  エスカレーション、確認応答が無ければ自動復帰するセーフティタイマー付き）
  を追加。
- **イベント駆動のファイル改ざん監視 (FIM)**: 監視対象ファイルへの書き込み
  完了の瞬間に改ざんを検知するように（従来は周期スキャン待ちで最大数分の
  遅延）。Fedora/RHEL/openSUSE でも Debian/Ubuntu と同様、パッケージ更新後の
  FIM ベースライン自動更新に対応。
- **サーバー版の Egress / C2 遮断**: ローカルの悪性IPブロックリストと任意の
  DNSシンクホール（社内DNS環境を壊さないよう既定OFF）を追加。ランサムウェア・
  カナリアと恒常的カーネル堅牢化もサーバー版で稼働するように。
- **Tetragon センサー対応**: eBPF ランタイムガードが Falco に加えて Cilium の
  Tetragon（ネイティブ gRPC、ローカル UNIX ソケット限定・新規リッスンポート
  無し）にも対応（クライアント・サーバー両版）。
- **自律封じ込めの安全性強化**: プロセスを凍結・強制終了しうる全経路に、
  基盤デーモン（init・コンテナランタイム・パッケージマネージャー・
  ブラウザ等）を除外する共通の保護リストを適用。プロセス単位のネットワーク
  隔離も、何にも一致しない不完全なルールを使っていた不具合を修正し、実際の
  UID またはコンテナを正しく指定する方式に改めた。
- **クライアント版: Air-Gap 復旧の安全性向上と適応型検知感度**:
  「解除」操作は、該当プロセスが判明している場合はネットワーク遮断を解く前に
  そのプロセスを終了するように変更（従来は遮断解除のみ）。eBPF の検知感度も、
  未信頼ネットワークでは自動的に強化し、信頼ネットワークに戻ると緩和するように。
- **診断・通知の整理**: セキュリティ診断表示の「CVE-2026-53362」表記を
  「Frag Gap」のみに簡潔化。起動直後、既知のネットワークに接続しただけでは
  「保護プロファイル切替」通知が出ないように調整（実際のローミング時は
  従来どおり通知）。

### 1.1.0

- **RoamSwitch Server Edition（サーバー・クラウド版）の正式導入**:
  - Falco eBPF ランタイム脅威検知と自動隔離（UNIXドメインソケット `/var/run/roamswitch/events.sock` 連携）。
  - 重要パス改ざん検知（FIM: Critical Path File Integrity Monitoring, SHA-256）と自動リストア。
  - サーバー専用25項目セキュリティ・ヘルスチェック（カーネルパラメータ、Docker特権コンテナ/ソケット露出、eBPF LSM、SYN cookies、空パスワードアカウント等）。
  - Webhook（Slack / Discord / Teams / Generic）および PagerDuty によるリアルタイム障害・脅威通知。
- **MCP サーバー（`roamswitch-mcp`）の Server Edition 対応**:
  - `get_security_report` ツールに `isServer: true` 引数を追加。Claude や AI エージェントから 25 項目のサーバーセキュリティ診断結果を直接取得可能に。
- **SDK（`roamswitchkit` / Python `roamswitch`）のサーバー診断メソッド追加**:
  - Rust SDK: `client.server_security_report().await?` を追加。
  - Python SDK: `client.server_security_report()` および `client.security_report(is_server=True)` を追加。
- **CLI サーバー管理機能の拡充**:
  - `roamswitch status --server`、`roamswitch server [config|setup|test-notify|restart]`、`roamswitch fim [verify|update]`、`roamswitch emergency-restore` コマンドを追加。

### 1.0.31 - 1.0.66

- **アップデート体験の刷新**: 進行状況スピナー付きの手動「アップデートを確認」
  ボタン、更新ステータスの1カード統合、新バージョンがある時だけ表示する
  アップグレードボタン、パッケージ更新後のアプリ自動再起動。
- **マルチディストリビューション対応**: Fedora/RHEL（`dnf`）、openSUSE
  （`zypper`）、Arch Linux（`pacman`/AUR）のアップグレード手順を
  Debian/Ubuntu（`apt`）と同様にネイティブ表示。全10言語に対応。
- **デーモンIPCソケット認証保護とVault堅牢化**: `SO_PEERCRED` による実ユーザー/root限定アクセス制御、シンボリックリンク解決・プレフィックス検証による任意ファイル削除・特権昇格防止、WireGuard引数サニタイズ。
- **セキュリティ診断の判定基準適正化とSBC最適化**: 無人自動更新の実効性検証、Raspberry Pi等のSBCにおける誤判定解消、SSH分割設定監査。
- **UIスレッド完全非同期化**: GNOME / KDE / XFCE / Wayland各環境への適合、
  無線復旧とAir-Gap耐性の強化。
- **ARPスプーフィング検知・復旧の抜本的改修**: 空間的・時間的差分チェックの
  組み合わせ、未確認ゲートウェイMACの誤固定防止、個別強化ボタンの追加。

### 1.0.1 - 1.0.30

- **主要防衛機能の追加**: VPNトンネル(WireGuard/Tailscale)とキルスイッチ、
  未知ポート自動遮断、受動リンク保護、日次脅威フィードアップデータ、
  BadUSBキーボード・USBストレージ承認ダイアログ、共有サービス(SSH/Samba等)
  の自動停止・復元。
- **検知しきい値の適正化**: ランサムウェア暗号化検知しきい値(5秒20ファイル/
  エントロピー7.92以上)、予防的ゲートウェイARP固定。
- **UIとデスクトップ環境の整備**: UIスレッド完全非同期化、タスクバー・
  システムトレイアイコンの動的切り替え、Tailscale/WireGuardの安定性強化。
- **初期セットアップと公式リポジトリ**: 初回セットアップウィザード、
  apt / dnf / zypper公式リポジトリ（GPG署名）の開設。

### 1.0.0

- Linux版初回リリース。macOS版のゼロトラスト思想をLinux（systemd + nftables）へ移植。接続先Wi-Fi/有線の自律識別、ランサムウェア挙動検知・緊急Air-Gap隔離、20項目セキュリティ診断、MCPサーバー搭載。


---

## RoamSwitch for Mac

## 1.9.17

- **改善: ログ監査のloginwindow起因誤検知を、個別文言の列挙ではなく
  クラス単位の構造的判定に変更**: `-[Application setState:]`・
  `-[ApplicationManager handleCASEvent:withData:] | kLSNotify...`が、
  通常のアプリ起動・終了・フォーカス移動のたびに出力される内部
  ブックキーピングにもかかわらず誤検知されていた(実際の通知で
  「新規パターン5件」として発報)。数日のうちに同種の除外を3件
  個別追加していたため、このMac自身の直近24時間分のloginwindow
  ログ(16,340件)を実地調査したところ、`Application`/
  `ApplicationManager`という2つの汎用AppKit/LaunchServicesクラスだけで
  全体の23%(1,756件)を占め、その全件が認証関連キーワードを一切含ま
  ない一方、`LWScreenLock`・`LWAuthServiceManager`・`LWPAMManager`等の
  loginwindow自身の認証ドメインクラスは同じトレース書式でも除外対象
  にならないことを確認できた。これに基づき、選択子や文言ではなく
  「クラス名がApplication/ApplicationManagerか」で一括除外するよう
  変更。将来これら2クラスが新しい選択子でログを出しても、個別追加
  なしに自動対応する。

## 1.9.16

- **追加: 重要システムファイルの改ざんをバックグラウンド監視する
  Critical Path FIM (Pro)**: `/etc/sudoers`・`/etc/pam.d/sudo`・
  `/etc/ssh/sshd_config`(+`sshd_config.d`)・`/etc/hosts`・rootの
  `authorized_keys`という、OSアップデートやHomebrewでは通常触られない
  低頻度変更パスに絞ったSHA-256ベースライン監視(roamswitch-linuxの
  Critical Path FIMのMac版対応)。LaunchAgent/Daemon全般は誤検知率が
  高いため対象外とし、既存の署名検証+Homebrew Cellar配下判定による
  監視(1.9.x以前から搭載)に引き続き委ねている。FSEventsによる準
  リアルタイム検知+1時間ごとの周期バックストップスキャンで、実際の
  改ざんは人間が明示的に再承認するまで再検知され続ける(自己修復しない)。
- **修正: CoreAudio HAL内部トレースログの誤検知**: `HALC_ProxyIOContext`
  等、loginwindow経由で稀に出力される内部トレースログが、ログ監査の
  異常として誤検知されていた。除外リストに追加。

## 1.9.13 - 1.9.15

- **自動ログ監査(1.9.10で追加)まわりの誤検知・二重通知を解消**: 有効化
  直後のキックオフスキャンは学習のみ行い通知しないよう修正、
  CFPasteboardRefの一時的な接続断ノイズとAppKit内部のフォーカス移動
  KVO通知(`firstResponder changed`)を除外リストに追加、重複する
  スキャンウィンドウによる同一事象の再検知・再通知を解消。
- **ログイン/ログアウト時のPersistentAppsSupport/BTMManagerイベントの
  誤検知を解消**。
- **通知本文を改善**: 内訳(新規パターンN件・頻度急増K件)+読みやすい
  実ログ行の例示を表示するように。

## 1.9.6 - 1.9.12

- **クリップボード上のClickFix型不正コマンド検知を追加**: Script Editor
  経由でTerminalの履歴監視を回避する手口にも対応。
- **通知履歴(過去1週間保持)を追加**。
- **自動ログ監査(Pro)を追加**: 1時間ごとにバックグラウンドで頻度異常の
  ベースラインを学習・通知(既定オン)。
- **ログ監査の頻度急増検知を、テンプレート自身の過去の頻度と比較する
  方式に再設計**: 正規の定期ジョブの誤検知を削減。
- **自動ログ監査の通知文言2件の翻訳漏れを修正**。

## 1.9.0 - 1.9.5

- **実証型脆弱性診断とパッケージCVE照合を追加**(Linux版から移植、既定オフ)。
- **MCPサーバーの機能欠落を解消**: 機密情報漏洩スキャン・ログ監査・隔離
  ファイル一覧・カナリア監視状況等をMCP経由で取得可能に。
- **Wi-Fiなりすまし(Evil Twin)警告**、**BadUSBキーストローク異常検知**、
  **Link GuardのJA3フィンガープリント照合基盤**を追加。
- **ダウンロードガードの静的シグネチャ検知にClamAVの裏付け検査を追加**。
- **ランサムウェア防護のAir-Gap隔離解除直後に誤検知が繰り返し発生する
  不具合を修正**。
- **重要: 特定条件下でMCPサーバーがクラッシュする不具合を修正**:
  ローカライズ文字列キャッシュが並行アクセスに未対応だった問題を解消。

## 1.8.4 - 1.8.9

- **静的シグネチャによるダウンロードファイル検知・ClickFix対策(既定OFF)・
  Dockerリスク検知ガード(既定OFF)を新設**: EICARテスト署名や定番リバース
  シェル一行コマンドの検知、不審なTerminalコマンド実行時の緊急遮断、
  `--privileged`起動等のコンテナ脱出リスク検知。
- **Apple XProtect検知に連動する自動Air-Gap隔離を追加**(Pro、既定ON)。
  エアギャップ・フェイルセーフを完全に独立した監視デーモンとして再設計し、
  ヘルパープロセスがクラッシュしてもタイムアウトが確実に解除されるように。
  *(Dev.to の技術ディスカッションにおける [Super Funicular 氏](https://dev.to/superfunicular)（[該当記事](https://dev.to/superfunicular/turn-an-old-android-phone-into-a-screen-off-security-camera-no-cloud-lan-only-5cll)）の洞察と提起に深く感謝いたします。)*
- **機密情報・APIキー漏洩監査がフォルダ全体をスキャン可能に**。
- **リンク保護「警告」モードをfail-closedに変更**し保留時間短縮(25秒→
  8秒)、特権ヘルパーの署名検証(動的audit_token検証)・外部バイナリ実行前
  の検証を強化。
- **ローカルポート監査時にエフェメラルなCookie分離を追加**し、認証Cookie
  の漏洩・セッション汚染を防止。
- 上記期間中、ダイアログ表示の不具合・多言語翻訳漏れ92件・Helperバージョン
  同期不具合など複数の軽微な修正あり。

## 1.8.0 - 1.8.3

- **緊急確認ダイアログの最前面オーバーレイ化**: 脅威検知時の4種の確認ダイアログ（BadUSB、ランサムウェア、ARPスプーフィング、リンク保護保留）が他アプリや全画面の背後に隠れず最前面に表示されるよう改善。
- **セキュリティ診断の項目別修復**: 要対策項目へのワンクリック「有効化」「設定を開く」ボタンの追加と即時再診断対応。
- **マルウェア検疫の適正化**: 無害なEICARテスト文字列の誤隔離防止（通知のみ表示、Linux版と挙動統一）。
- **リンク保護のコンテンツフィルタ強化**: ネットワーク拡張機能によるDoH/DoT・SNI検査対応、前面パネルによる警告・安全側ブロック判定。
- **VPNデュアルバックエンド対応**: WireGuardに加えてTailscale Exit Node連携に対応。

## 1.6.0 - 1.7.6

- **VPNトンネル（キルスイッチ付き）の追加**: 未信頼ネットワーク接続時の自動暗号化トンネル（WireGuard）と通信遮断キルスイッチ機能の導入。
- **予防的ゲートウェイARP/NDP固定**: カフェ等の未登録ネットワーク接続時にルーターMACを固定し、MITM攻撃を未然に防止。
- **不正USB / BadUSB物理キーボード承認ガード**、**USBストレージ承認プロンプト**を追加。
- **受動リンク保護**: 詐欺サイト一覧とブランド偽装判定に基づくフィッシング接続の自動遮断。
- **カナリアおとりファイルの永続化**とベースラインハッシュのディスク保存、**隔離Vaultの安全保護強化**(chmod 000)。

## 1.4.0 - 1.5.9

- **ローカルAI / LLM推論サーバー保護**: Ollama、LM Studio、Gradio、vLLM等の0.0.0.0露出検知とプロアクティブ遮断。
- **クリップボード機密保護**: APIキーや秘密鍵のクリップボード保持をローカル正規表現スキャンで検知・警告。
- **オープンソースMCPサーバー公開**: 読み取り専用MCPサーバーおよび検知ロジックをOSSとして公開。
- **Web・メール3重保護ガード**、**特権ヘルパーのセキュリティ堅牢化**、
  **クラッシュ自動復旧（Watchdog）**を追加。

## 1.0.0 - 1.3.0

- **初期公開および基本機能リリース**: ゲートウェイMACアドレスによる信頼ネットワーク自律識別、ファイアウォール/共有サービス自動切替、未知ポート自動遮断、ARPスプーフィング対応、読み取り専用MCPサーバーの初搭載。
