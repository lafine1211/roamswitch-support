# 変更履歴

[English](CHANGELOG.md) | **日本語**

RoamSwitch のユーザー向け変更点。Mac 版（1.x）と
Linux 版（別系列・1.0.x）でバージョン番号は独立しています。

---

## RoamSwitch for Linux

Linux 版（systemd + nftables）。apt / dnf / zypper で配布（GPG 署名）。
詳しくは <https://lafine.net/linux>。

### 1.8.0

- **改善: マルウェア検知通知にClamAVの裏付け検査を追加**。システム全体
  監視（fanotify）の自前YARA風エンジンが「危険なシグネチャ」を検出した
  際、これまでユーザは内部のルール名だけを頼りに「隔離」「許可」を判断
  する必要があり、実際に無害なファイル（プロジェクト自身のドキュメント
  やJSON/dbファイルなど）を誤検知した実例もあった。判定後にClamAV
  （clamdscan/clamscan）でも同じファイルを検査するようになり、一致すれ
  ば検出名を、不一致なら「誤検知の可能性がある」旨を確認ダイアログ・通
  知本文に追記するようにした。ClamAVが不一致の場合は通知の緊急度を
  criticalからnormalへ下げ、誤検知によるアラート疲れを軽減する。検査結
  果に関わらず、隔離は引き続きユーザの明示的な確認を経てのみ実行され、
  自動隔離はしない。

### 1.7.0

- **新機能: Air-Gap発動の引き金となる3ガードのインシデント履歴を、MCP・
  CLI両方に露出**。Air-Gap（緊急ネットワーク遮断）がなぜ発動したかを、
  別プロセスから調査する手段がこれまで存在しませんでした。発動しうる
  3つのガードすべてで、実際の発動理由がdaemonのメモリ内にしかなかった
  ためです。
  - **eBPFランタイムガード**（Server Edition）: 新規MCPツール
    `get_ebpf_incidents`とCLIコマンド`roamswitch server ebpf`が、現在の
    隔離状態（隔離モード・隔離対象PID・保守用に開放中のポート）と、直近
    最大50件の隔離判断履歴（発動ルール・対象プロセス・実施した対処内容）
    を報告します。`/var/lib/roamswitch/{ebpf_status,ebpf_incidents}.json`
    へ永続化されるようになりました。
  - **Port Anomaly Guard**: 新規MCPツール`get_port_anomaly_incidents`と
    CLIコマンド`roamswitch port-anomaly`が、ベースライン取得状況・現在
    自動遮断中のポート・直近最大50件の検知インシデント（未知の実行ファ
    イルが外部公開ポートで待ち受けを開始した記録）を報告します。既存の
    `/var/lib/roamswitch/port_guard.json`へ永続化されるようになりました。
  - **ランサムウェア・カナリア**: `get_canary_status`の`recent_incidents`
    が常に空になっていたバグを修正しました（MCPハンドラが呼び出しのた
    びに検知エンジンを新規作成しており、メモリ内のインシデント履歴が毎
    回破棄されていました）。`/var/lib/roamswitch/canary_incidents.json`
    への永続化により実際の検知結果を反映するようになりました。
    `roamswitch canary`（コード自体は変更なし）もこの修正の恩恵を自動的
    に受けます。
  - ネットワーク遮断下でのローカルLLM＋MCPによるオフライン・トリアージ
    において、これら3つの主要な発動理由がいずれもdaemonプロセスの外か
    ら確認できないという実質的な穴を解消しました。
- **SDK**: `roamswitchkit`（モノレポ内）と公開クレート
  `roamswitch-linux-kit`の両方に`get_port_anomaly_incidents()`・
  `get_ebpf_incidents()`を追加。`canary_status()`は上記バグ修正により
  自動的に実データを返すようになりました。
- **修正**: CLI自身の`--help`末尾と`roamswitch(1)` manページの
  `SEE ALSO`が、実在しないヘッドレス運用ガイドのURL（そもそも存在した
  ことのないファイルを、しかも誤ったリポジトリ内で参照）を案内していま
  した。両方とも<https://lafine.net/linux-cli.html>を指すよう修正しま
  した。
- **ドキュメント**: `roamswitch(1)` manページに、CLIには既に実装済み
  だった複数のサブコマンド（`server`とその全サブコマンド・`fim`・
  `emergency-restore`・`scan-vulns`・`scan-packages`）の記載が漏れてい
  たため追加しました。あわせて、これらが読み書きするサーバー側ファイル
  （`server.conf`・`fim_baseline.db`・新規のインシデント履歴JSON群）も
  記載しました。

### 1.6.0

- **新機能: ログ監査のテンプレート異常検知 + シークレット自動マスキング**。
  `roamswitch audit-logs`/MCPツール`audit_security_logs`が、ログメッセー
  ジをテンプレート化（IP・16進/ハッシュ文字列・数値をマスク）し、「過去
  に見たことのない新規パターン」と「頻度異常（Z-score > 3.0の統計的スパ
  イク）」の2種類の異常を検知するようになりました。既知テンプレートの
  ベースラインは`~/.config/roamswitch/log_template_baseline.json`に永続
  化されます（テンプレート識別子のみ、時系列データは保持しません）。
  - **セキュリティ修正**: ログ行にたまたま含まれていたAPIキー・トーク
    ン・秘密鍵ヘッダー等が、GUI表示・「AIに相談する材料をコピー」・そ
    して特に**MCP経由でAIエージェント等が受け取る生のJSON**に渡る前に
    マスキングされるようになりました。従来はAI相談用に組み立てた文章
    のみをマスクしており、MCP呼び出し元に渡る元データ自体はマスクされ
    ていませんでした。
  - 「AIに相談する材料をコピー」のプロンプトは、外部AIチャットに貼り付
    けるためのものでありアプリ自体の表示言語とは無関係であるべきとい
    う判断から、日本語ハードコードから英語ハードコードに変更しました。
- **新機能: Server EditionのLog Audit通知**。軽量な`journalctl`定期スキ
  ャン（`log_audit_enabled`、既定ON。`log_audit_interval_secs`、既定
  1800秒）により、新規パターンや頻度異常を検知した際にFile Scan Guard・
  FIMと同じ通知チャネル（Telegram/LINE/Webhook）へ通知します。
- **新規MCPツール: `verify_fim`**。約150箇所のクリティカルパスファイル
  整合性検証（FIM）は従来CLI専用（`roamswitch fim verify`）でしたが、読
  み取り専用でMCP・Rust SDKからも呼び出せるようになりました。AIエージェ
  ントや外部ツールがシェルアウトせずにファイル改ざん検知状況を確認でき
  ます。
- **SDK: 既存の3つのMCPツールにクライアントラッパーが無かった欠落を解
  消**。`roamswitchkit`（モノレポ内）と公開クレート`roamswitch-linux-kit`
  の両方に`package_cve_scan()`・`package_cve_scan_languages()`・
  `verify_fim()`を追加。これらのツール自体は`roamswitch-mcp`に既に実装
  されていましたが、自前でJSON-RPCを組まない限り外部から呼び出す手段が
  ありませんでした。

### 1.5.4

- **`clamd`が使っていなくても常時1GB超のRAMを消費し続けていた問題を修正
  し、opt-in化**。ClamAV連携(clamdscan/clamscan)を一度も使わなくても、
  パッケージが`clamav-daemon`を推奨依存として引き込み、ディストリの
  postinstが問答無用でsystemdサービスを常時起動させるため、インストール
  しただけで実測1.1GBのRAMが常時消費され続けていた。Server Editionには
  既にこの問題を解決済みの仕組み(`clamav_enabled=false`の間は`clamd`を
  mask+stopで待機させ、`true`にした瞬間だけ`freshclam`実行→unmask→
  起動する自己修復ロジック)があったため、共有関数として切り出しデスク
  トップ版にも移植。新設定`clamav_enabled`(既定`true`、従来動作を維持)
  をGUIのチェックボックスから切り替え可能にし、無効化すると`clamd`は
  停止してメモリを解放(組み込みYARAエンジンによる一次防御は継続)。
  トグルは再起動不要で即座に反映される。なお、USBストレージガードは
  ClamAVのみに依存しYARAの一次防御を持たないため、無効化時はUSB挿入時
  のスキャン自体が完全にスキップされる点は既知のトレードオフとして開示。

### 1.5.3

- **重要: USB Zero-Trust有効時、マウス等キーボード以外の新規USBデバイスが
  永久に使えなくなる不具合を修正**。`authorized_default=0`(USB
  Zero-Trust)を有効にすると新規USBデバイスはカーネルレベルで未承認の
  まま接続されるが、監視ループが自動的に再承認していたのはUSBハブと
  許可リスト登録済みのストレージだけだった。マウス・Webカメラ・
  ヘッドセットなど、それ以外の全デバイスクラスには承認を得る手段が
  一切なく、挿した瞬間から使えなくなっていた。ストレージ以外は
  常に自動承認する方針に一般化(キーボードはEVIOCGRABによる
  ソフトウェア側の防御のみで、カーネル認可には引き続き関与しない)。
- **RHEL既知CVEマップが実際には一度も配信できていなかった不具合を修正**。
  対象パッケージ×CVEの組み合わせが約13万件に達し平文JSONで約130MBと
  GitHubの単一ファイル100MB上限を超えるため、生成のたびpushが失敗して
  いた。gzip配信(約3.4MBに圧縮)に変更し、`roamswitch-updater`は署名
  検証をgzipバイトのまま行い、検証後にのみ展開してインストール先には
  従来通り平文JSONを書き込む。あわせて、マップ生成時のzstd展開を
  ディスクへ一切書き出さないストリーミング方式に変更し、GitHub
  Actionsランナーのディスク容量不足によるログなしクラッシュを解消。
  RHEL・CentOS Stream・AlmaLinux・Rocky Linuxでパッケージ照合機能に
  実データが届くのは今回が初めて。

### 1.5.2

- **重要: パッケージCVE照合・実証型脆弱性診断のCVEマップが、公開当日は
  常に「未取得」扱いになる不具合を修正**。組み込みシードのバージョン
  表記が「日付-seed」形式で、配信データのバージョン（日付のみ）と
  文字列比較した際、公開日当日に限り誤ってシード側（空データ）が
  優先されてしまっていた。シードのバージョンを空文字に変更し、
  実データが常に優先されるように修正。
- **依存パッケージCVE照合（npm/PyPI/crates.io等）で、データ未取得なのに
  「見つかりませんでした」と誤表示していた不具合を修正**: 既知CVEマップが
  未取得の状態でも、OSパッケージ照合タブと同様に「データ未取得」と正しく
  区別して表示するように。
- **監視フォルダの「削除」ボタンの表記を修正**: 実際にはRoamSwitchの
  監視リストから外すだけでフォルダ自体は削除しないため、「一覧から削除」
  という誤解のない表記に変更（パッケージCVE照合・マルウェアスキャン
  監視フォルダの両タブ）。
- **openSUSE向けパッケージCVEマップが一度も配信できていなかった不具合を
  修正**: マップ生成時に1つのパッチ定義内の全パッケージ×全CVEを無条件に
  紐付けていたため、大規模な更新パッチで組み合わせが爆発し226MBのファイル
  が生成され、GitHubの単一ファイル100MB上限で配信そのものが失敗し続けて
  いた。対応関係が多すぎる場合は安全側に倒して紐付けをスキップするよう
  修正。

### 1.5.1

- **重要: インストール直後にネット接続が切れる不具合を修正**。Link Guard
  (フィッシング対策のHTTP/HTTPS/DNS通信検査機能)のnftルールが、確立済み
  コネクションの全パケットを無期限にNFQUEUEへ転送する設計になっており、
  実際の通信量(ページ閲覧・動画・ダウンロード等)の下で単一スレッドの
  キュー処理が詰まり、ネット接続断・ガードのクラッシュを引き起こす場合が
  あった。`ct original packets`によるカーネル側フィルタで、ホスト名判定に
  必要な先頭数パケットのみをキューイングするよう修正。あわせて、脅威
  フィード(84万件超のドメインリスト)の再読込をパケット処理スレッドから
  専用スレッドへ分離し、キュー処理ループが異常終了しても自動的に復旧
  (再起動)するように変更した。
- **USB Zero-Trustの3秒毎の再設定に冪等性チェックを追加**。有効/無効の
  状態がすでに一致している場合でも毎回sysfsへの書き込みとログ出力を
  行っていた無駄を解消。

### 1.5.0

- **パッケージCVE照合機能を新規追加**: インストール済みのOSパッケージと、
  開発中プロジェクトの依存関係を、ローカルに保持した既知CVEマップと
  突き合わせる新機能。ネットワーク接続は一切行わない（組み込みの
  ベースラインは意図的に空で、`roamswitch-updater`の日次配信で実データを
  取得するまでは何も検出しない）。
  - **OSパッケージ**: `dpkg`（Debian/Ubuntu）・`pacman`（Arch Linux）・
    `dnf`（RHEL・CentOS Stream・AlmaLinux・Rocky Linux。Fedoraは
    Red Hat自身のセキュリティデータが対象外としているため非対応）・
    `zypper`（openSUSE Leap。SLESは有料フィードのため非対応）を
    自動判定して列挙。
  - **言語エコシステム7種**（開発者向け・任意）: npm・PyPI・crates.io・
    RubyGems・Packagist・Go・Mavenの依存関係ロックファイル
    （package-lock.json・requirements.txt・Pipfile.lock・poetry.lock・
    Cargo.lock・Gemfile.lock・composer.lock・go.sum・pom.xml）を、
    指定したプロジェクトフォルダから解析。
  - CLI: `roamswitch scan-packages [FOLDER...]`。MCPツール:
    `run_package_cve_scan`・`run_package_cve_scan_languages`。GTK GUIに
    新規タブ「📦 パッケージCVE照合」を追加。
  - CVEデータが未取得の場合、スキャン実行時にバックグラウンドで
    日次配信の取得を試行するよう改善（次回スキャンや24時間後の定時
    実行を待たずに済む）。
- **内蔵ヘルプ（`get_app_help`）の不具合を修正**: `topic: "setting"`
  （設定ガイド）で絞り込むと、該当データが存在せず常に空振りしていた。
  実在するconfig.jsonの設定項目に基づいたガイドを追加し、対応するMCP
  リソース `roamswitch://docs/settings-guide` も新規登録。

### 1.4.3

- **機密情報・APIキー漏洩スキャナ: デスクトップGUIにフォルダスキャンを追加**:
  GTK版「機密情報・APIキー漏洩監査」タブ（従来テキスト・クリップボード
  のみ）に「フォルダを選択してスキャン」ボタンを追加。下記のCLI/MCP/SDK
  向け機能とGUIの機能差を解消。
- **Dockerファイアウォール迂回対策（DOCKER-USER）を修正しデスクトップ版にも
  移植**: Server Edition の対策には実は拒否ルールが存在せず、しかもDNAT後の
  ポート番号と誤って照合していたため、実質何も遮断していなかった。外部
  インターフェース限定の拒否ルールとDNAT前ポートでの照合に修正し、同じ
  対策をデスクトップ版にも追加（従来Server Edition限定）。セキュリティ
  健康診断の新項目として追加（Server Editionは27→28項目に増加）。
- **リスクのあるDockerコンテナのリアルタイム検知を追加**: `docker events`を
  監視し、`--privileged`起動や`/var/run/docker.sock`のマウント（コンテナ
  脱出リスク）を検知した瞬間に通知する新ガードを追加。デスクトップ版・
  Server Edition双方で動作。リスクの高い「設定」であって確認された侵害
  ではないため、自動遮断は行わず通知のみ。
- **Server Edition向けにFile Scan Guardを新設**（オプトイン）: 組み込みYARA
  エンジンが外部依存無しで設定ディレクトリ（メール中継・共有フォルダ・
  アップロード先など）を常時スキャンし、`clamav_enabled`を有効にすると
  ClamAVによるセカンドオピニオンも追加。確認された脅威は隔離され運用者に
  通知。`roamswitch server config`、対話式`roamswitch server setup`
  ウィザード、新設の`get_file_scan_guard_status` MCPツールから設定可能。
  この機能の回帰テスト作成中に発覚したサンドボックス不具合も修正:
  systemdサービスの`ProtectSystem=strict`がClamAV自身のディレクトリへの
  書き込み権限を許可しておらず、daemon経由で起動した`freshclam`/
  `clamdscan`が常に失敗していた。
- **機密情報・APIキー漏洩スキャナがディレクトリ全体をスキャン可能に**:
  `roamswitch audit-secrets <ディレクトリ>`でフォルダ（gitチェックアウト等）
  を再帰的にスキャン。`.git`/`node_modules`/`target`/`vendor`/`dist`/
  `build`/`__pycache__`/`venv`および2MB超・バイナリファイルは除外。同じ
  機能をAIエージェント向けに`audit_secrets` MCPツールの新しい`path`
  パラメータとして、Python SDK向けに`audit_secrets_directory()`として
  提供——いずれもローカルで完結し、内容が外部送信されることはない。
- **`get_quarantine_status` MCPツールがServer Editionに対応**: 新しい
  `isServer`パラメータでServer Editionの隔離Vault
  （`/var/lib/roamswitch/quarantine`）を参照できるように（従来は常に
  デスクトップ版の個人用Vaultを参照していた）。
- **修正: Dockerリスク検知の通知が言語設定を無視していた問題** —
  UI言語設定に関わらず常に日本語で表示されていた。デスクトップ版の通知は
  対応10言語すべてで正しく表示されるよう修正。Server Editionの通知は
  日本語・英語のみの方針のまま。
- **ダイアログの表示不具合を修正**: 一部の表示環境で、セキュリティ警告
  ダイアログの背後に黒い矩形のウィンドウが残り、ダイアログを閉じても
  消えないことがあった。

### 1.4.1

- **コンテナ隔離ポスチャ監査を追加**: Dockerソケットマウント露出・特権コンテナ
  監査をクライアント版にも追加（従来サーバー版限定）。コンテナランタイム種別
  （`runc`/gVisor/Kata Containers）を検出し、ホストカーネル共有によるコンテナ
  脱出リスクを両エディションで可視化。稼働中カーネルの既知CVE露出チェックを
  追加（クライアント版は既定で有効、Server Edition は下記オプトイン設定に連動）。
- **Server Edition: CVEデータ自動更新の明示的オプトインを新設**: 既定の
  「ゼロネットワークコード」方針を維持したまま、運用者が `cve_kernel_map_updates_enabled`
  （既定 false）を明示的に有効化した場合のみ、唯一の例外として1日1回
  lafine.net からコンテナ隔離監査用カーネルCVEデータを匿名取得（クエリ文字列・
  Cookie・識別ヘッダなし、署名検証済み）。`roamswitch server config` /
  `roamswitch server setup`（対話式ウィザード）から確認・変更可能。
- **ランサムウェア凍結通知の不具合修正**: 短命プロセスがバースト検知の評価前に
  終了していた場合、通知にプロセス名の代わりに紛らわしいプレースホルダーが
  表示されていた問題を修正。プロセス名を書き込み検知の最初の時点でスナップ
  ショットするように変更し、特定できない場合は正直に「不明」と表示。

### 1.1.1 - 1.3.2

- **Server Edition の設定・ドキュメント全面整合**: 運用マニュアルに記載された
  設定項目をすべて実際に機能するように修正 — Docker バイパス防止のON/OFF、
  SSH保守経路温存のON/OFF、`isolate`/`freeze`/`alert_only` の封じ込めモード、
  eBPFソケットパスの変更に加え、重大度ごとに動作を細かく指定できる新しい
  任意設定ファイル `guard.yaml`（同一インシデントの再発でホスト隔離へ自動
  エスカレーション、確認応答が無ければ自動復帰するセーフティタイマー付き）
  を追加。
- **イベント駆動のファイル改ざん監視 (FIM)**: 監視対象ファイルへの書き込み
  完了の瞬間に改ざんを検知するように（従来は周期スキャン待ちで最大数分の
  遅延）。Fedora/RHEL/openSUSE でも Debian/Ubuntu と同様、パッケージ更新後の
  FIM ベースライン自動更新に対応。
- **サーバー版の Egress / C2 遮断**: ローカルの悪性IPブロックリストと任意の
  DNSシンクホール（社内DNS環境を壊さないよう既定OFF）を追加。ランサムウェア・
  カナリアと恒常的カーネル堅牢化もサーバー版で稼働するように。
- **Tetragon センサー対応**: eBPF ランタイムガードが Falco に加えて Cilium の
  Tetragon（ネイティブ gRPC、ローカル UNIX ソケット限定・新規リッスンポート
  無し）にも対応（クライアント・サーバー両版）。
- **自律封じ込めの安全性強化**: プロセスを凍結・強制終了しうる全経路に、
  基盤デーモン（init・コンテナランタイム・パッケージマネージャー・
  ブラウザ等）を除外する共通の保護リストを適用。プロセス単位のネットワーク
  隔離も、何にも一致しない不完全なルールを使っていた不具合を修正し、実際の
  UID またはコンテナを正しく指定する方式に改めた。
- **クライアント版: Air-Gap 復旧の安全性向上と適応型検知感度**:
  「解除」操作は、該当プロセスが判明している場合はネットワーク遮断を解く前に
  そのプロセスを終了するように変更（従来は遮断解除のみ）。eBPF の検知感度も、
  未信頼ネットワークでは自動的に強化し、信頼ネットワークに戻ると緩和するように。
- **診断・通知の整理**: セキュリティ診断表示の「CVE-2026-53362」表記を
  「Frag Gap」のみに簡潔化。起動直後、既知のネットワークに接続しただけでは
  「保護プロファイル切替」通知が出ないように調整（実際のローミング時は
  従来どおり通知）。

### 1.1.0

- **RoamSwitch Server Edition（サーバー・クラウド版）の正式導入**:
  - Falco eBPF ランタイム脅威検知と自動隔離（UNIXドメインソケット `/var/run/roamswitch/events.sock` 連携）。
  - 重要パス改ざん検知（FIM: Critical Path File Integrity Monitoring, SHA-256）と自動リストア。
  - サーバー専用25項目セキュリティ・ヘルスチェック（カーネルパラメータ、Docker特権コンテナ/ソケット露出、eBPF LSM、SYN cookies、空パスワードアカウント等）。
  - Webhook（Slack / Discord / Teams / Generic）および PagerDuty によるリアルタイム障害・脅威通知。
- **MCP サーバー（`roamswitch-mcp`）の Server Edition 対応**:
  - `get_security_report` ツールに `isServer: true` 引数を追加。Claude や AI エージェントから 25 項目のサーバーセキュリティ診断結果を直接取得可能に。
- **SDK（`roamswitchkit` / Python `roamswitch`）のサーバー診断メソッド追加**:
  - Rust SDK: `client.server_security_report().await?` を追加。
  - Python SDK: `client.server_security_report()` および `client.security_report(is_server=True)` を追加。
- **CLI サーバー管理機能の拡充**:
  - `roamswitch status --server`、`roamswitch server [config|setup|test-notify|restart]`、`roamswitch fim [verify|update]`、`roamswitch emergency-restore` コマンドを追加。

### 1.0.63 - 1.0.66

- **アップデート体験の刷新**: 進行状況スピナー付きの手動「アップデートを確認」
  ボタンを追加。アプリ本体の更新と脅威フィード更新のステータスを1枚のカードに
  統合し、実際に新バージョンがある時だけアップグレードボタンを表示。パッケージ
  更新後はアプリが自動的に再起動。
- **マルチディストリビューション対応**: Fedora/RHEL（`dnf`）、openSUSE
  （`zypper`）、Arch Linux（`pacman`/AUR）のアップグレード手順を Debian/Ubuntu
  （`apt`）と同様にネイティブ表示。全10言語に対応し、確認時刻も表示。

### 1.0.50 - 1.0.62

- **デーモンIPCソケット認証保護とVault堅牢化**: `SO_PEERCRED` による実ユーザー/root限定アクセス制御、シンボリックリンク解決・プレフィックス検証による任意ファイル削除・特権昇格防止、WireGuard引数サニタイズ。
- **セキュリティ診断の判定基準適正化とSBC最適化**: 無人自動更新（`unattended-upgrades` / `dnf-automatic`）の実効性検証、Raspberry Pi等のSBCにおけるUEFI/tmpfs/LSMの検出・誤判定解消、SSH分割設定監査。
- **UIと更新フローの刷新**: 言語・DNS設定のプルダウン（`ComboBoxText`）化、1100pxウィンドウ幅最適化、「アップグレード」タブへの統合とロケール依存解消。
- **無線復旧とAir-Gap耐性の強化**: 物理無線（Wi-Fi/Bluetooth）復旧パイプライン強化（rfkill/NetworkManager/BlueZ）、Air-Gap発動時の脅威情報可視化・復旧処理、ARPスプーフィング攻撃終了後の自動復旧、UI負荷軽減。

### 1.0.41 - 1.0.49

- **ARPスプーフィング検知・復旧の抜本的改修**: 空間的（同一MACの複数IP応答）および時間的差分チェックを組み合わせた検知ロジックの導入、スプーフィング継続時のAir-Gap解除定着、未確認ゲートウェイMACの誤固定防止、balanced/信頼ネットワークでの遮断確認ダイアログの追加。
- **個別強化とセキュリティ診断の適正化**: 診断タブへの項目別「個別強化」ボタン追加、信頼ネットワークでの仕様（noexecやARP固定）を踏まえた判定と解説の適正化、fanotify実稼働チェックの厳格化。
- **UIおよびIPC応答性の向上**: セキュリティ確認ダイアログの最前面表示制御、デーモンIPCの非同期化によるメインウィンドウのフリーズ防止。

### 1.0.31 - 1.0.40

- **受動リンク保護の強化**: 警告モードのfail-closed（安全側遮断）化、保留時間短縮（8秒）、通知集約とダイアログ改善。
- **マルウェア・ランサムウェア誤検知防止**: パッケージマネージャー（apt/dpkg/rpm等）、ブラウザキャッシュ、EICARテストファイルの誤検知・誤隔離を防止。除外パス設定の追加。
- **VPN / Tailscale連携の最適化**: Tailscale Exit Node設定とキルスイッチ順序の最適化、UI余白・ドロップダウン幅の調整。
- **多言語対応の拡充**: 全10言語（日・英・簡・繁・韓・独・仏・西・伊・葡）への完全対応。

### 1.0.21 - 1.0.30

- **UIスレッド完全非同期化**: 手動保護レベル切替およびVPN接続時のUIフリーズを解消。
- **デスクトップ環境への適合**: GNOME / KDE / XFCE / Wayland各環境でのタスクバー・システムトレイアイコンのリアルタイム動的切り替え。
- **Tailscale Exit Node / WireGuardの安定性強化**: Snap版Tailscale検出、引数解決フォールバック、バックエンド切替UIの整理。

### 1.0.11 - 1.0.20

- **VPNトンネルとキルスイッチの導入**: 未信頼ネットワーク接続時の自動WireGuard / Tailscaleトンネル接続と通信遮断キルスイッチ機能の追加。
- **検知しきい値とシステム連携の最適化**: ランサムウェア暗号化検知しきい値の適正化（5秒20ファイル/エントロピー7.92以上）、Raspberry Pi OS等でのアイコン・依存関係修正、予防的ゲートウェイARP固定。

### 1.0.1 - 1.0.10

- **主要防衛機能の追加**: 未知ポート自動遮断（ポート異常ガード）、受動リンク保護、日次脅威フィードアップデータ、BadUSBキーボード・USBストレージ承認ダイアログ、共有サービス（SSH/Samba等）の自動停止・復元。
- **初期セットアップと公式リポジトリ**: 初回セットアップウィザード、apt / dnf / zypper公式リポジトリ（GPG署名）の開設。

### 1.0.0

- Linux版初回リリース。macOS版のゼロトラスト思想をLinux（systemd + nftables）へ移植。接続先Wi-Fi/有線の自律識別、ランサムウェア挙動検知・緊急Air-Gap隔離、20項目セキュリティ診断、MCPサーバー搭載。


---

## RoamSwitch for Mac

## 1.9.5 (未リリース)

- **新機能: Wi-Fi履歴学習によるなりすまし(Evil Twin)SSID警告**: 過去に
  接続したWi-Fiの{SSID, ゲートウェイMAC}を学習し、未知のSSIDが登録済み
  ネットワーク名と編集距離的に酷似している場合(例: "Airport_WiFi" vs
  "Airport_WlFi")に警告するようになった。
- **改善: ARPスプーフィング検知の誤検知を削減(正当なローミングの判定)**:
  ゲートウェイIPは同じだがMACアドレスが変わった場合でも、SSIDが同時に
  切り替わっていれば正当なネットワーク間ローミングとして扱うように
  なった。多くのルーターが192.168.1.1等の同一プライベートIPを使い回す
  ことによる偽陽性を削減。
- **新機能: BadUSBキーストローク時系列の異常検知(補助シグナル)**: 未承認
  として遮断中のキーボードからの入力を打鍵間隔で分析し、スクリプトに
  よる自動入力(Rubber Ducky等のBadUSB攻撃デバイスに特徴的なパターン)
  らしさを検知した場合、通知文言に注意喚起を追加するようになった。既存
  の遮断・承認フロー自体には一切影響しない補助的な情報。
- **新機能: Link Guardの通信分類にJA3 TLSフィンガープリント照合を追加**
  (基盤のみ): 既知マルウェアのJA3フィンガープリント(TLS ClientHelloから
  算出するハッシュ値)と照合する仕組みを追加。現時点ではフィード配信の
  実データは投入していないため、実際の検知が発生することはない。
- **改善: ログ監査のテンプレート異常検知で、Homebrew等のパッケージ更新に
  伴う大量の正常なGatekeeper評価ログを異常として誤検知しないよう除外**
  (Linux版との機能パリティ)。
- **重要: 特定条件下でMCPサーバーがクラッシュする不具合を修正**: 1.9.3で
  追加したローカライズ文字列のキャッシュ機構が、バックグラウンドキュー
  からの並行アクセスに対して未対応だった。`get_exposed_ports`等の一部
  MCPツール呼び出しで、複数スレッドが同時にキャッシュへ書き込むことで
  内部データが破損しクラッシュすることがあった。排他制御を追加して修正。
- **改善: ダウンロードガードの静的シグネチャ検知にClamAVの裏付け検査を
  追加**: `StaticSignatureScanner`(自前の軽量ヒューリスティック)がダウ
  ンロードファイルをThreat判定した際、これまでは判定と同時に隔離し、
  ユーザには内部の検出名しか通知していなかった。隔離前にClamAVでも同じ
  ファイルを検査するようになり、一致すればその検出名を、不一致なら
  「誤検知の可能性があるため隔離Vaultで内容を確認し復元できる」旨を通知
  本文に追記するようにした。ClamAVが不一致の場合はアラート音も緊急
  （defaultCritical）から通常（default）へ軽減。隔離自体の実行有無や、
  誤検知時に復元できる既存の設計は変更していない。

## 1.9.4

- **修正: ランサムウェア防護のAir-Gap隔離を解除するたびに、デコイファイルを
  一切改ざんしていないのに誤検知が繰り返し発生し、ネットワーク遮断が何度も
  誤発動することがある不具合を修正**: 隔離解除時の自己修復処理(デコイ
  ファイルの再生成→リアルタイム監視ハンドルの再構築)のタイミング次第で、
  実際には改ざんされていないファイルに対して偽の検知イベントが発生する
  ことがあった。監視ハンドルを再構築した直後の短い猶予期間は検知を無視
  するようにし、この誤検知ループを防止した。

## 1.9.3

- **新機能: Air-Gap発動の引き金となる3ガード(Ransomware Canary/Port Anomaly/
  Runtime Threat Containment)のインシデント履歴をMCPに露出**: Air-Gap(緊急
  ネットワーク遮断)がなぜ発動したかを、メインアプリと別プロセスのMCPサーバー
  から確認する手段がこれまで存在しなかった。既存の`get_canary_status`に
  `recentIncidents`を追加、新規MCPツール`get_port_anomaly_incidents`・
  `get_runtime_threat_status`を追加、`get_guard_status`のガード一覧に
  `runtimeThreatContainment`を追加。各インシデントはアプリ本体の共有
  UserDefaultsへ永続化されるため、メインアプリを経由せずMCP単体でも直近の
  検知内容を確認できる。
- **修正: 上記の開発中に見つかったバグ、Runtime Threat Containmentのシミュ
  レーション実行時に検知インシデントの詳細(`lastIncident`)がMCPレスポンス
  に反映されないことがあった**: 実際のXProtect検知経路は発動前に個別に
  インシデント情報を記録していたが、動作確認用のシミュレーション経路は
  この記録処理を経由しておらず、発動日時は記録されても検知内容の詳細が
  欠落していた。すべての発動経路で一貫してインシデント情報が記録される
  よう修正。

## 1.9.2

- **重要: 「AIに相談する材料をコピー」とMCP経由のログ監査結果で、シークレット
  （APIキー等）のマスキングが適用されていなかった問題を修正**: 「Macセキュ
  リティログ監査」機能には元々シークレットを検知してマスクする仕組み
  （`SecretLeakScanning`）があったが、クリップボードにコピーする経路と
  MCPサーバーが返すJSONレスポンスの両方でこの仕組みが未使用のままだった。
  生のシステムログにAPIキー等の機密情報が偶然含まれていた場合、外部AI
  チャットへの貼り付けやMCP経由でのAIエージェント連携時にそのまま渡って
  しまう可能性があった。ログ監査が抽出する全メッセージにマスキングを
  適用するよう修正し、クリップボードコピー・MCPレスポンスの両方に一貫
  して効くようにした。
- **ログのテンプレート異常検知を新規追加**: 「Macセキュリティログ監査」に、
  既存の6カテゴリ（sudo/ssh/gatekeeper/xprotect等）のハードコード文字列
  マッチに加え、ログメッセージをテンプレート化した上での頻度急増・未知
  パターン検知を追加（Linux版で先行実装済みの機能を移植）。KPIカードと
  AI相談用テキスト、MCPレスポンス（`templateAnomalies`）に反映される。
- **MCPの`get_security_report`レスポンスに監査実行時刻（`timestamp`）を追加**。
- **不正USB / BadUSBガード: 有効化した時点で既に接続されている外部キー
  ボードが許可リストに登録されない不具合を修正**: 外部キーボードを挿した
  ままガードを有効化すると、そのキーボードは自動承認されず「たまたま今は
  弾かれていない」だけの状態になっていた。次に抜き差しした際、普段使って
  いる本人のキーボードにもかかわらず未承認デバイスとして扱われ、承認
  ダイアログが表示されキーストロークが一時的に遮断されてしまう不具合が
  あった。ガード有効化時に現在接続中のキーボードを自動的に許可リストへ
  登録するよう修正。
- **「Macセキュリティログ監査」でカテゴリフィルタを「すべて」にした際、
  イベント件数が多いとアプリがフリーズする不具合を修正**: 表示件数に上限
  を設け、ローカライズ文字列の内部処理を効率化した。

## 1.9.1

- **重要: パッケージCVE照合・実証型脆弱性診断のCVEマップが、公開当日は
  常に「未取得」扱いになる不具合を修正**（Linux版1.5.2と対）。組み込み
  シードのバージョン表記が「日付-seed」形式で、配信データのバージョン
  （日付のみ）と文字列比較した際、公開日当日に限り誤ってシード側
  （空データ）が優先されてしまっていた。シードのバージョンを空文字に
  変更し、実データが常に優先されるように修正。
- **パッケージCVE照合の概要欄で、CVE説明文に含まれるMarkdown記法
  （見出し・太字・コードスパン）がそのまま表示されていた不具合を修正**:
  表示時にプレーンテキスト化するように。

## 1.9.0

- **実証型脆弱性診断を新規追加（既定オフ）**: Linux版で先行実装済みの機能を
  移植。ポートスキャンで見つかった`127.0.0.1`上のサービスに対し、無害な
  読み取り専用プローブを1回だけ送り、「ポートが開いている」以上の
  「実際に認証なしで到達できるか」を確認する。対象はRedis・Memcached・
  MongoDB・dockerd（既知の危険サービス）、および任意の開発用サーバーへの
  CORS設定ミス・パストラバーサル・オープンリダイレクト検知。Redis
  （CVE-2022-24834ほか計8件）・Memcached（CVE-2018-1000115）は既知CVEの
  該当バージョンかどうかも無害なバージョン確認コマンドのみで判定。
  攻撃ペイロードは一切送信しない。メニューバーの「ポート」からオプトイン
  し、ポート詳細シートから手動実行する。既知CVEマップはランサムウェア
  カーネルCVEマップと同じ「1日1回・受信専用・署名検証済み」の配信で
  更新される。
- **パッケージCVE照合機能を新規追加**: Linux版と同じ設計をmacOSに移植。
  Homebrewのインストール済みパッケージ、およびnpm・PyPI・crates.io・
  RubyGems・Packagist・Go・Mavenの依存関係ロックファイルを、ローカルに
  保持した既知CVEマップと突き合わせる。ネットワーク接続は一切行わず、
  データが未取得の場合はスキャン実行時にバックグラウンドで取得を試行する。
- **MCPサーバーの機能欠落4件を追加**: AIエージェント向けMCPサーバーに、
  デスクトップGUIには既に存在していた機密情報漏洩スキャン
  （`audit_secrets`）・セキュリティログ監査（`audit_security_logs`）・
  隔離ファイル一覧（`get_quarantine_status`）・ランサムウェアおとり
  ファイル監視状況（`get_canary_status`）を追加。あわせて、MCPサーバーの
  バージョン応答が常に「1.0.0」固定になっていた不具合と、アプリ本体で
  選択した表示言語がMCPサーバー側の応答には反映されず常に日本語に
  なっていた不具合を修正。
- **Pro既定値の不整合を2件修正**: ランサムウェアおとりファイル監視を
  他の自動遮断系ガードと異なりOFFに設定しても再起動時に復活していた
  問題を修正し、メニューバーからON/OFFを切り替えられるようにした。また、
  ARPスプーフィング対策・XProtド連動の自動ネットワーク遮断が初回有効化時
  に無予告で発動していたため、初回のみ内容と解除方法を説明する通知を
  追加。
- **ヘルプ & ガイドに未収録だった機能を追加**: パッケージCVE照合・実証型
  脆弱性診断・ClickFix対策・永続化監視・XProtect連動の自動遮断など、
  実装済みだが「ヘルプ & ガイド」に載っていなかった項目を追記。

### 1.8.9

- **Dockerリスク検知ガードを新設（Pro、既定OFF）**: `--privileged`起動や
  `/var/run/docker.sock`のマウント（コンテナ脱出リスク）を検知した瞬間に
  通知。リスクの高い「設定」であって確認された侵害ではないため、自動的な
  対応は行わない。Linux版の同等機能と判定ロジックを一致させている。
  Dockerを使わないユーザーが大半のため、Proでも既定はOFFのまま。
- **機密情報・APIキー漏洩監査がフォルダ全体をスキャン可能に**: 従来は
  貼り付けたテキストやクリップボードのみが対象だったが、「機密情報・API
  キー漏洩監査」ウィンドウに「フォルダを選択してスキャン」を追加し、
  ディレクトリ（ソースコードのチェックアウト先など）を再帰的に監査できる
  ように。`.git`/`node_modules`/`target`/`vendor`/`dist`/`build`/
  `__pycache__`/`venv`および2MB超・バイナリファイルは除外。従来通り
  完全にローカルで完結する。
- **ダイアログの表示不具合を修正**: コンポジタが有効でない環境で、
  セキュリティ警告ダイアログの背後に黒い矩形のウィンドウが残り、
  ダイアログを閉じても消えないことがあった。
- **フォルダアクセス許可の事前説明ダイアログを追加**: Web・メール保護の
  監視開始時、および機密情報・APIキー漏洩監査のフォルダスキャン時に、
  macOSのデスクトップ/ダウンロード/書類フォルダへのアクセス許可プロンプトが
  直前の説明なしに表示され、不審なアプリと誤解されることがあったため、
  初回のみ理由とZero Telemetryである旨を説明するダイアログを追加。
- **多言語UIの翻訳漏れを一斉修正**: 一部のメニュー項目やダイアログで
  日本語のまま表示されていた翻訳漏れ92件を、対応する9言語すべてで補完。

### 1.8.8

- **Helperバージョン同期の修正**: 1.8.7で追加したSudo権限昇格監査(NOPASSWD)用の
  特権ヘルパーメソッドが、ヘルパー自身のバージョン番号更新漏れにより、
  既存インストールからのアップグレード時に反映されないことがある不具合を
  修正しました。該当する場合、セキュリティ総合診断の「Sudo 権限昇格設定」
  項目が「未確認」のまま進まなくなります（新規インストールでは発生しません）。

### 1.8.7

- **静的シグネチャによるダウンロードファイル検知**: EndpointSecurity権限を
  使わずに、業界標準のEICARテスト署名と、公開されている定番リバースシェル
  一行コマンド(bash/sh `/dev/tcp/`、netcat `-e`、Python `pty.spawn`、Perl
  `Socket`、PHP `fsockopen`)を検知する軽量な静的シグネチャ層を追加しました。
  ClamAVと並列で動作し、ClamAV未導入でも機能します。
- **新しい自動起動登録(LaunchAgent/LaunchDaemon)の監視**: ログイン時
  自動起動として新しくplistが設置された瞬間を検知し、`/bin/bash`等の
  スクリプトインタプリタを直接登録している場合は警告します(実行ファイル
  自体の署名が正当でも、悪意ある内容は多くの場合インタプリタへの引数に
  隠されるため)。
- **不審なTerminalコマンド実行時の緊急遮断(既定OFF)**: 偽の警告画面等が
  ユーザー自身にTerminalへコマンドを貼り付け・実行させる「ClickFix」型の
  攻撃を想定し、コマンド履歴に既知の不審パターンを検知した場合に緊急で
  通信を遮断する機能を追加しました。正規のインストーラーで頻繁に使われる
  `curl | bash`のような単体のパターンは誤検知を避けるため対象外とし、
  Base64デコードをシェル/AppleScriptに直接渡す等の複合パターンのみに
  限定しています。誤検知時の影響が大きいため、既定では無効です。
- **セキュリティ総合診断に3項目追加**: ゲートウェイARP固定の状態表示、
  SSHリモートログインの設定監査(root禁止・鍵認証必須になっているか)、
  sudoのパスワード省略設定(NOPASSWD)の監査を追加しました。

### 1.8.4 - 1.8.6

- **エアギャップ・フェイルセーフの安全な設計**: ヘルパープロセスが
  クラッシュしても10分のパケットフィルター全遮断タイムアウトが自律的に
  解除されるようにする改善。当初(1.8.5)は特権ヘルパー本体の
  `KeepAlive.PathState`で自律再起動させる方式で実装しましたが、まれに通常の
  「終了」操作後にアプリが再起動してしまう不具合があったため撤回し、
  ヘルパー本体の常駐設定には一切手を加えない完全に独立した監視デーモンに
  再設計しました(1.8.6)。3分ごとに単体で起動し、期限を超えていれば
  自律的にネットワークを復旧して終了するため、通常のアプリ終了操作とは
  物理的に干渉しません。タイムスタンプの判定もNTP補正・手動時計変更・
  reboot直後のいずれにも影響されないよう、単調増加時間と壁時計を
  組み合わせた方式に強化しています。
  *(Dev.to の技術ディスカッションにおける [Super Funicular 氏](https://dev.to/superfunicular)（[該当記事](https://dev.to/superfunicular/turn-an-old-android-phone-into-a-screen-off-security-camera-no-cloud-lan-only-5cll)）の洞察と提起に深く感謝いたします。)*
- **Apple XProtect検知に連動する自動Air-Gap隔離**: EndpointSecurity権限を
  使わずに、AppleのマルウェアXProtectが実際にマルウェアを検知・駆除した
  瞬間を検知条件として、通信を緊急遮断する機能を追加(Pro版、既定ON)。
  動作確認用のシミュレーションメニューも追加。
- **機密情報・APIキー漏洩の手動監査ツール**: テキストを貼り付けてAPIキー・
  トークン等の漏洩を即時監査できる機能を追加(行番号・マスク済み文字列・
  種別ごとの推奨対応を表示)。
- **リンク保護「警告」モードをfail-closedに変更**: 警告通知に応答がない場合、
  従来は接続を許可(fail-open)していましたが、Linux版と挙動を統一し、
  遮断(fail-closed、キャッシュなし・次回再確認)に変更しました。保留時間も
  25秒→8秒に短縮。
- **ローカルポート監査時のCookie分離**:
  開放ポートのHTTP監査プローブにおいて、エフェメラルな専用Cookieストレージを使用するように分離しました。ブラウザや他サービスの認証Cookieの漏洩や、監査リクエストによる意図しないセッション汚染を防止します。
- **特権ヘルパーのコード署名検証強化 (動的 audit_token 検証)**:
  `RoamSwitchHelper` への XPC 接続において、接続元プロセスの監査トークン（`audit_token`）に基づく Apple Developer ID の動的署名検証を実装しました。不正なプロセスやインジェクションされたバイナリからの特権操作要求を確実に遮断します。
- **外部バイナリ実行前のパーミッション検証 & 設定サニタイズ**:
  外部バイナリの起動前にパーミッションおよび所有者を厳格に検証し、WireGuard設定・引数インジェクションを遮断しました。

### 1.8.0 - 1.8.3

- **緊急確認ダイアログの最前面オーバーレイ化**: 脅威検知時の4種の確認ダイアログ（BadUSB、ランサムウェア、ARPスプーフィング、リンク保護保留）が他アプリや全画面の背後に隠れず最前面に表示されるよう改善。
- **セキュリティ診断の項目別修復**: 要対策項目へのワンクリック「有効化」「設定を開く」ボタンの追加と即時再診断対応。
- **マルウェア検疫の適正化**: 無害なEICARテスト文字列の誤隔離防止（通知のみ表示、Linux版と挙動統一）。
- **リンク保護のコンテンツフィルタ強化**: ネットワーク拡張機能によるDoH/DoT・SNI検査対応、前面パネルによる警告・安全側ブロック判定。
- **VPNデュアルバックエンド対応**: WireGuardに加えてTailscale Exit Node連携に対応。

### 1.7.0 - 1.7.6

- **VPNトンネル（キルスイッチ付き）の追加**: 未信頼ネットワーク接続時の自動暗号化トンネル（WireGuard）と通信遮断キルスイッチ機能の導入。
- **予防的ゲートウェイARP/NDP固定**: カフェ等の未登録ネットワーク接続時にルーターMACを固定し、MITM攻撃を未然に防止。
- **不正USB / BadUSB物理キーボード承認ガード**: 未承認キーボードの物理接続を検知し、キーストロークを一時破棄して承認ダイアログで確認。
- **受動リンク保護**: 詐欺サイト一覧とブランド偽装判定に基づくフィッシング接続の自動遮断（/etc/hosts管理連携）。
- **USBストレージ承認プロンプト**: 未知の外部ドライブを読み取り専用で保護マウントし、承認選択（読み書き/読み取り専用/取り出し）を提示。

### 1.6.0 - 1.6.4

- **カナリアおとりファイルの永続化**: ベースラインハッシュのディスク保存により、改変・削除を厳格に検知し隔離解除時の自律修復を強化。
- **隔離Vaultの安全保護強化**: 退避された脅威ファイルの実行・読み取り権限を完全に剥奪（chmod 000）。
- **未知ポート自動遮断の互換性改善**: 旧バージョン登録の自動マイグレーションと通知スパムの解消。

### 1.5.0 - 1.5.9

- **ローカルAI / LLM推論サーバー保護**: Ollama、LM Studio、Gradio、vLLM等の0.0.0.0露出検知とプロアクティブ遮断。
- **クリップボード機密保護**: APIキーや秘密鍵のクリップボード保持をローカル正規表現スキャンで検知・警告。
- **ClamAVリアルタイム検疫の網羅性向上**: .tmp拡張子やターミナル経由配置ファイルの検疫漏れ防止、重複隔離防止。
- **クラッシュ自動復旧（Watchdog）**: LaunchAgentによる自律監視と自動再起動機構の導入。

### 1.4.0 - 1.4.8

- **オープンソースMCPサーバー公開**: 読み取り専用MCPサーバーおよび検知ロジックをOSSとして公開、ナレッジベース検索ツール（get_app_help）の追加。
- **Web・メール3重保護ガード**: ダウンロード自動スキャン、DNS脅威保護、リンク安全性診断の実装。
- **特権ヘルパーのセキュリティ堅牢化**: audit_tokenによるクライアント検証とTOCTOU攻撃防止。

### 1.0.0 - 1.3.0

- **初期公開および基本機能リリース**: ゲートウェイMACアドレスによる信頼ネットワーク自律識別、ファイアウォール/共有サービス自動切替、未知ポート自動遮断、ARPスプーフィング対応、読み取り専用MCPサーバーの初搭載。
