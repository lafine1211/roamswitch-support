# 変更履歴

[English](CHANGELOG.md) | **日本語**

RoamSwitch のユーザー向け変更点。Mac 版（1.x）と
Linux 版（別系列・1.9.x）でバージョン番号は独立しています。
古い版は、範囲でまとめています。

---

## RoamSwitch Sensor

ユーザー自身の汎用機器(PC・Raspberry Pi等、x86_64/arm64)にインストール
するソフトウェア(別プロダクト)。RoamSwitch 導入済み端末（Mac / Linux
Client / Server Edition）とのペアリングコード方式(固定IP+短命コード)に
よる能動的な脆弱性監査、LAN 上の新規機器出現・なりすまし検知を行います。
現在も開発中です。現状の実装状況・動作確認方法は
<https://lafine.net/roamswitch-sensor-manual.html> を参照してください。

### 0.3.11

- **改善: 手動ペアリング画面のフィンガープリント欄の説明を分かりやすくしました**。表示コマンドに
  既に`--fingerprint`が含まれるようになったため（0.3.10）、この欄はGUI/Mac版の入力欄用であることを
  明記しました。

### 0.3.10

- **修正: 案内されるペアリングのコマンドが、そのままでは失敗しました**。TUIの `sudo roamswitch sensor pair
  --addr … --code …` には指紋が含まれておらず、Linux 1.9.90以降のクライアントは指紋なしのペアリングを
  拒否するため、案内どおりに実行すると必ず失敗していました。TLS有効時は `--fingerprint` を含めます。
  `sensor-cli issue-code` も、端末側で実行するコマンド（Sensor自身のIPと指紋つき）を表示します。

### 0.3.9

- **追加: 制御APIがTLS 1.3と証明書の指紋固定に対応しました**。これまでは、ペアリングコードや診断結果が
  LAN上で平文でした。Sensorが自己署名の証明書を持ち、RoamSwitch側がその指紋を固定します。
  ペアリング要求には指紋が署名で束ねられ、中間者は成立しません。`control.tls` は
  `off` / `optional` / `required`（既定は `optional`）です。`optional` では、対応前の
  クライアントは従来どおり平文で接続でき、その間はTUIとCLIに警告が出ます。すべてのクライアントが
  対応したら `required` に切り替えてください。`sensor-cli tls show|rotate`、`pairing show`。
- **変更: リプレイ対策**。署名つきの要求にタイムスタンプ（±300秒）と使い捨ての値を加えました。
  従来の形式は `control.allow_legacy_signatures` が有効な間だけ受け付け、使うたびに警告します。
  ペアリングの総当たりは、送信元ごと・全体でロックアウトします。要求の行の長さも64KiBに制限しました。
- **追加: TUIが、CLIとデーモンの全機能を扱えるようになりました**。設定の表示と編集（全25キー）、
  TLSの状態と証明書の更新、手動ペアリング、テスト通知、受動キャプチャ・IoT挙動・ネットワーク脅威・
  IoT勧告のイベント、collectorの管理、絞り込みつきの書き出しなど。10言語に対応し、レポートと
  書き出しも10言語で出力します。
- **追加**: 待ち受けアドレス（`control.bind_addr`）、CVEマップとNSEデータベースの取得を止める設定
  （`updates.fetch_cve_map`）、RoamSwitchが入っているのにペアリングしていない端末と、不明な端末の区別
  （collectorへは任意で、端末ごとの概要を送れます）。
- **互換性**: Mac版のRoamSwitchは、このSensorのTLS・新しい署名にまだ対応していません。Mac版とは、
  `control.tls=optional` と `allow_legacy_signatures=true`（どちらも既定）のままで、従来どおり
  ペアリングできます。Linux版は1.9.90以降でTLSに対応します。

### 0.3.8

- **修正: 低速なARPスキャンを検知できませんでした**。30秒に15個の宛先という短い窓では、
  数秒に1個ずつ探る低速なスキャンが対象外でした（2.5秒/IPで24個を探っても未検知）。
  24時間で64個の宛先に達した場合に通知する長い窓を追加しました（2.5秒/IPの走査を
  実時計で試し、64個目、158秒後に検知）。
- **変更: 実在ホスト宛ての要求は、長い窓では数えません**。ゲートウェイやDHCPサーバーは、
  実在のクライアントを大量に引くのが正常です。ARPの送信元アドレスとデバイス台帳から
  実在と分かっているホスト宛ての要求を除き、空きアドレスへの要求が多い走査だけを
  数えます。台帳に載る実在ホストだけを狙う走査は、区別できません。
- **修正: 送信元を偽装した洪水でメモリが増え続けないようにしました**。追跡する
  MACアドレス数、MACごとの宛先数、実在ホストの表に上限を設けました。

### 0.3.7

- **変更: aptとrpmのリポジトリに置くのは、最新の1版だけになりました**。過去のすべての
  版を残し続けていたため、リポジトリが肥大化していました。旧版は、リポジトリから
  取得できなくなります。

### 0.2.1 - 0.3.5

- **ネットワーク構成の把握を強化**（0.2.1〜0.2.7）: ネットワーク構成の常時把握と、
  拡張検知（ボットネット・DDoSへの参加、DNSトンネリング）を追加。`nmap -sn`
  相当の能動ARPスイープで検出漏れを解消し（自機が一覧から漏れる不具合も修正）、
  ネットワーク構成とARPイベントを、現場で判断できる情報量に作り直しました
  （更新は1時間ごと）。機器メモ機能の追加と、TUIの表示の不具合も修正しています。
- **大規模組織のセキュリティ担当者向けの監査運用機能を追加**（0.3.0）: 定期監査、
  前回との差分、通知（Webhook・syslog）、改ざん検知つきの操作ログ、エクスポート、
  保持期間、複数のSensorを集約する中央コレクター。すべてオプトインで、初期状態では
  何も外部へ送信しません。
- **CVEマップとNSEスクリプトDBの自動更新が、systemdの書き込み制限で静かに失敗して
  いた不具合を修正**（0.3.1〜0.3.2）。
- **TUIに、差分タブ・操作ログタブ・エクスポートを追加**（0.3.4）。
- **能動監査の対象を拡充**（0.3.5）: Elasticsearch、CouchDB、VNC、RDP、SMBを追加。

### 0.2.0

- **追加: IoT機器のセキュリティリスクに特化したARPイベント機能**。従来は
  MAC/IPアドレスの生の差分しか分からず現場で判断できなかったため、
  4段階で拡充しました。
  - ① IEEE OUIレジストリから検証済みのベンダー一覧を照合し、
    「カメラ」「スマートプラグ」等のカテゴリに分類。機器ごとの
    永続インベントリを追加（`sensor-cli device-inventory`）
  - ① 単一MACが短時間に多数の異なるIPへARP要求を送る挙動（LAN内偵察の
    疑い）を検知
  - ② mDNS/SSDP/DHCPの自己申告情報を読み取り、OUI単体より高精度な
    機器特定（既存の受動パケットキャプチャ基盤を拡張）
  - ③ 分類済みIoT機器からの不正通信先アクセス・管理用ポートへの接続を
    文脈付きで提示（`sensor-cli iot-events`、要パケットキャプチャ設定）
  - ④ 実在する公開CVE（Hikvision製カメラの認証バイパス・
    リモートコード実行等）に限定した検証済みの参考情報
    （`sensor-cli iot-advisories`。ネットワーク経由の定期更新は
    今回のスコープ外です）
  - いずれも検知・参考情報の提示のみで、Sensorが遮断を行うことは
    ありません。`sensor-tui`のARPイベントタブでも分類情報を表示します。

### 0.1.0 - 0.1.7

- **deb/rpmパッケージで配布**（0.1.0）。systemdサービスとして常駐します。受動的な
  LAN可視化の拡張（オプトイン）も追加しました。
- **ペアリングを、mDNS自動探索からペアリングコード方式に刷新**（0.1.3）。mDNSは
  同一LANセグメントの中でしか機能せず、常時ブロードキャストし、標準では厳格な
  認証もないためです。固定IPのSensorが発行する使い捨てコード（8文字・10分で失効）
  と、Ed25519署名で認証します。クライアントからの監査リクエストの受付と、nmap
  NSE補完診断の常時有効化も同じ版で入りました。
- **ペアリング済み端末の監査状態バッジと、最新レポートの閲覧を追加**（0.1.4）。
  手動ペアリングを廃止して、コード方式に一本化し、IPアドレスを常に表示するように
  しました。
- **不具合の修正**（0.1.5〜0.1.7）: 同じ端末に対して監査が並列に走る問題、
  `Too many open files` でデーモンがクラッシュし、進行中の監査が失われる問題
  （ファイルディスクリプタの上限を65536に）、監査結果の指摘事項から詳細説明が
  欠落していた問題。

## RoamSwitch for Linux

Linux 版（systemd + nftables）。apt / dnf / zypper で配布（GPG 署名）。
詳しくは <https://lafine.net/linux>。

### 1.10.4

- **修正: ClamAV の検知が、`clamdscan --fdpass` の失敗をフォールバックが隠していたため、常時は片方の経路でしか動いていなかった不具合**。デーモンの systemd サンドボックス内では `--fdpass` が常に「Not a regular file」で失敗していました。`--stream` を先に使い、失敗したときだけ `--fdpass` を再試行します。両方失敗した場合は「エラー」として扱い、「問題なし」にはしません。
- **修正: USB ディスクのスキャンで、`clamdscan` の終了コード2(実行失敗)まで「マルウェア」と判定して `umount -f` していた誤り**。終了コード1(検知)のときだけ取り外します。
- **追加: ClamAV の検知の自己診断と、fanotify のイベント溢れの診断(診断項目 Client 24→26、Server 30→32、10言語)**。EICAR テストファイルで、起動120秒後と6時間ごとに、検知経路が実際に動くことを確認します。
- **修正: DNS の強制が、3秒ごとに `resolvectl`(`dns`・`default-route`・`flush-caches`)を実行していた不具合**。定常状態でも毎回 DNS キャッシュが破棄され、名前解決が遅くなっていました。状態が変わったときだけ実行します。
- **改善: Docker の保護(`DOCKER-USER`)を、60秒ごとに自己修復します(IPv6 と native nftables を含む)**。デスクトップ版でも、起動時の1回だけではなくなりました。
- **改善: Link Guard**。パケット待ちのスリープを `poll(2)` に置き換え、キュー長を4096にし、TCP のシーケンス番号で ClientHello を再構成します(最大8192バイト)。ブロックモードでは、UDP/443(QUIC)を遮断して TCP にフォールバックさせるオプション(`linkGuard.blockQuic`、既定オフ)を追加しました。
- **改善: fanotify のオンアクセススキャン**。許可イベントは実行(`FAN_OPEN_EXEC_PERM`)のみで、`open` は通知にしました(`fanotify_open_perm` で従来の動作)。スキャン済みの実行ファイルは60秒キャッシュし、デーモン自身の子プロセスは除外します。`/home` が `/` と同じファイルシステムのときは警告します。
- **変更: BadUSB ガード**。未承認のキーボードの掴みを、承認待ちの3分では解除しません(`usb_keyboard_timeout_release` で従来動作)。`/dev/input` の変化で、ホットプラグを即時に検知します。USB ディスクリプタの指紋(初回信頼)で、許可済みの VID:PID を名乗る別デバイスを検出します。
- **変更(セキュリティ): 状態を変える IPC は、root か、ローカルのログインセッションを持つユーザーだけが呼べます**。読み取り系は従来どおりです。拒否は固定のコードで返り、GUI(10言語の通知)と CLI(日英)で表示します。CLI の `airgap enable/disable` が、応答を読まずに成功と表示していた誤りも直しました。
- **改善: ハニートークン(囮ファイル)が、実際に使うツールを壊さないようにしました**。AWS の囮は `[default]` をやめて別のプロファイル名に、Docker の囮の宛先は Docker Hub をやめて実在しないレジストリ名にしました。権限は 0600 です。`ssh`・`scp`・`sftp` など OpenSSH のクライアントと、RoamSwitch 自身のアクセスは、実行ファイルのパスで判定して誤検知しません。アクセス元のプロセスは、通知クラスの fanotify でイベント時点に特定します。旧版が設置した囮は、起動時に新しい内容へ移行します(本物のファイルには触れません)。
- **改善: 外部コマンドを絶対パスで起動します**。`PATH` 探索の失敗による監査ログのノイズを解消しました。`/proc/net/route` は直接解析し、不要なコマンドの起動を減らしました。
- **修正: 詳細設定が、未知のキーを保存時に捨てていた不具合**。GUI に、QUIC 遮断・open の同期スキャン・キーボード自動解放のチェックボックスを追加しました(10言語)。

### 1.10.3

- **改善: 同じ通知が繰り返しポップアップされる問題を抑えました**。同一（タイトル・本文）の通知は
  10分間、ポップアップと IPC キューへ出さず、通知履歴にだけ記録します（履歴は全件残ります）。
  緊急度が低い通知は履歴のみで、ポップアップしません。Docker の危険な構成の通知も、同じイメージ・
  同じ理由の連続通知を抑えます（理由が変われば、すぐに通知します）。
- **修正: DNS トンネリング検知が `clamav.net` の TXT 応答（freshclam の定義確認）を誤検知していた
  不具合**。この TXT は検知の対象から外しました（NULL レコードや高エントロピーの応答は、従来どおり
  検知します）。再通知の間隔は 300→600→1200→1800 秒と、段階的に延びます。通知本文と
  タイムラインに、対象のドメイン（apex）を追加しました。
- **改善: 開発用ビルドの生成物（`target/debug`・`target/release` 配下）への YARA 誤検知で、
  通知と承認要求を出さないようにしました**。ClamAV が同意せず、実行の拒否も伴わない場合は、
  履歴にのみ記録します。ClamAV が同意しない同じディレクトリ・同じファミリの検知は、30分に1回に
  集約します。
- **改善: Arch 系ディストリビューションでは、「自動セキュリティアップデート」の診断を「非該当」として
  扱います（減点なし）**。ローリングリリースの無人の `pacman -Syu` は、部分アップグレードによる
  起動不能を招きうるため、公式に推奨されていません。利用者が自動更新を有効にしている場合は、
  通常どおり評価します。Arch 系以外の挙動は変わりません。
- **修正: 特定の環境で、開発者のホームディレクトリの固定パスを参照する箇所を取り除きました**。
  `HOME` が未設定のとき、MCP サーバーの設定ファイルの探索と、アプリのスキャン対象が、固定のパスに
  フォールバックしていました。ユーザー名を引けない UID は、固定のユーザー名でコマンドを実行せずに
  スキップします。

### 1.10.2

- **修正: Server Edition の未知ポート露出ガードが、同居する RoamSwitch Sensor 自身のペアリング用
  制御ポート（50543）を、起動時のタイミング次第で誤って永続ブロックしてしまう不具合**。このガードが
  「まだ知らない公開ポート」のベースラインを取得するタイミングが、Sensor が制御ポートで待ち受けを
  開始するより一瞬早いと、Sensor自身の制御ポートを新規の不審なリスナーとして遮断し、以後デーモンを
  再起動するまで解除する手段がありませんでした。ファイアウォールが既に常時許可しているポート
  （`ssh_ports`・`allowed_ports`）は、検知タイミングに関わらず自動遮断の対象から除外するようにしました。

### 1.10.1

- **修正: `wsdd`のようにインタプリタ経由でランダムなエフェメラルUDPポートへ再バインドするプログラムが、
  再起動のたびに別ポート扱いされ、通知が無限に繰り返されていた不具合**。ポートを含まないフィンガー
  プリント（実行ファイルパス＋スクリプト引数）を新設し、初回のみデスクトップ通知、以降は同一
  フィンガープリントである限り通知履歴への記録のみに抑制するようにしました。個別インシデントの記録
  （MCPの履歴用）は従来通り毎回行います。

### 1.10.0

- **追加: DNSトンネリング/持ち出し検知**（Client / Server Edition 共通）。DNSトンネリングツール
  （iodine・dnscat2等）が使う「ラベルが長く高エントロピー」または「TXT/NULLタイプ」の不審な形の
  クエリを、このホスト自身が送信するDNSクエリの中から検知します。短時間に一定件数を超えたら通知
  （インシデントタイムラインにも記録）。DNS以外のトラフィックは一切検査対象にせず、検知プロセスが
  停止・遅延していても名前解決を妨げない設計です。Server Edition では新規設定キー
  `dns_tunnel_detect_enabled`（既定オン）で切り替えられます。
- **追加: フォレンジック証跡バンドル**（Client / Server Edition 共通）。ランサムウェア・カナリア発火や
  eBPF重大検知などの封じ込め発動時に、プロセス一覧・直近のファイル変更・SHA-256付きマニフェストを
  自動保存します。
- **追加: デセプション/ハニートークン**（Client / Server Edition 共通）。`.aws/credentials`・
  `.ssh/id_rsa`・`.docker/config.json`等に「本物らしい」おとりファイルを配置し、実際にアクセスされたら
  即座に検知・通知します（関連する複数のおとりを連鎖配置）。
- **追加: 脆弱性スキャンの実施状況（経過日数）を確認できるようになりました**。実証型脆弱性診断
  （`scan-vulns --confirm`）の各項目について、最後に確認できた結果と、それからの経過日数を
  `roamswitch vuln-status`・GUIアプリの「ポート」タブ・MCPツールから確認できます。
- **追加: 健全性診断の各項目に、NIST CSF 2.0 / CIS Controls v8 のマッピングを追加しました**（Client /
  Server Edition 共通）。監査対応や法人利用での説明にお使いいただけます。確信を持って対応付けできる
  項目のみに付与し、不確かな項目は空欄のままにしています。
- **修正: まれにCPU使用率が張り付き続けることがありました**。マルウェア検知（YARA）がClamAVへ
  セカンドオピニオンを依頼する際に起動する`clamdscan`自身のファイルオープンを、オンアクセス監視が
  再度検知してしまい、通知の生成が自己増殖的に繰り返される場合がありました。`clamdscan`自身のプロセス
  は監視対象から除外するよう修正しました。
- **改善: `credential_watch`（既定オフ）のAWS向け許可リストに`ansible`/`python3`/`python`を追加**し、
  IaCツール経由の誤検知を減らしました。
- **追加: roamswitch-mcp（MCPサーバー）に、AIエージェント向けの検知結果トリアージ手順（skill）を
  4本追加しました**。ポート異常・実証型脆弱性診断・パッケージCVE・ランサムウェア/インシデント
  タイムラインの優先度判断を、AIエージェントが一貫した手順で行えるようになります。

### 1.9.94

- **改善: 未知のポート待受の通知で、実体が分かるようになりました**。`python3`などの汎用インタプリタ
  経由で待受しているプロセスは、これまで通知に「python3」とだけ表示され、実際に動いているプログラム
  （例: WS-Discoveryの`wsdd`）が分かりませんでした。コマンドラインからスクリプト名を解決し、
  「wsdd (python3)」のように表示します（Client / Server Edition 共通）。

### 1.9.93

- **改善: 脆弱性スキャンログのCSV出力で、安全または判定不能な検査行に脅威の説明が表示されないようにしました**。これまでは「safe（安全）」や「inconclusive（判定不能）」の検査結果に対しても脆弱性検出時の説明文や改善アドバイスが出力されていましたが、結果に応じた説明（「この検査では、問題は確認されませんでした。」「対応は不要です。」など）を10言語で正しく表示するようにしました。
- **改善: 脆弱性スキャンログの記録を安全にし、スキャンIDを追加しました**。ログ追記時のファイルロックを導入して並行書き込みによる破損を防止し、1回のスキャンごとに固有のスキャンID（UUID）を記録して一連の診断結果を識別できるようにしました。また、旧形式ログなどのポート番号が空欄の箇所は「不明」と表示します。

### 1.9.92

- **修正: 通知の一部が日本語のみ（または日本語と英語のみ）でした**。次の通知を、10言語で表示するように
  しました。クライアント: 重要ファイル（FIM）の改ざん検知、依存関係ロックファイルの変更（パッケージ
  マネージャーが動いていたかの説明を含む）、eBPFの低深刻度の検知、カーネル攻撃の検知（Air-Gapの緊急遮断）。
  Server Edition: すべてのアラート（Telegram・LINE・webhook）、テスト通知、Air-Gapの通知。
  eBPF・FIM・ロックファイルの通知に付く精査の要約と、その詳細レポートも10言語です。
- **変更: Server Editionの通知の言語は、設定 `language` で決まります**（`system` は OS のロケール。
  `roamswitch server config set language <ja|en|zh-Hans|zh-Hant|ko|de|fr|es|it|pt-PT|system>`、
  設定キーが無い既存の環境は日本語のままです）。これまでは、英語以外の設定でも日本語で送られていました。
  1台につき1言語です。
- **修正: 2つ目のアプリを起動したときのメッセージが日本語のみでした**。10言語にしました。
- **修正: ヘルスチェックの2項目（ゲートウェイARP、/tmp の noexec）の状態表示が、環境によっては日本語が
  混ざっていました**。すべての表示パターンを10言語にし、全パターンを確認するテストを追加しました。
- **変更: Sensorの手動ペアリング欄で、フィンガープリントを、任意の名前より先に入力する並びにしました**。
### 1.9.91

- **修正: ペアリングに失敗したとき、原因が分からず、コードや指紋の誤りに見えました**。Sensor側の
  ファイアウォールがTCP 50543を塞いでいても、常に「IPアドレス・ペアリングコード・公開鍵を確認して」
  と表示されていました。GUIの待ち時間（6秒）が接続の待ち時間（10秒）より短く、デーモンが返す理由を
  受け取る前に諦めていたためです。待ち時間を30秒にし、接続できない場合はファイアウォールなどの
  原因の候補（Sensor側で確認するコマンドつき）を、応答が全く無い場合は別の文言を表示します。
- **修正: 通知履歴の画面が重くて固まりました**。ログ監査の通知の本文が最大99,000文字あり、全件を
  全文で描画していたためです。通知の本文は保存時に2,000文字までに切り詰め（既存の巨大なエントリは
  読み込み時に切り詰め）、デスクトップ通知の本文は1,000文字までにしました。画面は最新200件を表示し、
  全文はCSVの書き出しで残ります。
- **修正: 同じファイルについて「マルウェアの疑い」の通知が繰り返されました**。承認の重複防止が
  更新時刻を含んでいたため、開発中のファイルを保存するたびに新しい脅威に見えていました。同じファイルと
  脅威の種類の通知は、30分間、繰り返しません（検出が続けば30分ごとに再通知）。実行の拒否や、確認後の
  隔離には影響しません。
- **追加: ARPの再確認が、ゲートウェイの近隣キャッシュが無いときにARPを直接問い合わせます**
  （`arp_recheck_active_probe`、既定で有効）。ARP起因の隔離が、リンクが生きているのに解消と判定されず
  手動解除になる場合を減らします。応答に信頼MAC以外が1つでもあれば不一致で、Ethernet送信元と
  ARP内の送信元が食い違う応答も不一致にします。
- **変更: 画面をタブに分けました**。パッケージCVE照合（6つの照合）と、ポート＆DevIsolator（チェック設定・
  Sensor・ポート一覧）を、タブで切り替えられます。
- **修正: 手動のAir-Gapの記録文が、日本語の固定文でした**。言語設定に従うようにしました（10言語）。

### 1.9.90

- **変更: 高確度の検知で発動した隔離は、時間が来ても自動では解除されなくなりました**。
  カナリアの改ざん、ランサムウェアの疑い、eBPFの重大な検知などによる隔離は、原因が続いている
  まま短いタイマーで全開放されていました。今後は、再確認しても原因が消えていない場合、
  上限（既定1時間）の後に「縮退モード」へ移り、時間では開きません。解除は、`roamswitch
  emergency-restore`・GUI・サーバーの `roamswitch server ack`、または原因の解消を確認できたとき
  だけです。誤検知が多い種類の検知は、これまでどおり短いタイマーで解除されます。
  設定: `airgap_low_confidence_secs` / `airgap_hard_cap_secs`（クライアント）、
  `isolation_hard_cap_secs`（サーバー）。
- **修正: 隔離中も、すでに開いていた接続が生き残っていました**。攻撃者が先に張った通信
  （リバースシェル等）が、隔離後も続く状態でした。サーバー版は、保守用のSSHと許可IP
  以外の確立済み接続をすべて遮断し、クライアント版の縮退モードはループバックのみにしました。
- **追加: プロセス実行の継続記録**（`roamswitch events search|tree|export|verify|status`）。
  実行されたプログラム、親子関係、ハッシュ、コンテナを、端末の中にだけ記録します
  （既定の保持は200MB・14日、ハッシュ連鎖で改ざんを検知）。通知専用の相関ルール7件
  （Webサーバー配下のシェル、一時領域からの実行、ダウンロード直後の実行など）が付きます。
  サーバー版とクライアント版で既定はオンです（メモリ1GiB未満のクライアントを除く）。
  止めるときは `exec_recorder_enabled=false`。外部への送信はありません。
- **追加: イベント転送**（既定オフ）。運用者が指定した宛先にだけ、syslog（RFC 5424、UDP/TCP/TLS）、
  CEF、JSON Lines、HMAC署名つきwebhookで送れます。`roamswitch forward status|test|queue`。
  TLSとwebhookには、システムの `openssl` と `curl` を使います（パッケージの依存に追加しました）。
- **追加: Server Edition の全機能TUI**（`roamswitch server tui`、`roamswitch-server-tui`）。
  CLIの全操作と、`server.conf` と `guard.yaml` の120個の設定キーを、画面から確認・編集できます。
  危険な操作は確認画面つきで、root以外は読み取り専用です。10言語に対応します。
- **追加: Dockerの遮断回避の防止が、nftablesを使うDockerとIPv6にも効くようになりました**。
  Dockerがルールを作り直しても、1分以内に自己修復します。起動前から動いていたコンテナの
  実行監視、FIMの監視範囲（`authorized_keys`、`ld.so.preload`、新規ファイルなど）も広げました。
- **修正（セキュリティ）**: ローカルの一般ユーザーが、Falcoのイベント用ソケットへ偽の重大イベントを
  送って全遮断を起こせました。ソケットの権限を0600にし、接続元のUIDを確認します。
  `sshd-x` のような似た名前のプロセスが保護対象から外れて封じ込めを免れる問題、許可リストを
  プロセス名の偽装で回避できる問題、UID 0のプロセスを遮断すると管理用の通信まで止まる問題、
  隔離の記録が残らない問題も修正しました。許可リストには `--exe` で実行ファイルを紐づけられます。
- **修正（セキュリティ）**: Telegram・LINEのトークンやwebhookのURLが、`curl` の引数として
  プロセス一覧から見える状態でした。標準入力で渡すように変更しました。
- **追加**: `--json` / `--export`（`status`・`report`・`timeline`・`notifications`）、
  凍結したプロセスの再開時に隔離も解除、`sensor pair --fingerprint` と `sensor repin`、
  認証情報ファイル（SSH鍵など）を他のプログラムが開いたときの通知（既定オフ）。
- **変更: RoamSwitch Sensor との連携が、TLS 1.3と証明書の指紋固定に対応しました**。
  新規のペアリングにはSensor 0.3.9以降が必要で、Sensorの指紋を入力します（0.3.8以前のSensorとは
  新規にペアリングできません）。ペアリング済みのSensorは、指紋を固定するまで従来どおり平文で
  動作し、警告が出ます。`roamswitch sensor repin` で固定できます。

### 1.9.89

- **修正: 未知のリスニングポート検知が、システムデーモンの名前で回避できました**。
  `/tmp/sshd` や `/tmp/named` のように、デーモンと同じ名前を付けたバックドアが、検知の
  対象から完全に外れていました。実行パスがシステムのディレクトリ配下で、ファイル名が
  そのデーモンであることを条件にしました。実行パスが一時的に取れない場合は、再起動中の
  正規デーモンを誤検知しないよう、3回続くまで見送ります。
- **修正: インタプリタ経由の待受を一時領域から見分けられませんでした**。
  `python3 /tmp/x/evil.py` は実行パスが `/usr/bin/python3` なので、通常の開発サーバーと
  区別できませんでした。スクリプトが一時領域にある場合は、常に検知し、基準に取り込み
  ません。
- **修正: 開発サーバーの遮断ルールを再適用する間、約16ミリ秒だけ遮断が外れていました**。
  ルールの削除と再作成を別々に行っていたためです。再適用中に接続を試み続けると、
  8112回中7557回が通りました。1回の操作にまとめ、同じ条件で0回にしました。
- **修正: ログの頻度の異常検知が、一度の大きな突発で鈍くなりました**。通常35件のログが
  1万件出た後は、200件の突発を検知できなくなっていました。学習に取り込む値を頭打ちに
  しました。恒常的な増加（35件から50件）は、4回ほどで正常として学習されます。
- **変更: ポートスキャン検知を強化**。偽装した送信元の洪水でメモリが増え続ける問題を
  修正し、待受のあるポートは閉じたポートの1/8として数えて、正常なクライアントの
  誤検知を減らしました。24時間の長い窓で、低速なスキャンも検知します。IPv6は/64単位で
  集約します。
- **変更: 未知のリスニングポート検知の対象を拡大**。ワイルドカード以外のLANアドレスへの
  バインド、一時領域・メモリ上・削除済みのバイナリからの待受を検知します。インタプリタや
  パッケージ管理外のUDP待受は、通知のみで検知します。走査の間隔は5秒です。

### 1.9.88

- **変更: aptとrpmのリポジトリに置くのは、最新の1版だけになりました**。公開の処理が
  過去のすべての版を作り直して残し続けていたため、リポジトリが肥大化して、公開の
  処理が途中で失敗していました。そのため、1.9.86と1.9.87の変更は、aptとrpmでは
  このバージョンから届きます。旧版は、リポジトリから取得できなくなります。

### 1.9.87

- **修正: 偽装SYNの洪水で、ポートスキャンのログが押し流され得た問題**。検知ルールの
  ログ制限が、ログの後ろに書かれていて効いていませんでした。制限をログの前に置き
  （毎秒200件）、超えた分は数えるようにしました。隔離環境で、約7万件/秒の偽装SYNの
  洪水に対して、ログに出るのは約900行に収まります。通常のスキャンや、洪水のない
  高速スキャンの検知は変わりません。
- **追加: 検知の飽和の通知**。SYNの受信が毎秒2000件を超えて、ログの枠を使い切った
  ときに、「この間のポートスキャンを見逃した可能性がある」と通知します（6時間に1回、
  10言語）。洪水で本物のスキャンが隠れることは、有限の枠では避けられないため、
  見逃した可能性を知らせる形にしました。
- **修正: ペアリング済みSensorの免除**。Sensorのアドレスを名乗るだけの偽装スキャンが、
  遮断も強い警告も逃れられました。ペアリング時にSensorのMACを記録して、同じMACから
  届いたSYNだけを免除します（同じセグメントのSensorが対象。ルーター越しは、アドレスと
  偽装でないことの確認のみ）。
- **修正: 遮断の通知文**。「すべての通信を遮断」と書かれていましたが、実際は新規の
  接続だけを遮断します。通知文を10言語で直しました。
- **修正: クライアント版で、Frag Gap対策（ユーザー名前空間の拒否）が「開放」以外の
  すべてのネットワークで適用され、ブラウザ、Flatpak、Electron、rootlessコンテナなど、
  ユーザー名前空間でサンドボックスを作るアプリが壊れていた不具合**。この対策は、
  既定では適用せず、設定（`auto_enable_userns_restriction`）で有効にする、
  オプトインに変更しました。Server Editionが恒久的に無効化する動作は、従来どおりです。
- **修正: root権限のデーモンが、`~/.local` の配下をroot所有で作り、デスクトップが
  不安定になる不具合**。

### 1.9.85 - 1.9.86

- **修正**（1.9.85）: クライアント版の診断が、デスクトップ環境に
  `max_user_namespaces=0` を推奨していた問題。
- **修正**（1.9.86）: VirtualBoxのNAT環境で、ARPの誤検知が再発してAir-Gapが発動する
  不具合（DHCPリースのインフラIPを除外対象に追加）。OS起動時にRoamSwitchが自動起動
  せず、タスクバーに表示されない不具合。ヘルスチェックの助言が、Arch系でもaptを
  案内していた不具合。Air-Gapを解除するたびに、管理者パスワードの認証を求められる
  不具合。

### 1.9.84

- **修正: ポートスキャン検知の自動遮断が、送信元アドレスを偽装した攻撃で
  悪用され得る問題**。SYNスキャンは送信元アドレスが無認証のため、同じ
  ネットワーク上の攻撃者がゲートウェイのアドレスを送信元にしたSYNを
  多数のポートへ送るだけで、こちらがゲートウェイを自動遮断してしまい、
  通信を止められました（隔離した検証環境で、偽装SYN 20本により10分間
  ゲートウェイと通信できなくなることを確認）。次の対策を入れました。
  - 遮断を新規の着信接続だけに絞りました。自分の接続への返信や、確立
    済みの通信は止まりません。
  - ゲートウェイ、DNSリゾルバー、自端末のアドレスは遮断しません。
  - SYNの送信元MACアドレスを近隣テーブルと照合し、偽装と判断した場合は
    遮断を見送ります（検知の通知と記録は行います）。
  - 自動遮断は、これまで通り既定で有効です。Client / Server の両
    Edition に適用されます。

### 1.9.75 - 1.9.83

- **能動的脆弱性診断の対象を拡充**（1.9.82）: Elasticsearch、CouchDB、Jenkins、VNC、
  RDP（NLA）、SMB（SMBv1・署名）のプローブを追加しました（10言語）。
- **Sensorとの同居に対応**（1.9.78〜1.9.79）: Serverのセットアップウィザードに、
  RoamSwitch Sensorとの同居の確認とプリセットを追加し、eBPFガードの許可リストに
  nmapを加えました。
- **自動更新の不具合を修正**（1.9.80〜1.9.81）: nmap NSEのスクリプトDBの定期更新が、
  systemdのサンドボックスで静かに失敗していた不具合と、CVEマップの更新が、共有
  マーカーのせいで1か月待たされていた不具合。自動生成のCVEマップの定型文は、
  10言語に翻訳して出力するようにしました。
- **そのほかの不具合を修正**（1.9.75〜1.9.77、1.9.83）: 通知履歴タブの表示範囲が
  狭められていた不具合。実証型脆弱性診断のテストが、実際のログを汚染していた不具合。
  アップデートのたびにFIM（重要ファイルの改ざん検知）の警告が出る不具合。初回
  セットアップの「完了」ボタンで最終ページへ飛んでしまう不具合と、「適用」の
  フリーズ、NAT環境でのARP誤検知によるAir-Gap発動。

### 1.9.55 - 1.9.74

- **RoamSwitch Sensorとの連携を刷新**（1.9.64〜1.9.70、1.9.74）: ペアリングを、
  mDNSからペアリングコード方式に刷新しました（mDNSは同一LAN内でしか機能せず、常時
  広告し、認証も弱いため）。Sensorへ能動的脆弱性監査をリクエストして、結果を後から
  取得・保存できるようにし、MCPツール `get_sensor_audit_results` を追加しました。
  監査結果は、開放ポート、NSEの結果、確認済みの安全チェックまで、表示・エクスポート
  できます。ペアリング画面の入力欄の並びや、空欄のときのボタンの無効化、表示され
  ない不具合、「手動でペアリング」という紛らわしい表記も直しました。
- **nmap NSE補完スキャンを常時有効化**（1.9.64）。CVEマップ系の自動更新チェックは、
  月1回に変更しました（1.9.65）。
- **調査エージェント連携の拡充**（1.9.55〜1.9.56）: sudoラップの再設計。メモリの
  持続的増加、CPUの高使用率、クラッシュループ、ゾンビプロセス、システム負荷の上昇の
  通知にも対応。エージェント自身の調査行為が、新しいインシデントとして検知される
  再帰の防止。
- **ネットワーク制御の総点検**（1.9.71〜1.9.72）: デーモン再起動の直後に、ファイア
  ウォールの設定が再適用されなかった不具合。共有サービスの自動制御をOFFにしても、
  停止中のサービスが復元されなかった不具合。手動のセキュリティレベルの切り替えと、
  デーモンの3秒ごとの自律巡回が同時に走ると、nftablesの適用が競合する不具合。
- **画面まわりの不具合**（1.9.69〜1.9.74）: CSVの列見出しの10言語対応、GTKの入力欄や
  ボタンが表示されない不具合（`no_show_all` の誤用）、URL安全性診断のボタンが無反応
  だった不具合、オンボーディングの既定のセキュリティレベルが最も緩い「開放」に
  なっていた不具合（「標準保護」に変更）。

### 1.9.48 - 1.9.54

- **RoamSwitch Sensorとの相互ペアリングに必要な自端末公開鍵表示を追加**
  (1.9.48〜1.9.50): CLI(`roamswitch sensor key`)とGUI(Sensorペアリング
  カード)の両方に追加。GUIの自端末公開鍵がラベル選択だけではクリップ
  ボードにコピーできない不具合も修正。
- **調査エージェント連携(eBPF/FIM検知の二次深掘り調査、Server Edition
  限定)を拡充**(1.9.51〜1.9.54): 設定ウィザードのツール選択が既定値を
  憶えていない不具合を修正、対象をeBPF以外(Critical-Path FIM・ロック
  ファイルFIM)にも拡張、FIM検知にも一次自動調査レポートを追加、root
  権限下でのPATH未解決・キーリング未認証により実運用で常に失敗していた
  不具合を修正。

### 1.9.39 - 1.9.47

- **追加(1.9.39)**: タイポスクワッティング検知(npm/pnpm package.json、
  静的検知、Pro) — 有名パッケージ名との編集距離で偽装パッケージの
  可能性を検出。ネットワーク接続不要、判定リストはupdaterパイプライン
  経由で配信。
- **修正(1.9.40)**: ログ監査の頻度急増通知を検知直後に確認できない
  不具合 — 通知履歴に記録するよう修正(Mac版1.9.31と同じ設計変更)。
- **修正(1.9.41)**: カーネルCVE表示が縦1文字に折り返される不具合。
  **改善**: ネットワーク管理タブに見出しを追加。
- **修正(1.9.42)**: 稼働中カーネルCVE判定が、ABIビルド番号のみの
  ディストリ独自バックポートで誤検知していた不具合。
- **追加(1.9.43、Server Edition限定)**: コンテナ内exec検知ガード —
  実際の侵害事例(Docker上のRCE→コンテナ内マイナー実行→自己削除)を
  受けて新設。fanotify `FAN_OPEN_EXEC`で通知専用監視し、自己削除
  されてもイベント自身のfd経由で内容を取得。あわせてEgress Guardの
  forwardチェーン対応(コンテナ発通信の素通り修正)、CPU飽和検知、
  eBPF Runtime Guard稼働判定の誤判定修正も収録。
- **修正(1.9.44、Server Edition限定)**: FIM/Lockfile FIMの再通知
  ループ、ロックファイル改ざん通知の推奨対応の誤り、eBPFアラートの
  「PID: none」誤表示、systemd-executor誤検知、アップデータ検証
  失敗時の誤ったエラー文言を修正。
- **追加(1.9.45)**: eBPFアラートの一次自動調査(誤検知可能性判定+
  Markdownレポート)とエージェント型CLIへの調査ハンドオフ
  (`guard.yaml`の`investigation`、Server Edition、既定オフ、Air-Gap
  直前はスキップ)、env/.envファイル経由のシークレット持ち出し検知。
  Linux版クライアント自身のFIM通知ループ・`lockfile-fim verify`の
  `$HOME`解決不具合も修正。
- **追加(1.9.46)**: セットアップウィザードに調査エージェント連携の
  対話設定を追加(Claude Code / agy / Codex CLI / OpenCode / カスタム)。
- **修正(1.9.47)**: `lockfile-fim`をsudoなしで実行すると分かりにくい
  エラーで落ちる不具合。

### 1.0.0 - 1.9.38（初期リリース〜安定期、まとめ）

- **初回リリース(1.0.0)**: macOS版のゼロトラスト思想をLinux(systemd +
  nftables)へ移植。以降、VPNトンネル(WireGuard/Tailscale)+キルスイッチ、
  未知ポート自動遮断、受動リンク保護、日次脅威フィード、BadUSB/USB
  ストレージ承認、ARPスプーフィング自律検知等の主要防衛機能を順次追加
  (1.0.1〜1.0.66)。
- **RoamSwitch Server Editionを正式導入(1.1.0)**: Falco eBPFランタイム
  脅威検知・自動隔離、Critical Path FIM、サーバー専用25項目セキュリティ
  診断、Webhook/PagerDuty通知。以降、イベント駆動FIM・Egress/C2遮断・
  Tetragonセンサー対応・guard.yamlによる細粒度設定を追加(1.1.1〜1.3.2)。
- **パッケージCVE照合(1.5.0)**、**ログ監査のテンプレート異常検知+
  シークレット自動マスキング(1.6.0)**、**Air-Gap発動ガードのインシデント
  履歴公開(1.7.0)**、**File Scan Guard・Dockerリスク検知(1.4.1〜1.8.0)**
  を追加。
- **Wi-Fiなりすまし(Evil Twin)警告・BadUSB HIDキーストローク異常検知・
  ランサムウェア被害範囲表示(1.9.0〜1.9.5)**、**通知履歴・ClickFix検知・
  ログ監査バックグラウンド実行のクライアント版追加(1.9.6〜1.9.15)**。
- **クライアント版へのCritical Path FIM拡張、リソース枯渇・プロセス
  異常検知ガード(Server Edition)、ログ監査の数値マスキング根本バグ修正、
  Server Editionでの成否偽装バグ修正(1.9.16〜1.9.26)**。
- **サプライチェーン対策4機能(Lockfile FIM・インストールスクリプト
  一覧・npm署名検証・bubblewrapサンドボックス封じ込め)、タイポ
  スクワッティング検知、暗号資産シードフレーズ漏洩検知、Default-Deny
  ファイアウォールのDockerアウトバウンド遮断という重大な不具合の修正
  (1.9.27〜1.9.38)**。


---

## RoamSwitch for Mac

## 1.10.2

- **修正: Tailscale Exit Nodeへの到達性確認（`tailscale ping`）が、DERPリレー経由で正常に
  疎通しているExit Nodeを「到達不能」と誤判定し、正当な手動接続まで切断していた重大な不具合**。
  `tailscale ping`は直接（P2P）接続が確立していない場合、実際にはリレー経由でpongが返っていても
  終了コードが非0になる。終了コードのみで判定していたため、自宅と異なるネットワークにいる際など、
  DERPリレー経由でしか繋がらない環境で正常なExit Node接続まで誤って切断していた。実機でpongが
  返っているのにfalse判定になることを確認し、標準出力の"pong from"も見るよう修正した。
- **修正: Tailscale Exit Nodeが、直近のいずれかのネットワークで一度でも接続失敗（到達不能・経路未
  確認）によるバックアウトが発生すると、それ以降どのネットワークに移動しても自動接続が働かなくなって
  いた不具合**。バックアウト時に立てる抑制フラグがアプリ再起動までリセットされない設計になっており、
  無関係な別ネットワークへの移動時にも抑制が持ち越されていた。ネットワーク変更を検知した時点で
  抑制フラグをリセットするようにした（「今すぐ接続」による明示的な手動接続の意思は維持したまま）。
- **修正: ブラウザ認証情報監視が、アクセス元プロセスを特定できない場合（ブラウザ終了時はほぼ毎回
  発生し、実際の脅威とは無関係）でも、通知回避後もなお履歴への記録を残していた**。特定できない場合は
  記録も含めて一切何もしないよう単純化した（実際の攻撃プロセスは実行中に読み取るため、通常は特定
  可能）。
- **変更: VPN自動接続の発動レベルをLinux版に統一**。「標準保護 (balanced)」ではなく「最大ロック
  ダウン (lockdown)」でのみ自動接続するようにした（WireGuard・Tailscale共通）。「今すぐ接続」に
  よる手動接続はレベルに関わらず維持される。
- **多言語対応: 前回追加したTailscale関連の新規通知文言（経路未保護・ネットワーク復旧失敗）に、
  未翻訳のまま残っていた9言語分の翻訳を追加**。

## 1.10.1

- **修正: `wsdd`のようにインタプリタ経由でランダムなエフェメラルUDPポートへ再バインドするプログラムが、
  再起動のたびに別ポート扱いされ、通知が無限に繰り返されていた不具合（Linux版と同じ設計で移植）**。
  ポートを含まないフィンガープリント（実行ファイルパス＋スクリプト引数）を新設し、初回のみアラート、
  以降は同一フィンガープリントである限り通知履歴への記録のみに抑制するようにしました。
- **修正: 汎用エントロピー型ランサムウェア検知（1.10.0で追加）が、無害な常駐プロセス（Spotlightの
  mdworker等）の存在によって、実際の攻撃プロセスを見逃す場合があった不具合**。安全なプロセスかどうかの
  判定を、候補プロセスを集める段階で行うよう修正し、安全でないプロセスが1件でも見つかれば、無害な
  プロセスと同時に存在していても必ずそちらを優先して検知するようにしました。
- **修正: 認証情報ハニートークン（1.10.0で追加）が、ハニートークンファイル作成後にアプリを再起動すると
  監視が無音で停止していた不具合**。再起動時にも、自分が過去に設置したハニートークンファイルだと
  判定できた場合は監視を再開するようにしました。
- **修正: プロセス実行の記録のDYLD_INSERT_LIBRARIES検知（1.10.0で追加）が、Xcodeでのデバッグ実行の
  たびに誤検知していた不具合**。判定基準を、実行したバイナリ自身の署名ではなく、注入されたライブラリ
  自身が信頼できる場所（システム領域・Xcode.app内）にあるかどうかに変更しました。
- **修正: リンク保護のブランドなりすまし（ホモグラフ）検知が、Apple・PayPalの2ブランドについて、
  一度も機能していなかった不具合**。內部の文字正規化処理に、文字"l"を常に別の文字へ変換してしまう
  誤りがあり、この2ブランド名の判定が構造的に成立しない状態でした。あわせて、`app1e.com`のような
  1文字だけを似た記号に置き換える手口の検知漏れも修正し、`microsoft-security-alert.com`のような、
  ブランド名と「security」「support」等の語を組み合わせた新規ドメインによるなりすまし（フィード側の
  把握が追いつく前の新規登録ドメインで特に有効）を新たに検知するようにしました。
- **修正: Web・メールダウンロード保護（ブラウザ/メールでダウンロードされたファイルの即時スキャン）が、
  起動直後の内部的な不具合により、一度も動作していなかった重大な不具合**。初回起動時にだけ表示される
  フォルダアクセスの案内ダイアログの表示タイミングが原因で、アプリの起動シーケンス中にクラッシュする
  ことがありました。この機能に依存するEICARテスト検知等が効かなかったのは、検知精度の問題ではなく
  この起動時クラッシュが原因でした。
- **修正: 起動直後にゲートウェイのMACアドレス解決が一時的に失敗すると、以後ずっと失敗したまま
  固定され、「現在のネットワークを信頼済みネットワークに登録」が反応しなくなる不具合**。再試行の
  対象を起動直後の初回失敗にも広げ、さらに解決できるまでバックグラウンドで自己回復リトライを
  続けるようにしました。
- **修正: Tailscale Exit Nodeが、tailscaled側の自己申告（接続中の報告）のみを信頼しており、実際の
  通信がExit Node経由になっているかを検証していなかった重大な不具合**。tailscaled自身にmacOS固有の
  既知バグ（Exit Node設定後もOSのデフォルトルートが物理ネットワーク側のままになることがある）が
  あり、RoamSwitchが「保護中」と表示していても実際には保護されていないサイレントな失敗状態が
  発生し得ました。実際のOSデフォルトルートを独立検証し、経路が確立していなければExit Nodeの選択を
  解除した上で通知するようにしました。あわせて、バックアウト時に自動リトライを止め忘れて通知が
  繰り返される不具合、Exit Node切断後のネットワーク復旧処理（DHCP再取得）がネットワークサービスの
  再有効化コマンドの終了コードのみを見て、実際に有効なアドレスを取得できたかを確認せずに成功を
  報告していた不具合（放置するとMacがネットに全く繋がらなくなる場合がありました）も修正しました。
- **修正: ブラウザ認証情報監視が、Chrome/Brave等のHelperプロセス（レンダラー・GPU等）による正当な
  アクセスを不審なアクセスとして誤検知していた不具合**。プロセス名の取得に`lsof`の人間可読形式の
  出力を使っており、長いプロセス名（例:「Google Chrome Helper (Renderer)」）が切り詰められて
  許可リストと一致しなくなっていました。切り詰めのない機械可読形式に変更して修正しました。

## 1.10.0

- **追加: 汎用エントロピー型ランサムウェア検知**。おとりファイルの改ざんに加え、Documents・Desktop・
  Downloads・Pictures フォルダへの書き込みのエントロピー（データのランダムさ）を解析し、短時間に
  大量の暗号化されたようなファイルが書き込まれた場合を検知します。おとりファイルの無い場所での
  暗号化も捉えられます。検知すると、既存のランサムウェア検知と同じくネットワークを緊急遮断します
  （Pro、既定オン）。
- **追加: フォレンジック証跡バンドル**。緊急ネットワーク遮断が発動すると、その時点のプロセス一覧・
  ネットワーク接続・直近の変更ファイルを、SHA-256付きで自動保存します。
- **追加: 認証情報ハニートークン**。`~/.aws/credentials`・`~/.ssh/id_rsa`・`~/.docker/config.json`に
  偽の認証情報ファイルを設置し、アクセスされたら通知します（Pro、既定オン。既存の本物のファイルは
  上書きしません）。
- **追加: ブラウザ認証情報アクセス監視**。Chrome・Firefox等の保存済みパスワード・Cookieに、ブラウザ
  以外のプロセスがアクセスした場合に通知します（Pro内でも既定オフ、任意で有効化）。
- **追加: プロセス実行の記録の検知ルールを2件追加しました**。python3・perl・ruby・php・pwshの
  インライン実行でbase64/evalデコードとcurl/wget等が同じ行に現れるパターン、
  `DYLD_INSERT_LIBRARIES`環境変数によるdylibインジェクションを新たに検知します（Pro）。
- **追加: 脆弱性スキャンの実施状況（経過日数）を確認できるようになりました**。実証型脆弱性診断の
  各項目について、最後に確認できた結果と、それからの経過日数をMCPツールから確認できます。
- **追加: 健全性診断（18項目）に、NIST CSF 2.0 / CIS Controls v8 のマッピングを追加しました**。
  監査対応や法人利用での説明にお使いいただけます。
- **追加: roamswitch-mcp（MCPサーバー）に、AIエージェント向けの検知結果トリアージ手順（skill）を
  4本追加しました**。

## 1.9.54

- **改善: 未知のポート待受の通知で、実体が分かるようになりました**。`python3`などの汎用インタプリタ経由で待受しているプロセスは、これまで通知やインシデント履歴に「python3」とだけ表示され、実際に動いているプログラム（例: `wsdd`）が分かりませんでした。コマンドラインからスクリプト名を解決し、「wsdd (python3)」のように表示します。

## 1.9.53

- **改善: 実証型脆弱性診断ログのCSV出力で、安全または判定不能な検査行に脅威の説明が表示されないようにしました**。これまでは「safe（安全）」や「inconclusive（判定不能）」の検査結果に対しても脆弱性検出時の説明文や改善アドバイスが出力されていましたが、結果に応じた説明（「この検査では、問題は確認されませんでした。」「対応は不要です。」など）を10言語で正しく表示するようにしました。
- **改善: 実証型脆弱性診断ログの記録を安全にし、スキャンIDを追加しました**。ログ追記時のファイルロックを導入して並行書き込みによる破損を防止し、1回のスキャンごとに固有のスキャンID（UUID）を記録して一連の診断結果を識別できるようにしました。また、旧形式ログなどのポート番号が空欄の箇所は「不明」と表示します。

## 1.9.52

- **変更: ネットワーク隔離（Air-Gap）の仕組みを作り直しました**。隔離のきっかけを、確度で分けます。
  ランサムウェアのおとりの改ざん、XProtectの確定検知、ARPの警告に応じた手動の隔離は「高確度」、
  それ以外（ClickFix、ポート異常など）は「低確度」です。低確度は、従来どおり10分で自動解除します。
  高確度は、10分では解除せず、上限（既定1時間）まで全面隔離を続けます。上限で、原因が解消したと
  確認できれば解除し、確認できなければ「縮退モード」に移ります。縮退モードは、新規の通信を止めたまま、
  時間では開かず、利用者の解除か、原因の解消の確認だけで戻ります。隔離の発動時に、すでに開いていた
  外向きの接続も切ります。状態は、ヘルパーの再起動後も続きます。
- **変更: ARPスプーフが原因の隔離は、自動では解除されません**。隔離の間は、ゲートウェイのARPの項目が
  消えるため、原因が解消したことを、通信なしで確認できないためです。手動で解除してください。
- **新機能（Pro）: プロセス実行の記録**。macOS標準の`eslogger`で、プロセスの起動を、このMacの中だけに
  記録します（既定はオフ。ヘルパーにフルディスクアクセスが必要です）。改ざんの検知ができる形で保存し、
  実行のブロックはしません。次の7つの動きを、通知します。ブラウザ・Office・メールアプリが直接シェルを
  起動、一時フォルダや隔離属性つきの署名なしバイナリの実行、ダウンロードしてシェルに渡す一行コマンド、
  base64やevalを使うosascript、隔離属性を外した直後の実行、書き込まれたばかりの自動起動登録から
  署名なしバイナリの起動、Apple以外の親によるキーチェーンの秘密情報の取得。MCPツール
  `search_exec_events`と`get_process_tree`（Pro、読み取り専用）で、記録を検索できます。
- **新機能: 通知の整理と、許可**。実行記録の通知は、重大度が高いものだけバナーにし、同じ種類は10分に1回に
  まとめます（記録には全件残ります）。Claude Codeと`gh`（GitHub CLI）が、自分の認証情報を読む動作は、
  通知しません。ほかのプログラムが、キーチェーンの項目を読んだ場合は、通知の「このプログラムを許可」で、
  その項目だけを許可できます。
- **新機能: SensorのTLSとv2署名に対応しました**。証明書の指紋を固定したTLSで、Sensor 0.3.9以降と
  ペアリング・監査・結果の取得ができます。指紋のないSensorには、従来どおり接続し、警告を出します。
  証明書が変わったときは、指紋の再登録が必要です。自動では、固定を更新しません。
- **修正: 隔離属性を外してから実行するルールが、`/tmp`の下では検知できませんでした**。
- **修正: ランサムウェアの復旧で、失敗の理由が英語のまま表示されていました**。10言語で表示します。
  Sensorの接続エラーも同じです。ペアリングのとき、指紋を間違えると、入力ミスとして案内します。
- **修正: 韓国語・中国語の一部の文言に、日本語用の中黒が混ざっていました**。
- **制約**: 実行記録は、ヘルパーの起動前や、記録を有効にする前の実行を、記録しません。フルディスクアクセスが
  ないと動かず、許可を戻すと、約1分以内に自動で再開します。ヘルパーの許可を外しても、ヘルパーを
  再起動するまでは、反映されません。

## 1.9.51

- **修正: ランサムウェアおとりの検知から緊急遮断まで、数分かかることがありました**。
  不審なプロセスを探す処理が、開いているファイルの全行について、実行ファイルを
  丸ごと検証する署名チェックを繰り返していたためです（この Mac では約2.9万行）。
  おとりや監視対象のフォルダに関係する行だけを、プロセスごとに1回だけ検証するように
  しました。検知の判定基準は変えていません。この Mac では、書き換えから遮断までが、
  数分から約2秒になりました。

## 1.9.50

- **新機能: ランサムウェアの被害から、必要なファイルだけを取り戻せる「ランサムウェア復旧」を追加しました**。
  検知とは無関係に、APFSのローカルスナップショットを既定で6時間ごとに取ります
  （無効・1・3・6・12・24時間から選べます）。検知の後に取ったスナップショットには、
  すでに暗号化されたファイルが入っているため、復旧元にはなりません。検知時にも
  スナップショットを取りますが、その間は新しい事前スナップショットの取得と古い世代の
  整理を最大7日止め、暗号化前の最後の世代が押し出されないようにします。
- **復旧は常に手動で、ファイル単位です**。メニューの「ランサムウェア復旧…」（Pro）から、
  推奨（残っている最新の事前スナップショット）のファイルやフォルダを
  `~/RoamSwitch-Recovered/` へ取り出します。現在のファイルは上書きせず、ボリューム全体の
  復元は提供しません。
- **制約**: 取り出しには、RoamSwitchのヘルパーにフルディスクアクセスの許可が必要です
  （スナップショットの作成には不要）。macOSはローカルスナップショットを約24時間で、
  空き容量が少ないとさらに早く削除することがあります。
- **MCP: 読み取り専用のツール `get_ransomware_recovery_snapshots` を追加しました（Pro）**。
  スナップショットの一覧、推奨、保持モード、取得間隔を返します。復元は行えません。

## 1.9.49

- **修正: 更新直後に、root権限のプログラムのUDP待受を「未知」として通知していました**。
  1.9.48で、特権ヘルパーが初めて見つけた既存のリスナーを基準に静かに取り込むように
  しましたが、TCPだけが対象で、UDPが漏れていました（`tailscaled`などで、更新直後に
  「未知のUDP待受を検知しました」と1回通知されました）。TCPとUDPの両方を取り込みます。
  通知のみで、遮断はされていません。

## 1.9.48

- **修正: root権限のプログラムが開いたポートを、未知ポートの自動遮断が見られませんでした**。
  ログインユーザーの権限で動くアプリからは、root所有のリスナー（リモートから乗っ取られ
  やすいデーモン）が一覧に出ませんでした。特権ヘルパーがrootで一覧を取得し、アプリに
  返すようにしました。ヘルパーが初めて見つけた既存のリスナーは、基準に一度だけ静かに
  取り込みます（更新直後に、自分のVPNなどを遮断しないためです）。
- **修正: 署名の確認結果を使い回す仕組みが、差し替えに気付けませんでした**。署名済みの
  実行ファイルを未署名のものに差し替え、更新日時だけを元に戻すと、古い「署名済み」が
  返っていました。変更時刻・サイズ・inodeも照合するようにしました。
- **変更: リスナーの取得を高速化し、走査を2秒間隔にしました**。プロセスを起動せず
  libprocで取得します（約2.4ミリ秒。従来の`lsof`は約16ミリ秒）。取得できない場合だけ
  `lsof`に戻ります。
- **変更: ポートスキャン検知と未知ポート検知を強化**。偽装した送信元の洪水でメモリが
  増え続ける問題を修正し、待受のあるポートは閉じたポートの1/8として数えて誤検知を
  減らしました。24時間の長い窓で低速なスキャンも検知します。ワイルドカード以外の
  LANアドレスへのバインド、一時フォルダから実行された待受、署名者の変更・署名の破損を
  検知します。インタプリタや署名のないUDP待受は、通知のみで検知します。

## 1.9.44 - 1.9.47

- **能動的脆弱性診断の対象を拡充**（1.9.44）: Elasticsearch / CouchDB / Jenkins / VNC の認証なし露出に加え、
  SMB（smbd）のSMBv1有効化と署名の未要求も確認するようにしました。
- **着信ポートスキャン検知の信頼性を強化**（1.9.45〜1.9.47）: ゲートウェイのアドレスを送信元に偽装したSYNで
  自動遮断が悪用され得る問題を修正し（遮断を新規のTCP接続だけに絞り、ゲートウェイ・DNSリゾルバー・自分の
  Macは遮断対象から除外）、検知が起動直後に停止していた不具合（`pflog0`インターフェースが自動で作られない）
  と、偽装SYNの洪水でpfの状態テーブルが埋まり得た問題を修正しました。検知の処理上限を超えた場合の見逃し
  通知の追加、ペアリング済みSensorからの監査が誤って遮断される不具合の修正も含みます。

## 1.9.33 - 1.9.43

- **RoamSwitch Sensorとの連携を刷新**（1.9.33〜1.9.37）: ペアリングを、mDNSから
  ペアリングコード方式に刷新しました。Sensorへ能動的脆弱性監査をリクエストして、
  結果を後から取得できるようにし、MCPツール `get_sensor_audit_results` を追加
  しました。ペアリングに成功するたびに、特権ヘルパーがクラッシュしていた重大な
  不具合（再入ロックによるデッドロック）を修正しました。監査結果は、開放ポート、
  NSEの結果、確認済みの安全チェック、詳細説明まで、表示・エクスポートできます。
- **nmap NSE補完スキャンを常時有効化**（1.9.33）。CVEマップの自動更新チェックは、
  月1回に変更しました（1.9.34）。
- **アップデートまわりの不具合を修正**（1.9.34、1.9.35、1.9.42、1.9.43）: アップデート
  時に特権ヘルパーが確実に再起動されるようにしました。アップデート直後に、警告の
  通知が何件も出る不具合は、一時的な接続断の自動再試行と、同種の通知の間引きで
  解消しました。アップデート直後に、`/etc/hosts` の改ざんとして警告される不具合も
  修正しています。アップデートの確認が、メニューバーアプリでは目に留まりにくかった
  問題は、アプリを前面に出すようにして改善しました。
- **着信ポートスキャン検知の不具合を修正**（1.9.35、1.9.38）: pfの再読み込みで
  ログ機能が無効になり、`tcpdump` が無限に再試行してヘルパーのリソースを使い続ける
  不具合。有効化に失敗しても、ユーザーに伝わらなかった不具合。
- **共有サービスの停止・復元の不具合を修正**（1.9.38）: `launchctl` の終了コードを
  確認せず、失敗しても「成功」と報告していた不具合。
- **画面まわりの不具合を修正**（1.9.37、1.9.39〜1.9.41）: CSVの列見出しの10言語対応、
  パッケージCVE照合のタブが画面幅からはみ出して最後のタブを開けなかった不具合、
  セキュリティログ監査のフィルターがスクロールできると分かりにくかった問題、
  実証型脆弱性診断のテストが、実際の診断ログに擬似の結果を書き込んでいた不具合。

## 1.9.27 - 1.9.32

- **npm/pnpmインストールの供給網リスク対策を拡充**(1.9.28〜1.9.30、Pro):
  依存関係ロックファイルの改ざん監視・インストールスクリプト一覧・
  npm署名/provenance検証の3機能に続き、実際にインストールをネットワーク
  遮断サンドボックスに封じ込める`roamswitch-npm`ラッパー、編集距離判定
  によるタイポスクワッティング検知(`expres`→`express`等)を追加。
  暗号資産ウォレットのシードフレーズ・秘密鍵漏洩検知(BIP39/WIF/BIP32、
  チェックサム検証付き)も1.9.27で追加。
- **追加: RoamSwitch Sensorペアリング・着信ポートスキャン検知**
  (1.9.32、Pro): 別製品「RoamSwitch Sensor」とのmDNS相互信頼ペアリング
  による能動的脆弱性監査、nmap/masscan等の偵察行為の自動検知・10分間
  自動遮断。通知履歴・パッケージCVE照合・実証型脆弱性診断ログのCSV
  出力も追加。
- **修正: ログ監査の頻度急増通知が直後に確認できない不具合**(1.9.31)、
  **Google AI Studio新形式APIキー(`AQ.`形式)の検知漏れ**(1.9.32)。
- **改善: ログ監査の「新規パターン」検知が毎回ポップアップ通知していた
  点を調整**(1.9.32): 頻度急増を含む検知は引き続き通知、新規パターン
  のみは通知履歴への記録に留めるように。

## 1.9.16 - 1.9.26

- **追加: 重要システムファイルの改ざんを監視するCritical Path FIM**
  (Pro、1.9.16): sudoers・SSH設定・hosts等、OSアップデートやHomebrewでは
  通常触られない低頻度変更パスに絞ったSHA-256ベースライン監視。
- **重要: セキュリティ関連の成否偽装・未検証項目を4件修正**(1.9.19):
  未知ポート遮断・マルウェア隔離の成功通知がXPC呼び出しの完了を待たずに
  出ていた問題、「macOSアクセサリ接続保護」項目が実際の設定を一切確認
  していなかった問題等を修正。
- **ログ監査の誤検知を多数解消**(1.9.16〜1.9.22): loginwindow起因ログの
  個別列挙ではなくクラス単位の構造的判定への変更、数字と英字が隣接する
  箇所でマスキングが効かない根本バグの修正(Linux版と対)、ロック画面
  レンダリング・スリープ/ウェイク・`0x`接頭辞ポインタアドレス等の
  誤検知を個別に解消。
- **修正: Air-Gap自動発動時のWi-Fi無線遮断が、アプリのクラッシュやMac
  再起動後に自動復帰しない不具合**(1.9.23): 独立したAirGapFailsafe監視
  にWi-Fi復帰も統合し、最大10分での自動復帰を実機確認。
- **改善: ログ監査の異常通知を平易化し、テンプレート異常を一覧・検索
  可能に**(1.9.23〜1.9.24): KPIカードのクリック不具合、新規テンプレート
  異常が通知履歴に残らない不具合もあわせて修正。
- **改善: EICARテスト署名検出時の不要な通知を抑制**(1.9.25): 通知は
  出さず通知履歴への記録のみに変更。
- **修正: リンク保護のホモグラフ判定・ランサムウェア/カナリアのMITRE
  ATT&CK ID判定が、翻訳後の文字列比較のため日英仏以外で正しく動作
  していなかった不具合**(1.9.26): 判定を言語非依存化。あわせて
  MCP・SDK・ヘルプ&ガイド・内蔵ナレッジベースを現行機能に合わせて
  全面更新(全10言語対応)し、一部地域ロケールが日本語にフォールバック
  していた不具合も修正。

## 1.9.13 - 1.9.15

- **自動ログ監査(1.9.10で追加)まわりの誤検知・二重通知を解消**: 有効化
  直後のキックオフスキャンは学習のみ行い通知しないよう修正、
  CFPasteboardRefの一時的な接続断ノイズとAppKit内部のフォーカス移動
  KVO通知(`firstResponder changed`)を除外リストに追加、重複する
  スキャンウィンドウによる同一事象の再検知・再通知を解消。
- **ログイン/ログアウト時のPersistentAppsSupport/BTMManagerイベントの
  誤検知を解消**。
- **通知本文を改善**: 内訳(新規パターンN件・頻度急増K件)+読みやすい
  実ログ行の例示を表示するように。

## 1.9.6 - 1.9.12

- **クリップボード上のClickFix型不正コマンド検知を追加**: Script Editor
  経由でTerminalの履歴監視を回避する手口にも対応。
- **通知履歴(過去1週間保持)を追加**。
- **自動ログ監査(Pro)を追加**: 1時間ごとにバックグラウンドで頻度異常の
  ベースラインを学習・通知(既定オン)。
- **ログ監査の頻度急増検知を、テンプレート自身の過去の頻度と比較する
  方式に再設計**: 正規の定期ジョブの誤検知を削減。
- **自動ログ監査の通知文言2件の翻訳漏れを修正**。

## 1.0.0 - 1.9.5（初期リリース〜安定期、まとめ）

- **初期公開(1.0.0〜1.3.0)**: ゲートウェイMACアドレスによる信頼ネット
  ワーク自律識別、ファイアウォール/共有サービス自動切替、未知ポート
  自動遮断、ARPスプーフィング対応、読み取り専用MCPサーバー初搭載。
- **ローカルAI/LLM推論サーバー保護・クリップボード機密保護・MCPサーバー
  のOSS公開(1.4.0〜1.5.9)**。
- **VPNトンネル+キルスイッチ(WireGuard)・予防的ゲートウェイARP/NDP固定・
  BadUSB/USBストレージ承認ガード・受動リンク保護・カナリアおとり
  ファイル永続化(1.6.0〜1.7.6)**。
- **緊急確認ダイアログの最前面化・セキュリティ診断のワンクリック修復・
  VPNデュアルバックエンド(WireGuard/Tailscale)対応(1.8.0〜1.8.3)**。
- **静的シグネチャダウンロード検知・ClickFix対策・Dockerリスク検知
  ガードを新設、Apple XProtect連動の自動Air-Gap隔離、機密情報漏洩監査の
  フォルダ全体スキャン対応(1.8.4〜1.8.9)**。
- **実証型脆弱性診断・パッケージCVE照合をLinux版から移植、Wi-Fiなりすまし
  (Evil Twin)警告、BadUSBキーストローク異常検知、MCPサーバーの機能欠落
  解消(1.9.0〜1.9.5)**。
