# 変更履歴

[English](CHANGELOG.md) | **日本語**

RoamSwitch のユーザー向け変更点。Mac 版（1.x）と
Linux 版（別系列・1.0.x）でバージョン番号は独立しています。

---

## RoamSwitch for Linux

Linux 版（systemd + nftables）。apt / dnf / zypper で配布（GPG 署名）。
詳しくは <https://lafine.net/linux>。

### 1.0.36

- **リンク保護の「警告」モードが、無応答時に *通す* のではなく *ブロック* する
  ようになりました（fail-closed）**。リンク保護を*警告*にして危険サイトを開くと
  判断するあいだ接続が保留されますが、従来は保留時間内に答えないと接続が
  通っていました（fail-open）。今は**ブロック**します —「この危険サイトは
  安全か？」に答えないことを「はい」とみなすべきではないためです。許可／
  ブロックのダイアログは残るので、あとで許可すれば次のリクエストは通ります
  （「警告」モードは既定ではなく、自分からハードブロックすることもありません）。
- リンク保護: タイムアウトした判定を記憶しなくなりました。次にそのホストへ
  アクセスすると、黙ってブロックされ続けるのではなく再度確認されます。
- **アプリの多言語対応（10 言語）を拡充**: 「外出先保護レベル」タブ、「VPN
  トンネル」タブの状態表示、「カナリア防御」タブ、コピーできるセキュリティログ
  監査レポート、Air-Gap ダイアログが、従来は日本語のみ／日英のみでした。
- 不具合修正: 「ランサムウェアプロセスを凍結」の通知（凍結のみで Air-Gap では
  ない）で、Air-Gap の復旧ダイアログが出ることがありました。通知タイトルの
  日本語文字列ではなく言語非依存のタグでダイアログを判定するようにしました。

### 1.0.35

- **リンク保護の「警告」モードが実用的になりました**。リンク保護を*警告*に
  設定して危険サイトを開くと、判断するあいだ接続が保留されます。この保留が
  30 秒で、ブラウザやサーバーが半開き接続を維持する時間より長かったため、
  許可される前に最初のリクエストが失敗していました。保留を**8 秒**に短縮し、
  無応答の場合は fail-open して接続します。許可／ブロックのダイアログは
  残るので、あとで下した選択は次のリクエストに適用されます（「警告」モードは
  既定ではありません）。
- リンク保護のモードや許可／ブロックリストを変更したあと、ホストの危険判定を
  再評価するようにしました（従来はキャッシュされ、古い判定が残ることが
  ありました）。
- リンク保護の通知 5 件（危険なドメインの名前解決・接続の遮断・接続の保留 ×2・
  危険の可能性）を 10 言語対応にしました（従来は日本語のみ）。

### 1.0.34

- **マルウェアスキャンが不意にファイルを隔離しなくなりました**:
  - **EICAR テスト文字列は通知のみになりました。** 業界標準の EICAR テスト
    文字列を含むだけのファイル（セキュリティ手順書、シグネチャの例、当
    プロジェクトのサイト自身など）は情報通知が出るだけで、**隔離も遮断も
    されません**。従来はシステム全体のオンアクセスガードが隔離 Vault へ移動し
    たうえでファイルのオープンをすべて拒否し、git から復元しても隔離ループに
    なっていました。
  - **実シグネチャ検出時はまず確認します。** 無確認で隔離するのをやめ、モーダル
    ダイアログで「隔離」（Vault へ退避）／「許可」（除外リストに追加）／「後で」
    を選べるようにしました。3 分間応答が無い場合はファイルをそのまま残します
    （フェイルオープン）＋通知。実行（`execve`）そのものは「実行前ブロック」が
    有効なら従来どおりカーネル層で拒否します。
  - **スキャン除外パス。** 隔離タブ（および `scan_exclusions` 設定キー）に
    絶対パスの一覧を追加できます。配下も含めて、内蔵 YARA スキャナと ClamAV の
    両方から除外されます。ダイアログで「許可」を選んだとき、および Vault から
    ファイルを復元したときにも自動で追加されます。
  - GUI の**「システム全体 fanotify」**と**「実行前ブロック」**トグルが実際に
    効くようになりました（従来は無効でした）。
- **不具合修正 — 重いビルド中にマシンが重くなる／ネットが切れることがある**:
  ガードが設定ファイルを fanotify permission-event ループ上で読んでおり
  （このループはファイル I/O をしてはいけない）、高負荷時にファイルを開く
  すべてのプロセスが待たされていました。設定は専用のバックグラウンド
  スレッドで読むようにしました。
- ダウンロードガード／ランサムウェア凍結の通知を 10 言語対応にしました
  （従来は 6 言語＋英語フォールバック）。
- ホワイトペーパーを v1.5 に更新（§5.2、§5.4、§5.5、SP-4）。

### 1.0.33

- **1280x720 画面での余白最適化 & VPN / DNS ドロップダウン幅の修正**:
  - 1280x720 などの解像度においてウィンドウが画面からはみ出さず、周囲にゆとりのある余白（横約360px、縦約200px）を確保できるよう、ベースウィンドウ寸法と CSS スケーリング係数を最適化しました。
  - VPN 設定（Tailscale Exit Node / バックエンド切替）および DNS 設定のプルダウンメニューが画面いっぱいに横方向に広がりすぎていたレイアウトを修正し、自然な幅（260〜320px）で左揃えに整えました。

### 1.0.32

- **パッケージマネージャー（apt / dpkg / rpm / dnf / pacman 等）のランサムウェア誤検知防止**:
  - `apt upgrade` やパッケージ展開時（`dpkg`、`rpm` 等）に大量の圧縮ドキュメント（`.gz.dpkg-new` 等）が展開される際、ランサムウェア暗号化バーストと誤判定されてプロセスが凍結（`SIGSTOP`）される問題を修正しました。
  - ランサムウェア・エントロピー監視の免除リストおよび緊急凍結除外リストに Linux の主要パッケージマネージャー・インストーラー群（`dpkg`, `apt`, `rpm`, `dnf`, `pacman`, `zypper`, `flatpak`, `snapd` 等）を追加し、一時展開拡張子（`.dpkg-new`, `.rpmnew` 等）を高エントロピー判定から除外しました。

### 1.0.31

- **Tailscale VPN 接続処理・キルスイッチ適用順序の最適化 & VPN タブ UI の改善**:
  - Tailscale Exit Node 設定（`set_exit_node`）をキルスイッチ有効化より先に実行するよう順序を最適化し、キルスイッチ通信許可ルールに STUN (UDP 3478) および WireGuard (UDP 41641) を追加しました。これにより接続時のパケット遮断によるタイムアウト・フリーズを解消しました。
  - VPN タブの状態取得処理をバックグラウンド非同期チャネルへ移行し、「今すぐ接続」「切断」実行中のボタン感度制御を追加して UI の応答性を向上させました。
  - GTK3 コンテナの可視性制御を改修し、Tailscale 設定パネル内の Exit Node 選択項目が常に正常に表示されるように修正しました。

### 1.0.30

- **Ubuntu / GNOME 環境での起動直後タスクバーアイコン表示の即時反映**:
  - Ubuntu（GNOME AppIndicator）において起動直後にアイコンが「...」と表示される問題を修正しました。
  - アプリ起動直後の SNI シグナル即時配信（`update`）と、GNOME Shell が直接参照する `/usr/share/pixmaps` へのアイコン配置およびテーマパス解決を最適化し、起動直後から正しい保護モードアイコンが表示されるように改善しました。

### 1.0.29

- **UI スレッドの完全非同期化（手動保護モード切替・VPN 接続時のフリーズ解消）**:
  - メインウィンドウでの手動保護モード切り替え（Lockdown / Balanced / Open / Auto）および VPN 接続・切断操作時の IPC 通信を非同期スレッド（`call_daemon_ipc_bg` および `glib::MainContext::channel`）で処理するように改善しました。
  - デーモン側でのファイアウォール適用や共有サービス制御、VPN 適用処理中も GTK メインスレッドが一切ブロックされず、ウィンドウが瞬時に応答するようになりました。

### 1.0.28

- **信頼ネットワーク・標準保護モード時の VPN 挙動を最適化（待機状態を維持）**:
  - 信頼済みネットワーク接続中および「開放」「標準」保護モードでは、VPN（Tailscale Exit Node / WireGuard）を自動接続せず「待機」状態を維持するように変更しました。
  - VPN の接続は「今すぐ接続」ボタン押下時、または「最高保護（Lockdown）」モードへ切り替わった時のみ実行されます。

### 1.0.27

- **Tailscale Exit Node 適用時の引数フォーマットを最適化（ローカル実機検証済み）**:
  - Tailscale CLI（`tailscale set --exit-node`）の仕様に合わせて、完全修飾ドメイン名だけでなく Tailscale IP アドレス（`100.x.y.z`）およびホスト名を自動解決して渡すフォールバック処理を実装しました。
  - 実機ローカル環境にて `vpn_up`、`vpn_down`、保護モード変更（開放 ⇔ 最高）時の Tailscale Exit Node 連動・キルスイッチ設定・状態同期が完全に動作することを確認しました。

### 1.0.26

- **Tailscale Exit Node 適用処理と稼働状態（経由中／待機中）の判定ロジックを強化**:
  - `tailscale set --exit-node` コマンド実行時に SSH 警告を回避するフラグ（`--accept-risk=lose-ssh`）の自動フォールバックを追加し、Exit Node の適用失敗を防ぐよう改善しました。
  - Exit Node のホスト名・DNS名・ノードIDの照合処理（`find_node` / `matches`）を刷新し、Tailscale の JSON ステータス（`ExitNodeStatus` / Peer `ExitNode` / 内部State）と確実に同期して `✅ Exit Node: 経由中` が正しく反映されるようにしました。

### 1.0.25

- **タスクバー／システムトレイアイコンの動的切り替えの確実性を向上**:
  - 全デスクトップ環境（GNOME / KDE / XFCE / Wayfire / Sway）において、保護モード（開放・標準・最高・Air-Gap）変更時にトレイアイコンがリアルタイムに切り替わるよう、SNI 仕様に準拠した `icon_pixmap` ARGB レンダリングおよびテーマパス解決を強化しました。

### 1.0.24

- **Tailscale バックエンド選択時に WireGuard の設定ボタンが混ざって表示される UI 不具合を修正**:
  - GTK コンテナの `show_all` 伝播により、Tailscale 選択中にも WireGuard 設定用ボタン（「WireGuard設定(.conf)を読み込む」「設定を削除」）が表示されてしまう問題を修正し、選択中のバックエンド専用の UI のみが確実に表示されるよう改善しました。

### 1.0.23

- **Snap 版 Tailscale（`/snap/bin/tailscale`）の自動検出に対応**:
  - Ubuntu 等で Snap パッケージ経由でインストールされた Tailscale（`/snap/bin/tailscale`, `/var/lib/snapd/snap/bin/tailscale`）をデーモンが自動検出し、ネイティブパッケージ同様にシームレスに Exit Node 制御およびキルスイッチを連動できるようにしました。

### 1.0.22

- **セキュリティレベル切り替え時の VPN / Tailscale 自動連動および手動接続操作を改善**:
  - セキュリティレベル（Balanced / Lockdown）を手動で切り替えた際に、設定済みの VPN（Tailscale Exit Node / WireGuard）が自動で適用・連動するようデーモン処理を修正しました。
  - Tailscale バックエンド選択時にも「今すぐ接続」「切断」ボタンを操作可能にし、信頼済みネットワーク上からでも Exit Node トンネルを即座にテスト・手動適用できるように改善しました。
  - Exit Node のホスト名・DNS名および大文字小文字の差異を吸収し、トンネル稼働状態（`✅ Exit Node: 経由中` / `待機中`）が正しく画面に反映されるよう判定ロジックを最適化しました。

### 1.0.21

- **VPN バックエンド切り替え（WireGuard ⇔ Tailscale）時の UI 連動とメインウィンドウのフリーズを修正**:
  - WireGuard と Tailscale を切り替えた際に、説明文や案内カードが WireGuard のまま残る問題を修正し、選択されたバックエンド（Tailscale / WireGuard）に合わせて説明・ガイドが動的に切り替わるよう改善しました。
  - VPN 画面のウィジェット更新時に発生していた GTK シグナルの再帰ループを解消し、メインウィンドウがフリーズする不具合を完全に修正しました。

### 1.0.20

- **VPN 画面（Tailscale / WireGuard）のデーモン通信不具合および Exit Node 入力 UI を修正**:
  - VPN タブで Tailscale および WireGuard の状態取得時に「デーモンに接続できません」と誤表示され設定が読み込めなくなる IPC レスポンス解析の不具合を修正しました。
  - Tailscale の Exit Node 選択欄を入力対応コンボボックスに改修し、Tailscale が検出した候補からのドロップダウン選択に加え、任意のホスト名や IP アドレスをキーボードから直接手動入力できるように改善しました。

### 1.0.19

- **ランサムウェア暗号化検知しきい値の適正化と判定ロジックのリファクタリング**:
  - 個別アプリのパス除外ルールに依存するアプローチを廃止し、実際のランサムウェアの攻撃特性（大量一括暗号化・高エントロピー）に即した本質的な判定しきい値（**5秒間に20ファイル、エントロピー 7.92 以上**）へ適正化しました。
  - 通常のデスクトップアプリや開発ツールの単発ファイル生成による誤検知を防止しつつ、実際のランサムウェアによる一括暗号化攻撃のみを確実に捕捉します。
  - 過剰な個別アプリパス除外リストを整理・スリム化し、よりシンプルで堅牢なセキュリティアーキテクチャに改善しました。
  - ※カナリア監視（おとりファイル改変監視）および全書き込みに対する YARA マルウェア検査は引き続き稼働します。

### 1.0.18

- **Chrome / Chromium 実行時のランサムウェア誤検知および Air-Gap 誤発動を防止**:
  - Chrome / Chromium（Raspberry Pi OS 標準の `chromium-browser` 等）や Firefox、Brave が起動時・閲覧時に生成するディスクキャッシュ（`~/.cache/`）、GPU シェーダーキャッシュ、IndexedDB（LevelDB の `.ldb` ファイル等）をランサムウェア暗号化判定の対象から除外しました。
  - 主要ブラウザ（`chromium-browse`, `chrome`, `google-chrome`, `firefox` 等）および Raspberry Pi OS のデスクトップ環境（`wayfire`, `labwc`, `wf-panel-pi`, `pcmanfm-pi` 等）を暗号化監視の保護対象リストに追加し、誤ったプロセス凍結（SIGSTOP）や Air-Gap 発動を防止しました。
  - ※YARA によるマルウェア検知はゼロバイパスで引き続き漏れなく全件実施されます。

### 1.0.17

- **Raspberry Pi OS / Linux デスクトップでのタスク・トレイアイコン表示不具合を修正**:
  - `roamswitch-open.svg` 等のステータス別 SVG アイコンがパッケージインストール時に確実に配置されるよう修正しました。
  - `.deb` パッケージの依存関係に `librsvg2-common` を追加し、Raspberry Pi OS（Wayfire / Labwc 等）での SVG 描画を保証しました。
  - アプリ実行時のアイコン探索フォールバックを強化し、未インストール状態（`cargo run` 等）でも正常にアイコンが表示されるようにしました。
- **未知ポート自動遮断時の通知・確認ダイアログの改善**:
  - `0.0.0.0` の未知ポートが自動遮断された際、画面上に確認ダイアログが表示され、ダイアログ上の「このポートを許可する」ボタンからワンクリックで恒久許可リストに登録できるようになりました。
  - アラートキューのタイムスタンプ同期をミリ秒単位に修正し、イベント通知の取りこぼしを解消しました。
- **ファイル保護の検知精度と堅牢性の向上（YARAマルウェア検査と暗号化検知の分離）**:
  - `~/.cache/` や `node_modules/`、各種アーカイブ等を含め、すべての書き込みファイルに対して YARA マルウェア検知を漏れなく実施するよう保護を強化しました。
  - 圧縮アーカイブ（`.tgz`, `.zip` 等）や正規パッケージマネージャー（npm, cargo 等）のキャッシュ展開によるランサムウェア高エントロピー誤検知（SIGSTOP）を防止する精緻な判定ロジックを導入しました。

### 1.0.16

- **VPN のバックエンドを WireGuard と Tailscale から選べるようになりました**
  （「VPN トンネル」タブのバックエンド選択）。
  - **WireGuard**: これまでどおり設定ファイル（`.conf`）を読み込みます。
  - **Tailscale**: すでに Tailscale を使っている場合、**Exit Node**（全通信を
    経由させる出口ノード）を選ぶだけで、未信頼ネットワーク接続時に自動で全通信を
    その Exit Node 経由にし、キルスイッチを有効にします。ログインは事前に
    `sudo tailscale up` で済ませてください。Exit Node を指定しないと中間者攻撃
    対策にはなりません（「保護されていません」と表示されます）。選んだ Exit Node
    がオフラインになった場合は保護を自動解除して通知します。
  - Tailscale のキルスイッチは、単一エンドポイントに固定できない性質上
    WireGuard よりゆるめです（`tailscale0` / tailnet（100.64.0.0/10）/ STUN /
    DERP(443) / DHCP / MagicDNS のみ許可、それ以外は DNS も含め全遮断）。
  - `tailscale` パッケージを Recommends / optdepends に追加。
- **独立ネットワーク名前空間で Tailscale キルスイッチの動作を検証**（SP-10、
  16/16 PASS）: トンネル外の平文通信・ローカル DNS が遮断され、DERP 形の
  通信・MagicDNS は許可されること、有効化／無効化の往復で疎通が正しく戻ること。

### 1.0.15

- **不具合修正 — VPN トンネルが Ubuntu 24.04 以降で接続できない**: WireGuard の
  設定を `/etc/roamswitch/wireguard/` に保存していましたが、Ubuntu 24.04 以降 /
  Debian 13 の AppArmor が `wg-quick` にその場所の読み取りを許可しないため
  トンネルを張れませんでした。標準の `/etc/wireguard/roamswitch.conf` に
  変更しました。
- **不具合修正 — キルスイッチの適用失敗**: トンネル用インターフェースが
  作成される前にキルスイッチ（nftables）を設置する際、インターフェース名の
  照合方式が原因で適用に失敗することがありました。
- **保護プロファイル切替の通知に「理由」を追加**しました。ロックダウン等に
  切り替わったとき、「未登録ネットワークのため」「登録済みネットワーク
  「〇〇」の設定」「ネットワーク未接続のため」のいずれかを表示します
  （10 言語対応）。
- **実機同等の検証を実施**: 独立したネットワーク名前空間で、予防的 ARP/NDP
  ロックが偽の ARP を無視すること、キルスイッチがトンネル外の通信を遮断し
  ながら WireGuard のハンドシェイクを通すこと、トンネル切断時も漏れが無いこと、
  切断時に元通り復旧することを確認しました。

### 1.0.14

- **不具合修正**: 手動で保護レベルを固定（「次回切断まで」）していると、
  PC を起動するたびに「手動オーバーライドを解除しました」と表示され、
  設定が解除されていました。同じネットワークに再接続した場合は解除されず、
  実際に別のネットワークへ移動したときだけ解除されるようになりました。
- **apt の Notice を解消**: `apt update` のたびに表示される
  「リポジトリ … がアーキテクチャ 'i386' をサポートしないため …
  main/binary-i386/Packages の取得をスキップ」の Notice を出さないよう、
  apt ソース行を `arch=amd64,arm64` に限定しました（このバージョンへの
  アップグレード時に既存の `/etc/apt/sources.list.d/roamswitch.list` も
  自動修正されます）。手動で直す場合は次の 1 行です:
  `sudo sed -i 's|deb \[signed-by=|deb [arch=amd64,arm64 signed-by=|' /etc/apt/sources.list.d/roamswitch.list`

### 1.0.13

- **VPN トンネル（キルスイッチ付き）を追加**しました（「VPN トンネル」タブ）。
  WireGuard の設定ファイル（`.conf`）を読み込むと、カフェや公衆 Wi-Fi など
  未信頼ネットワークに接続したときに自動で暗号化トンネルを張ります。
  **キルスイッチ**により、トンネルが確立するまで（および切断時）は
  トンネルとハンドシェイク以外の通信をすべて遮断するため、ARP/NDP
  スプーフィングやパケット盗聴があっても内容は漏れません。これが中間者
  攻撃対策の本命で、ARP/NDP 固定・検知はその補助という位置づけです。
  信頼済みネットワークではトンネルを張りません。`wireguard-tools` が必要です
  （`sudo apt install wireguard-tools` 等）。

### 1.0.12

- **ARP スプーフィング検知時の挙動を Mac 版に合わせました。** ロックダウン中は
  これまでどおり即座に通信を全遮断（Air-Gap）しますが、バランス／信頼済み
  ネットワークでは**いきなり遮断せず通知でお知らせ**し、RoamSwitch から
  「緊急遮断（Air-Gap）」を実行できます。ルーター再起動やアクセスポイント
  切り替えでの誤発動を防ぎ、攻撃者が偽の ARP パケット 1 つで通信を止めさせる
  のも防ぎます。
- **不具合修正**: ARP スプーフィング自動遮断が、設定でオフにしていても
  動作していた問題を修正（デーモンが設定のスイッチを見ていませんでした）。

### 1.0.11

- **ゲートウェイ ARP 固定を「予防的 MITM 対策」として強化**しました。未信頼
  ネットワーク（カフェ・公衆 Wi-Fi）に接続すると、IPv4 ゲートウェイに加えて
  **IPv6 デフォルトルーターと同一ネットワーク上の DNS サーバー**の MAC アドレスも
  カーネルの近隣テーブルに固定し、ARP/NDP スプーフィングによる中間者攻撃を
  成立させないようにします。信頼済みネットワークでは固定しません。ネットワークを
  切り替えるたびに固定し直します。
- **不具合修正**: この設定が「ゲートウェイのみ・全ネットワーク」で動作していた
  問題を修正（未信頼ネットワークに限定）。

### 1.0.10

- **「アップデート」タブに「今すぐアップグレード」ボタンを追加**しました。
  更新確認で新しいバージョンが見つかったとき、ボタン 1 つで
  （管理者パスワードの入力後）apt / dnf / zypper 経由のアップグレードを
  実行できます。従来どおりコマンドをコピーして端末で実行することも可能です。
- **macOS 版との挙動の差異を一括で見直しました**（`docs/MAC_PARITY.md`）。
  - ガードの有効・無効を切り替えるときの確認ダイアログを、macOS 版と
    同じ対象（ポート異常・ARP 自動封鎖・USB ストレージ・BadUSB キーボード・
    Bluetooth）で復活させました。1.0.9 で確認ダイアログを撤去した際の
    「毎回起動時に確認ダイアログが出る」不具合は、根本原因（GTK の仕様で
    プログラム的な状態同期でもトグル通知が発火する）を修正した上で
    再実装しています。Bluetooth は macOS と同じく「有効化時のみ確認」です。
  - **USB ストレージガードと Bluetooth ガードの初期状態を「オフ」に**変更
    （macOS 版に合わせました。どちらもオンにすると確認ダイアログが出ます）。
  - DNS 脅威保護のオン・オフには確認ダイアログを出さないよう変更（macOS 版と一致）。
- **不具合修正**: Bluetooth ガードと「ゲートウェイ ARP ロック」を無効に
  していても、未信頼ネットワークに接続すると動作してしまう問題を修正
  （デーモンが設定のスイッチを見ていませんでした）。

### 1.0.9

- 設定でガードのスイッチを操作していないのに、アプリ起動時や画面更新のたびに
  「有効にしますか／無効にしますか」の確認ダイアログが表示される不具合を修正
  （1.0.7 の実装ミス）。確認ダイアログは、不明な USB キーボードの接続・
  USB メモリの挿入・緊急遮断（Air-Gap）といった**イベント時**にのみ表示されます。
- **未登録の USB メモリを挿したときに承認ダイアログを表示**するようにしました。
  「許可」でそのデバイスを使用可能にして許可リストに登録、「拒否」で保留を継続
  します（USB キーボードの承認と同じ仕組み）。

### 1.0.8

- 初回セットアップ ウィザードが最後の「適用」でフリーズすることがある不具合を
  修正（ウィザードが自身の適用処理中に自分を閉じようとして再入していた）。
- **インストール後、次回ログイン時にセットアップ ウィザードが自動起動する**
  ように変更（`/etc/xdg/autostart` エントリを同梱）。セットアップ完了後は
  トレイ常駐のみになります。
- セットアップ ウィザードの未翻訳箇所を修正（外出先保護レベルの選択肢が
  英語のままだった、ページ2の本文がタイトルと同じだった）。

### 1.0.7

- **重要な不具合を修正**: BadUSB 対策の USB キーボードガードが、許可リストに
  無いキーボードを検知した際に、そのデバイスをカーネルレベルで物理的に切断
  していました。キーボードとマウスが一体の受信機（Raspberry Pi やミニ PC で
  一般的）では**マウスも一緒に切断**され、内蔵入力の無いマシンが操作不能に
  なることがありました。今回:
  - キーボードの物理切断を廃止。代わりに、未承認キーボードの**キー入力だけ**を
    一時的に遮断し（デバイスは通電したまま、マウス・他のキーボードは通常どおり
    動作）、**承認ダイアログ**を表示します（Mac 版と同じ方式）。「許可」で
    入力を有効化・許可リストに登録、「拒否」で遮断を継続。3 分間応答が無ければ
    自動で解放します（ヘッドレス機で詰まらないため）。他に使えるキーボードが
    無い状況では遮断しません。
  - USB マウスをキーボードと誤認して遮断していた不具合も修正。
  - 旧バージョンで切断されたデバイスは、更新後の初回起動時に自動的に復旧します。
  - BadUSB キーボードガードの既定を**無効**に変更（初回セットアップで
    ホワイトリストを登録してから有効化してください）。
- **ガードの ON/OFF に確認ダイアログを追加**（Mac 版と同じ）。ARP 自動遮断・
  ポート異常ガード・USB ストレージ／キーボードガード・Bluetooth 自動オフ・
  DNS 脅威保護・リンク保護のモード弱体化で確認を求めます。

### 1.0.6

- **ポート異常ガードを追加**（既定で有効）。有効化時点で待ち受けている
  プログラムを「既知」として学習し、以降 **新しく現れて LAN に公開された
  未知のプログラム**（バックドア／C2、うっかり `0.0.0.0` で起動した開発
  サーバー等）を検知して、外部からのアクセスだけを自動遮断します
  （このマシン自身・localhost からの利用はそのまま）。通知が出るので
  自分で起動したものなら「ポート & DevIsolator」タブから許可できます。
  ネットワークの信頼可否に関わらず動作します。`config.json` の
  `portAnomalyGuardEnabled` を `false` で無効化（自動遮断も全解除）。
  「ベースラインを再学習」ボタンで学習し直せます。
- 同梱の脅威フィードが空だったのを修正。新規インストール直後から
  フィッシング／詐欺サイトの遮断が効くようになりました（従来は初回の
  日次アップデータ実行または `apt upgrade` まで、ヒューリスティック判定
  のみ）。
- リンク保護を `off` に切り替えても内部のファイアウォール補助テーブルが
  残ることがある不具合を修正。
- UI 文言を整理（Linux 版は全機能が無料の Community Edition なので、
  一部に残っていた「(Pro)」表記を削除）。

### 1.0.5

- **受動リンクガードを追加**（既定で有効）。外向き接続の宛先を監視し、
  フィッシング／詐欺サイトへの接続を警告します。明らかなフィッシング
  （脅威フィード掲載・ブランド偽装ドメイン）は既定で遮断し、通知から
  ワンタップで許可できます。判定はオフラインのヒューリスティック（IDN の
  混同文字、IP 直打ち、高リスク TLD など）で行い、対象を外部に送信しません。
  URL を貼り付けて診断する従来機能を置き換えます。off / warn / block は
  設定タブまたは `config.json` の `linkGuard.mode` で切り替えられます。
- **日次アップデータを追加**（`roamswitch-update.timer`）。1 日 1 回、
  署名付きのフィッシング サイト一覧・ClamAV 定義バージョン・本体の最新
  バージョンを受信します。受信専用で、利用者や端末に関する情報は一切
  送信しません（クエリ文字列・Cookie・識別子なし）。
  `systemctl disable --now roamswitch-update.timer` または `updates.json` の
  `"enabled": false` で完全に停止でき、その場合はパッケージ同梱のデータ
  のみで動作します。
- OS 起動直後などにデスクトップ全体が数十秒フリーズし、RoamSwitch が
  クラッシュ（自動復帰）することがある不具合を修正。暗号化されたローカル
  データを短時間に多数書き込むアプリ（Telegram など）をランサムウェアと
  誤検知していたのが原因です。あわせて誤検知の除外範囲を広げました。

### 1.0.4

- 「アップデート確認」タブの表示不具合を修正（案内コマンドが表示されないことがある問題）。
- 「リリースノートを開く」が製品ページを開いていたのを修正し、公開の変更履歴
  （GitHub の CHANGELOG）を開くように変更。
- 言語を切り替えてもウィンドウ上部の副題だけ旧言語のままになる不具合を修正。

### 1.0.2

- 共有サービスの自動停止・復元をデフォルト有効に。未信頼ネットワークで
  SSH / Samba / リモートデスクトップを停止し、信頼ネットワーク復帰で復元
  （Mac 版と同じ挙動。ネットワークタブまたは `roamswitch sharing off` で無効化可）。
- 保護レベル名を全10言語に対応。`config.json` の `level_labels` で任意の名前に変更可。
- ネットワーク登録名を全10言語に対応し、一覧の名前セルをダブルクリックでリネーム可能に。
- URL 安全性診断タブに機能の説明を追加。
- 総合セキュリティ診断のスコア表示を大きく見やすく改善。
- About のバージョン表記が更新されない不具合を修正。実装詳細の文言を整理。

### 1.0.1

- 初回配布リリース。apt（`lafine.net/apt`）/ dnf・zypper（`lafine.net/rpm`）
  リポジトリを RSA-4096 で署名して公開。GitHub Releases に `.deb` / `.rpm` /
  署名付き tarball を添付。
- Arch は AUR パッケージ `roamswitch-bin` を準備中（当面は同梱 PKGBUILD からビルド）。
- パッケージビルド CI の不具合を修正。

### 1.0.0

- 初版。macOS 版のゼロトラスト・ネットワーク防衛思想を Linux 向けに完全実装。
- 接続中の Wi-Fi / 有線ネットワークをゲートウェイ MAC で識別し、`nftables`
  プロファイルを自律切替（信頼 / 標準保護 / 外出先保護）。
- ランサムウェアの挙動検知（fanotify + シャノンエントロピー + カナリア）と
  緊急 Air-Gap 隔離、不正 USB / BadUSB ガード、20 項目のセキュリティ健全性診断、
  読み取り専用の MCP サーバーを標準搭載。
- 無償 Community Edition（プロプライエタリ・フリーウェア）。全10言語。

---

## RoamSwitch for Mac

## 1.8.1

- リンク保護の「警告のみ」モードを改善。保留中の接続の「許可／ブロック」を、
  通知だけでなく前面のパネルでも確認できるようにしました（macOS の通知スタイルが
  「バナー」だとボタンがホバーしないと出ないため）。パネルのデフォルト操作は
  安全側の「ブロック」です。
- 「警告のみ」モードで、以前に手動ブロックしたサイトが誤って即遮断されることが
  あった問題を修正（警告モードは強制遮断しません）。

## 1.8.0

- **リンク保護をコンテンツフィルタ機能拡張で強化**（Pro）。名前解決の後に
  実際の外向き接続を見るため、ブラウザーが独自に DNS 暗号化（DoH／DoT）を
  使っていてもフィッシング／詐欺サイトへの接続を遮断できます。TLS の
  ClientHello（SNI）も検査します。初回のみシステム設定での承認が必要です
  （Apple の申請は不要）。承認前・拒否時は従来の `/etc/hosts` 方式が
  フォールバックとして動きます。
- **「警告のみ」モードが本物の警告に**。疑わしい接続を検知すると、機能拡張が
  その接続を一時停止し、「許可／ブロック」ボタン付きの通知を出します。
  ボタンを押すと保留中の接続を再開または遮断します（25 秒無応答なら通します）。
  判断はサイト単位で記憶するので、同じページの他の通信や再訪はすぐ反映されます。
- **VPN トンネルのバックエンドを WireGuard／Tailscale から選択可能に**
  （Pro・既定オフ）。「ポート・デバイス監視」→「VPN トンネル」→
  「バックエンド」。Tailscale を選ぶと、既存の tailnet の **Exit Node** を
  指定して未信頼ネットワークで全通信をトンネル経由にできます。RoamSwitch は
  `tailscale up`／ログイン／導入は行わず、状態を読んで Exit Node を設定する
  だけです。CLI 版（`brew install tailscale`）を推奨します。
- ヘルパーを 1.8.2 に更新。初回起動時に再承認を求めます。

## 1.7.6

- **VPN トンネル（キルスイッチ付き）を追加**（Pro・既定オフ）。WireGuard の
  設定ファイル（`.conf`）を読み込むと、未信頼ネットワークに接続したときに
  自動で暗号化トンネルを張ります。pf キルスイッチにより、トンネルが確立する
  まで（および切断時）はトンネルとハンドシェイク以外の通信をすべて遮断する
  ため、ARP/NDP スプーフィングやパケット盗聴があっても内容は漏れません。
  これが中間者攻撃対策の本命で、ARP/NDP 固定・検知はその補助という位置づけ
  です。信頼済みネットワークではトンネルを張りません。Homebrew の
  `wireguard-tools`（`brew install wireguard-tools`）を使用します（Apple の
  Network Extension エンタイトルメント不要）。「ポート・デバイス監視」→
  「VPN トンネル」から設定できます。

## 1.7.5

- **予防的なゲートウェイ ARP/NDP 固定を追加**（Pro・既定オフ）。カフェや公衆
  Wi-Fi など未登録ネットワークに接続したとき、ゲートウェイ・IPv6 ルーター・
  同一ネットワーク上の DNS サーバーの MAC アドレスを固定し、ARP/NDP
  スプーフィングによる中間者攻撃を**未然に**防ぎます。「ポート・デバイス監視」
  サブメニューの「未信頼ネットワークでゲートウェイの ARP/NDP を固定（予防）」
  から有効化できます。
- **ARP スプーフィング自動遮断の挙動を見直し**。ロックダウン中はこれまで
  どおり即座に全遮断しますが、バランス／信頼済みネットワークでは**いきなり
  遮断せず通知でお知らせ**し、メニューの「今すぐ全遮断する」から実行できます。
  ルーター再起動やアクセスポイント切り替えでの誤発動を防ぎ、攻撃者が偽の
  ARP パケット 1 つで通信を止めさせるのも防ぎます。

## 1.7.4

- **USB ストレージガードが未知のドライブを即座に取り出さなくなりました。**
  許可リストにない外部ドライブは読み取り専用でマウントし、承認プロンプト
  （「読み書きで許可」「読み取り専用で許可」「取り出す」）を表示します。許可
  すると許可リストに登録し、選んだ権限を（通常どおり ClamAV スキャン後に）
  適用します。Linux 版の挿入時プロンプトと同じ方式で、誤って取り出して作業中の
  データを失う心配がなくなります。

## 1.7.3

- 1.7.2 で追加した「リンク保護」がメニューバーに表示されない不具合を修正。
  遮断エンジン自体は 1.7.2 でも動作していましたが、モード切替（オフ／警告のみ／
  自動ブロック）・詐欺サイト一覧の版数・自動更新トグルの UI が出ていませんでした。
  「マルウェア対策」サブメニュー内の「リンク保護」から操作できます。
- 手動チェックの項目名を「リンクを手動でチェック…」に統一。

## 1.7.2

- 🔗 受動リンク保護（フィッシング接続の自動遮断）を追加（Pro）:
  - 既知の詐欺サイト一覧とブランド偽装ドメイン判定に基づき、フィッシング／詐欺サイトへの接続を端末側で自動遮断します。特権ヘルパーが `/etc/hosts` の管理セクションに対象ドメインを `0.0.0.0` で追記する方式で、Apple の Network Extension 申請は不要。ブラウザ・アプリを問わず遮断されます。
  - メニューバーの「リンク保護」から「オフ」「警告のみ（遮断しない）」「明らかな詐欺サイトは自動でブロック（推奨）」を選択可能。**本バージョンから標準は「自動ブロック」**。遮断対象は「脅威フィード掲載」または「ブランド名のホモグラフ偽装」の明確なケースのみで、それ以外は警告に留めます。誤遮断は通知またはメニューからワンクリックで許可（5分間 or 恒久）できます。
  - 判定エンジンと脅威フィードは Linux 版と共通。
- 📡 脅威フィードの署名鍵をフィード専用の Ed25519 鍵に分離（本体アップデートの署名鍵とは別鍵）。フィードは 1 日 1 回、受信専用で取得します（送信なし・識別子なし）。「自動更新」をオフにすると外部通信はゼロになり、同梱データ（約280件）＋ホモグラフ検知で動作します。
- 🌐 新規 UI・ヘルプ文言を全 10 言語に対応。

## 1.7.1

- 🚀 初回セットアップ（特権ヘルパー承認）の改善:
  - RoamSwitch が「アプリケーション」フォルダの外（ダウンロードフォルダやディスクイメージ内など）で実行されている場合、macOS の仕様上ヘルパーを登録できないため、承認手順に進む前に検知して案内する画面を追加。「アプリケーションに移動して再起動」ボタンで解決できます。
  - ヘルパーの承認が完了しない場合のリカバリ導線を追加: 登録失敗時の再試行ボタン、承認画面での正確なスイッチ位置の案内、ヘルパーが見つからない場合の再ダウンロード案内。
  - ヘルパー未接続のとき、メニューバーに「⚠️ ヘルパーを承認する…」項目を表示し、設定画面へ直接誘導。
  - 承認されないまま数日が経過した場合、端末内で1回だけ「まだ有効化されていません」という控えめな通知を表示（外部送信なし）。
  - 新規の画面・文言を全 10 言語に対応。

## 1.7.0

- 🔌 不正USB / BadUSB 物理キーボード承認ガード（`USBKeyboardGuard`）の追加:
  - IOKit（`IOHIDManager`）により、外付けキーボードや改造USBケーブル（Rubber Ducky, O.MG Cable等）の物理ポート接続をリアルタイム検知。
  - MacBookの内蔵キーボードは自動的にホワイトリスト登録され、日常利用を一切妨げません。
  - 未承認のキーボード・ケーブルが挿入された瞬間に `CGEventTap` によりキーストロークを一時的に破棄（ドロップ）し、悪意ある自動コマンド注入攻撃を水際で防止。
  - 最前面の「キーボード承認モーダル」から、ユーザー自身が確認してワンクリックで安全に許可可能。
- 🛡️ USB / BadUSB ガード設定画面の刷新と一元化:
  - 「キーボード / BadUSB」と「USBストレージ」の許可リストをタブで直感的に切り替えて管理可能。
  - アクセシビリティ権限状態のリアルタイム自動検知と設定画面ダイレクト誘導を実装。
- 📊 セキュリティヘルスチェックの強化:
  - 「不正USB / BadUSB 物理ポートガード」および「macOS アクセサリ接続保護（Apple Silicon）」の診断項目を追加。
- 🌐 全10言語の多言語ローカライズ対応:
  - 新規UIおよびダイアログ文言を日本語、英語、ドイツ語、フランス語、スペイン語、イタリア語、韓国語、ポルトガル語、簡体字中国語、繁体字中国語で完全サポート。

## 1.6.4

- ランサムウェアおとり（カナリア）ベースラインハッシュのディスク永続化：
  - 各カナリアファイルのベースラインハッシュをディスク上に永続化（`UserDefaults`）。
  - 通常の起動時・整合性チェック時に消失したファイルを不用意に自動再生成せず、記録されたベースラインに基づき厳密に改ざん・削除を検知。
  - ユーザーが緊急隔離画面から「緊急隔離を解除」を実行した際にのみ、正規テンプレートからのおとり自己修復（再生成）を実行。

## 1.6.3

- メニューバーアイコンの表示安定化と手動切り替え時の即時反映：
  - 手動オーバーライド切り替え時および診断実行時の状態監視を強化し、処理中アイコン（`arrow.triangle.2.circlepath`）の残存を解消。
  - 診断処理（`isDiagnosing`）にタイムアウト保護を追加し、バックグラウンド処理の完了漏れによるスタックを防止。
- 隔離ファイル（Quarantine）の安全保護強化：
  - 隔離フォルダ（`~/Library/Application Support/RoamSwitch/Quarantine`）へ退避された脅威ファイルに対し、実行・読み取り権限を完全に剥奪（`chmod 000`）する保護処理を追加。

## 1.6.2

- 未知ポート自動遮断における旧バージョン互換データの自動クリーンアップ：
  - 起動時に `KnownExecutablesV2` を自動スキャンし、ポート番号サフィックス（`:port`）のない旧形式のインタプリタ登録（`"Python"` 等）を自動マイグレーション・削除。
  - 新旧バージョン混在時における未知ポート検知の完全性を保証。

## 1.6.1

- ランサムウェア緊急隔離発動時の重複通知抑止：
  - エアギャップ隔離中（モーダル表示中）に 60 秒定期整合性ポーリングが重複して緊急防護通知を送信していた問題を修正。
  - すでに隔離中の場合は定期スキャンおよび追加通知を一時停止し、通知スパムを解消。

## 1.6.0

- ClamAV ダウンロード検疫の同名ファイル衝突時フォールバック隔離：
  - 既に隔離フォルダ（Quarantine）に同名ファイルが存在する場合でも、ClamAV の移動スキップを自動検知し、一意のタイムスタンプ付きファイル名で確実に隔離フォルダへ強制退避・隔離。
  - 同名脅威ファイルの再配置時における「検疫漏れ（ALIVE残存）」を完全に解消。

## 1.5.9

- ClamAV ダウンロード検疫の完全網羅（バイパス経路の完全遮断）：
  - `.tmp` 拡張子ファイルの即時スキャン対応：悪意あるスクリプトやツールによる `.tmp` 名でのダウンロードも即時検疫。
  - Desktop / Documents フォルダへのコピー・直接配置検知：`quarantine` 属性が付与されないターミナル経由（`cp`, `curl`, `wget`）や USB 経由のファイル配置も漏れなく即時スキャン・隔離。

## 1.5.8

- リモートログイン（SSH）および共有サービス自動復元の精度向上：
  - macOS 13/14/15 launchd において `com.openssh.sshd` の稼働状態判定（`launchctl print` / `print-disabled` / `systemsetup`）を多重強化。
  - ロックダウン解除時および信頼ネットワーク復帰時、`launchctl load -w` に加えて `/usr/sbin/systemsetup -setremotelogin on` を連携実行し、リモートログイン（SSH）を確実に復旧。
- ClamAV ダウンロード検疫の重複隔離・多重通知防止：
  - FSEvents イベント重複による同一ダウンロードファイルの多重スキャンおよび隔離フォルダ（Quarantine）への重複保存バグを修正。
  - 隔離中・隔離直後のファイルパスキャッシュを導入し、安定した即時検疫を実現。

## 1.5.7

- ペネトレーションテスト（実環境監査）に基づくセキュリティ防護機能の設計強化：
  - **ランサムウェア対策（おとりファイル自律修復）**: 隔離解除（緊急対応完了）時に、消失・改ざんされたカナリア（おとり）ファイルを自動的に再生成・ハッシュ修復し、kqueueリアルタイム監視を即座に復元する自己修復機構を導入。
  - **未知ポート自動遮断の厳格化（Living-off-the-land攻撃防止）**: Python、Node.js、Ruby、PHP、Netcat などの汎用スクリプトインタプリタ・シェルについて、バイナリ単体ではなく「実行パス＋ポート番号」で個別識別。同一インタプリタであっても新規ポートでバックドアやサーバーが立ち上がった際は確実に個別検知・自動遮断。
  - **初期ベースライン汚染の防止 & リセット機能**: ガード初回有効化時の未確認ポートに対する過度なホワイトリスト化を防止し、ベースライン再学習・リセット機能を追加。
  - **Web & Mail ダウンロード保護（ClamAV）の隠しファイル検知強化**: 単なるOSメタデータ（.DS_Store 等）のみを除外し、実体を持つ隠しファイル（.hidden_eicar.txt 等）の流入時にもリアルタイムスキャンが確実に発火するよう改善。

## 1.5.6

- 緊急防護・隔離解除時の共有サービス（SSH / SMB / 画面共有）自動復旧の改善：
  - ARPスプーフィングやランサムウェア活動検知時の緊急隔離（Air-Gap / ロックダウン）が発動した後、安全を確認して隔離を解除した際に、以前有効だった共有サービス（リモートログイン・SSHやファイル共有等）が確実に元の有効状態へ自動復旧するように修正。
  - ヘルパーデーモンにおけるサービス状態の永続化と重複呼び出し時の保護ロジックを強化。
- 緊急セキュリティモーダル表示時およびバックグラウンド監視時のパフォーマンス改善：
  - ポート監視およびランサムウェア監視におけるプロセス探索処理を非同期バックグラウンド化し、UIメインスレッドの応答性を改善（虹色カーソル・フリーズの防止）。
  - macOS標準システムデーモン（photoanalysisd等）の安全判定を強化し、誤判定を防止。

## 1.5.5

- Web & メールダウンロード保護（ClamAVリアルタイムスキャン）の検知・通知改善：
  - ClamAVによる悪質ファイル検知時のパス解析ロジックを改善。感染ファイル検知時の隔離処理に加え、メニューバー「直近のスキャン履歴」への脅威表示および高優先度バナー通知（「🚨 危険なダウンロードファイルを隔離しました」）が確実に発火するように修正。

## 1.5.4

- 重要セキュリティガード機能（自動遮断）OFF時の確認・警告画面を追加：
  - 「ARPスプーフィング自動遮断」「未知のリスニングポート自動遮断」「不正USBストレージ自動遮断」などの重要な防御機能を無効化（OFF）にする際、リスク説明と確認ダイアログ（全10言語対応）を表示するように改善。
  - 誤操作による保護機能の停止や、リスクを把握しないままの無効化を防止。
- メニューバーUIの改善：
  - SwiftUI版メニューバー内にもARPスプーフィング自動遮断・未知ポート自動遮断のトグルを配置し、操作性を統一。

## 1.5.3

- MCP（Model Context Protocol）サーバー連携の強化：
  - `get_exposed_ports` ツールでローカルAI推論サーバー（Ollama, LM Studio, Gradio, vLLM）の露出検知・リスク評価を網羅。
  - `get_guard_status` および `get_app_help` でクリップボードAPIキー保護・Pickle形式AIモデルダウンロード保護の仕様・ガイダンス提供に対応。

## 1.5.2

- APIキー / シークレット誤送信防止チェッカー（クリップボード保護）：
  - OpenAI, Anthropic, GitHub, AWS, HuggingFace, Google Gemini等のAPIキーやSSH秘密鍵がクリップボードに保持された際、端末ローカル（Zero Telemetry）で正規表現スキャンし注意喚起。WebやAIチャットへの誤貼り付け・情報漏洩を防止。
- 危険なAIモデル形式（Pickle / PyTorch）のダウンロード保護：
  - HuggingFaceやCivitaiからダウンロードされた `.pkl` / `.pickle` / `.pt` ファイルの任意コード実行リスクを検知し、安全なSafeTensors / GGUF形式の利用を推奨。

## 1.5.1

- ローカルAI / LLM サーバー露出検知＆保護ガード（Ollama / LM Studio / Gradio / vLLM）：
  - Ollama（ポート 11434）、LM Studio（1234）、Gradio / AI WebUI（7860）、vLLM（8000）などのローカル推論サーバーが誤って `0.0.0.0`（LAN全体）でバインドされた際の露出リスク検知・プロアクティブアラート機能を追加。
  - 同一Wi-Fiからの未認証モデル実行・ダウンロード・削除、GPUリソース不正利用、機密プロンプト盗聴リスクを未然に防止し、`pf` による即時遮断および対処手順を案内。
- クラッシュ自動復旧（Watchdog）機能を追加：
  - アプリが予期せず終了・クラッシュした場合に自動検知して再起動する常駐監視エージェント（LaunchAgent）を導入。
  - 指数バックオフ（2秒、4秒、8秒、16秒、32秒）による再起動と、連続クラッシュ回数制限（最大5回）による無限ループ防止機構を搭載。
  - 自動再起動の実行時および上限到達時に macOS ネイティブ通知（UNUserNotificationCenter）で状態を通知。
  - ユーザーが明示的に終了（Quit）した場合は再起動を行わず正常停止。

## 1.4.8

- オンボーディング画面でヘルパーツールのシステム承認手順を動画で分かりやすく案内するよう改善。

## 1.4.7

- 「未知のリスニングポートを自動遮断」の通知に **「許可する」ボタン**を追加。LocalSend や Syncthing
  など、ガードを有効にしたあとに起動した正規の LAN 受信アプリが遮断された場合、通知バナーからその場で
  恒久的に許可できます（`anchor apple` を満たす macOS 標準のシステムデーモンは、これまでどおり監視対象外）。
- アプリ内ヘルプと MCP ナレッジベースを更新（自動遮断系ガードは Pro 有効化時に既定でオン、誤検知からの
  戻し方）。

## 1.4.6

- オープンソース化：
  - 同梱の読み取り専用 MCP サーバーと、その検知ロジックを MIT ライセンスで <https://github.com/lafine1211/roamswitch-mcp> に公開。サーバーが AI クライアントに何を渡しているか、外部送信がないことをコードで検証できます。
- 不具合修正：
  - 細工された JSON-RPC を 1 行送るだけで MCP サーバーがクラッシュしうる不具合を修正（深くネストした JSON オブジェクトで Apple の JSON パーサがスタックオーバーフロー）。パース前に過度にネストした入力を弾くようにしました。オープンソース化後にパーサをファジングして発見。詳細はリポジトリの `SECURITY_TESTING.md`。

## 1.4.5

- セキュリティ監査・堅牢化：
  - 特権ヘルパーとの XPC 通信において、PID 再利用攻撃（TOCTOU）を防ぐため `audit_token` ベースのクライアント検証および Developer Team ID ピン留めを強化。
  - パケットフィルタ（pf）の一時設定ファイルの配置先を特権保護領域に変更し、安全性を向上。
  - リンク安全性診断において、プライベート IP / ローカルネットワーク宛てへの不正な追従を防ぐ SSRF ガードを追加。
  - マルウェア隔離管理において、同名ファイルが存在する場合のメタデータ整合性と誤削除防止を強化。

## 1.4.4

- MCP サーバー機能の拡充：
  - アプリの全機能・通知メッセージ解説・トラブルシューティングを網羅した公式ナレッジベースを追加。
  - 新規ツール `get_app_help` を追加し、AI エージェントからアプリの仕様やエラー解説を自然言語検索可能に。
  - MCP リソース（`roamswitch://docs/...`）の提供を開始。

## 1.4.3

- ARP スプーフィング検知時とランサムウェア検知時の緊急ネットワーク遮断（Air-Gap）が、
  ポート自動遮断機能と併用したとき正しく作動しない不具合を修正。遮断の適用を検証し、
  失敗時はモーダルに正直に表示するようにした。
- 「未知のリスニングポートを自動遮断」が、待受ポートを変える正規プロセス（Handoff 等の
  macOS 標準機能を含む）を誤って遮断する不具合を修正。macOS 標準のシステムデーモンは
  対象外にした。
- 「未知のリスニングポートを自動遮断」を無効化したとき、この機能が作成した遮断ルールが
  残る不具合を修正。
- Pro ライセンス有効化時、「未知のリスニングポートを自動遮断」と「ARP スプーフィング
  検知時に自動遮断」をデフォルトで有効にするようにした（以降はユーザー設定を尊重）。
- 自律巡回に 1 日 1 回のウイルススキャンを追加（正常時は通知、脅威検出時は自動隔離＋警告）。
- ClamAV スキャンが正常終了したときは、ダイアログではなく通知で知らせるようにした。

## 1.4.2

- MCP サーバーのプロトコルバージョン折衝と JSON-RPC バッチ対応。

## 1.4.1

- アプリ内ヘルプ＆ガイドに Web/メール保護・DNS 脅威保護・リンク診断・監視フォルダ編集の解説を追加。

## 1.4.0

- Web・メール3重保護ガード（ダウンロード自動スキャン・DNS 脅威保護・リンク安全性診断）を追加。
- MCP 新ツール、監視フォルダのカスタム編集画面（デフォルト復元付き）を追加。

## 1.3.0

- 読み取り専用 MCP サーバーと 0.0.0.0 公開 DB ポートの即時アラートを追加。

## 1.2.0

- 未知のリスニングポート自動遮断、ARPスプーフィング自動対応ガードを追加。
- USB 接続時の ClamAV スキャンをヘルプに明記。

## 1.1.x

- オンボーディングにログイン時自動起動ステップを追加し、ヘルパー未接続を解消。

## 1.0.0

- 初回公開リリース。ゲートウェイ MAC アドレスによる信頼ネットワーク検知、プロファイル別の
  ファイアウォール / ステルス / 共有サービス / AirDrop 自動切り替え、Mac セキュリティ診断。
